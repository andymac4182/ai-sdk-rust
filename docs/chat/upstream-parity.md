# Vercel Chat SDK Rust Parity Ledger

_This ledger tracks the Rust port of [`vercel/chat`](https://github.com/vercel/chat) and is maintained by the `/goal` session driven from [`scripts/codex-goal-chat/`](../../scripts/codex-goal-chat/)._

> Sibling project: the AI SDK port lives in [`docs/upstream-parity.md`](../upstream-parity.md). That ledger is owned by a different `/goal` session - do not edit it from a chat-sdk slice.

## Upstream Source

- Repository: `github:vercel/chat`
- Fetch command: `npx opensrc@latest fetch github:vercel/chat`
- Local cache: `~/.opensrc/repos/github.com/vercel/chat/main`
- Inventory commit: `aba6aa94fe5a2ed909ec4daa7db0e21887507fa4`
- Inventory commit message: `feat(slack): add api primitives subpath (#548)`
- Inventory commit date: `2026-05-22T00:58:40Z`
- Inventory fetched: `2026-05-23T05:41:18Z`

## Status Legend

| Status | Meaning |
| --- | --- |
| `not-started` | Upstream package/feature identified, no Rust counterpart yet. |
| `in-progress` | Some Rust scaffolding/typed contract exists; not test-parity yet. |
| `ported` | Rust implementation exists and runs deterministic fake/mock tests, but adapter live validation or full test-floor mapping is still pending. |
| `verified` | Strict 1:1 Rust crate exists, every portable upstream TypeScript test/case is mapped to a Rust test, and adapter live validation has been recorded if credentials exist. |
| `js-only-documented` | Surface is intentionally JavaScript-only (e.g. JSX runtime, browser-only UI, Next.js example); justification is recorded in the row notes. |

## Test Floor

EVERY portable original upstream TypeScript test/case must exist as an equivalent Rust test in the matching 1:1 `chat-sdk-*` crate. Rust may add more tests, but never fewer mapped original TypeScript tests; a package with even one missing portable upstream test/case is incomplete.

Upstream test extensions counted: `*.test.ts`, `*.test.tsx`, `*.test-d.ts`, `*.test-d.tsx`, `*.spec.ts`, `*.spec.tsx`. Test counts in the inventory below are file counts at the inventory commit; the test-case parity map (later in this file) tracks the individual cases inside each file.

## Required Work Order

Two-phase gate, enforced strictly:

1. **Phase 1 - Core/shared.** `packages/chat` (the unified surface), `packages/adapter-shared` (utilities used by every adapter), `packages/tests` (test factories/matchers used by every other package), and `packages/state-memory` (deterministic in-memory state backend usable by every adapter test). No adapter package may start while any phase-1 row is `not-started` or `in-progress`.
2. **Phase 2 - Adapters.** All 11 `adapter-*` packages.
3. **Phase 3 - Production state backends.** `state-redis`, `state-ioredis`, `state-pg`.
4. **Phase 4 - Integration tests, examples, skills, docs.** `integration-tests`, examples, skills, apps/docs.

## Package And Provider Inventory

| Item | Kind | Status | Rust path | Evidence | Notes |
| --- | --- | --- | --- | --- | --- |
| `packages/chat` (`@chat-sdk/chat`) | core SDK package | in-progress | `crates/chat-sdk-chat` | `crates/chat-sdk-chat/src/{errors,logger,chat_singleton,markdown,cards,modals,emoji,callback_url,plan,message,types}.rs::tests` (17 + 13 + 5 + 75 + 29 + 25 + 42 + 5 + 10 + 10 + 9 + 4 + 76 colocated tests). errors/logger/chat-singleton are 1:1 with upstream `*.test.ts`. markdown ports 33 of 122. cards ports 29 of 28 (full data shape + card_to_fallback_text + card_child_to_fallback_text). modals ports 25 of 29 cases (20 portable + 5 additive coverage; the 9 JSX `fromReactModalElement` cases are deferred js-only-adjacent in the module). select/radio_select now panic on empty options matching upstream `throw`. emoji ports 42 of 42 cases (EmojiResolver from_slack/from_gchat/from_teams/to_slack/to_gchat/to_discord/matches/extend + DEFAULT_EMOJI_MAP + DEFAULT_EMOJI_RESOLVER + WELL_KNOWN_EMOJI + get_emoji singleton via Arc + EmojiCatalog + convert_emoji_placeholders + create_emoji). Also fixed EmojiValue::placeholder() to return upstream `{{emoji:name}}` format (was incorrectly `:name:`). types is additive Rust-only serde coverage + AdapterPostableMessage untagged enum union with From<T> impls. | 54 src files, 23 test files upstream. Crate skeleton + 7 source modules. 9/23 upstream test files partially mapped (emoji 1:1 complete; modals 20/20 portable; callback-url **17/17 portable** (5 pure helpers + 6 processCardCallbackUrls + 3 resolveCallbackUrl + 3 postToCallbackUrl via injected `HttpPoster` trait — slice 278 lands the upstream-shape `{ status?, error? }` result over an async-trait abstraction so callers plug their own HTTP client without forcing a workspace runtime decision); thread Per-thread state describe block **8/8 portable** (slice 279 adds `Thread::with_state_adapter` / `state()` / `set_state()` / `set_state_replace()` + `THREAD_STATE_KEY_PREFIX` const matching upstream's `THREAD_STATE_TTL_MS`-backed shallow-merge semantics); channel basic-properties + state-management describe blocks **7/7 portable** (slice 280 adds `Channel::is_dm` / `Channel::with_options` / `Channel::state()` / `set_state()` / `set_state_replace()` + `CHANNEL_STATE_KEY_PREFIX` const + `CHANNEL_STATE_TTL_MS` const); chat shutdown sequence **5/5 portable** (slice 281 adds `Adapter::disconnect` + `StateAdapter::disconnect` default no-op trait methods + `Chat::shutdown()` orchestration that runs every adapter disconnect first, then state, swallowing per-adapter disconnect errors so a single failing adapter cannot block shutdown); chat initialize sequence **1/1 portable** (slice 282 adds `Adapter::initialize` + `StateAdapter::connect` default no-op trait methods + `Chat::initialize()` orchestration); transcripts-wiring construction-validation describe block **5/5 portable** (slice 286 extends `ChatOptions` with `transcripts` + `identity` fields + `IdentityResolver` trait + `Chat::try_new` returning `Result<Self, ChatBuildError>` + `Chat::transcripts()` panicking getter + `try_transcripts()` non-panic accessor; throws/panics at construction when transcripts is set without identity to match upstream's `IDENTITY_REQUIRED_RE`); streaming_markdown.rs **38/51 portable** (slice 283 ports 10 render cases + 5 additive helpers; slice 284 ports 18 more render cases; slice 285 ports 4 getCommittableText cases + simulate_append_stream helper + 6 append-only delta integration cases (plain-text / table-wrap-in-code-fence / wrap-disabled / table-flushed-on-finish / monotonic-deltas / real-world-table-streams-correctly). **13 remend-dependent upstream cases js-only-documented in module header** — they exercise the `remend` npm package's inline-marker healing for `getCommittableText` with unclosed `**`/`*`/`~~`/`` ` ``/`[` markers (no Rust workspace equivalent)); thread-history 7/8 portable (the JS-only `re-exports under the old name` test is module-loader-specific - chat-sdk-chat's Rust module system has no equivalent); message 14/19 portable subset (slice 288 changed `MessageSubjectResolver::resolve` to collapse `AdapterError::Unsupported("fetch_subject")` to `Ok(None)` matching upstream's optional-chaining `adapter.fetchSubject?.(...)` null cascade; +1 case for default-trait-impl behavior); cards 29/28 with fallback text; markdown 122/122 (1:1 COMPLETE)) + plan (10 additive) + transcripts (9 additive parseDuration + constants + slice 289 `list_query(ListQuery)` filters by platform/threadId/role — 3 upstream cases + slice 290 list-describe-block base cases (chronological-order-by-default / empty-result / newest-N-with-limit) — 3 upstream cases + slice 291 delete-describe-block cases (hides-tombstone / append-after-delete / double-delete / interleaved-append-delete-append) — 4 upstream cases + slice 292 maxPerUser-eviction-1-case + formatted-round-trip-1-case — 2 upstream cases + slice 293 append-assistant-with-explicit-user-key + append-passes-retention-duration-string-as-ttl-ms + count-returns-zero-for-unknown-user-key — 3 upstream cases + slice 294 rejects-malformed-duration-strings-at-parse-time — 1 upstream case ported at type-system layer (DurationString::parse rejects invalid input; `RetentionPolicy::Duration(DurationString)` makes the bad-string case compile-time impossible)) + postable_object (4 additive shape guard + trait) + reviver (6 additive _type dispatcher) + from_full_stream (17 cases: 16 upstream 1:1 + 1 additive StreamChunk pass-through) + streaming_markdown (43 tests = 33 upstream + 5 additive helpers + 5 internal-helper assertions + 1 additive monotonicity invariant), **Slice 314 ports `Chat::thread(thread_id)` single-arg factory** that infers the adapter from the `<prefix>:<rest>` shape (1:1 with upstream `chat.thread(threadId)`). Adds `ThreadLookupError { Invalid | AdapterNotFound(String) }` typed error + non-panicking `try_thread` helper. Panicking `thread()` matches upstream's `throw new Error("Invalid thread ID")` / `Adapter "X" not found` semantics. Ports all 4 portable upstream `chat.test.ts > describe("thread")` cases (valid thread handle / posting / invalid id panic / unknown prefix panic). **Slice 315 ports `Thread::start_typing(status)` + `Thread::mention_user(user_id)`** 1:1 with upstream `Thread.startTyping(status?)` (delegates to `adapter.start_typing(thread_id, status)`) and `Thread.mentionUser(userId) -> "<@userId>"` (Slack-style mention wrapper independent of platform). Extends the test mod's `RecordingAdapter` with a `start_typing: Mutex<Vec<(String, Option<String>)>>` recorder. Ports all 4 portable upstream `thread.test.ts > describe("startTyping")` + `describe("mentionUser")` cases (start typing with thread id only / with status / formatted mention / various user-id formats). **Slice 316 ports `Thread::subscribe` / `unsubscribe` / `is_subscribed` + `Thread::with_subscribed_context()` builder** 1:1 with upstream. Adds `StateAdapter::subscribe/unsubscribe/is_subscribed` trait methods (default impls via `set/delete/get` under `subscribed:<thread_id>`; state-memory overrides with its native `HashSet`). Adds optional `Adapter::on_thread_subscribe` trait hook (default no-op). Adds `is_subscribed_context: bool` field on `Thread` for the upstream short-circuit semantics. Ports all 8 portable upstream `thread.test.ts > describe("subscribe and unsubscribe")` (4 cases) + `describe("isSubscribed")` (4 cases) cases. **Slice 317 ports `Thread::recent_messages()` / `set_recent_messages()` / `with_initial_message()`** 1:1 with upstream's `recentMessages` getter/setter + `initialMessage` constructor option. The field is `Arc<Mutex<Vec<Message>>>` so the handle remains `Clone` while still allowing live mutation. Ports all 4 portable upstream `thread.test.ts > describe("recentMessages getter/setter")` cases. **Slice 319 adds `derive_channel_id(adapter, thread_id) -> String`** in `channel.rs` 1:1 with upstream `deriveChannelId`. Adds optional `Adapter::channel_id_from_thread_id(thread_id) -> Option<String>` trait method (default `None`) so adapters can opt in to channel/thread separation without breaking existing impls. Ports both portable upstream `channel.test.ts > describe("deriveChannelId")` cases + 1 additive fallback-to-thread-id case for the upstream `?? threadId` semantics. **Slice 320 adds `Channel::fetch_metadata` + `Channel::name` lazy cache** 1:1 with upstream. Adds optional `Adapter::fetch_channel_info(channel_id) -> Result<ChannelInfo>` trait method (default `Err(Unsupported)`) — `fetch_metadata` collapses Unsupported to a synthesized basic `ChannelInfo { id, is_dm, metadata: {} }` matching upstream's missing-method optional-chaining branch. Caches the fetched name on the handle (Arc<Mutex<Option<String>>>) so the `name()` accessor reflects it. Ports both portable upstream `channel.test.ts > describe("fetchMetadata")` cases. **Slice 321 wires `Channel::post` through new `Adapter::post_channel_message` trait method** 1:1 with upstream's `postChannelMessage` preferential routing. Default trait impl returns `Err(Unsupported("post_channel_message"))`, and `Channel::post` collapses Unsupported to a fallback `adapter.post_message(channel_id, text)` call (1:1 with upstream's `if (adapter.postChannelMessage) { ... } else { adapter.postMessage(...) }` branch). Ports 2 of 3 portable upstream `channel.test.ts > describe("post")` cases (preferred-when-available, fallback-when-not); the 3rd "streaming" case needs async-stream + StreamingMarkdownRenderer integration and is deferred. **Slice 322 adds `Channel::to_json` + `Channel::from_json`** 1:1 with upstream `Channel.toJSON()` / `Channel.fromJSON(json, adapter)` wire shape `{ _type: "chat:Channel", id, adapterName, channelVisibility: "unknown", isDM }`. The adapter is passed externally to `from_json` (1:1 with upstream's parameter — adapters aren't serialized in the JSON). Ports both portable upstream `channel.test.ts > describe("serialization")` cases. **Slice 323 adds `Thread::to_json` + `Thread::from_json` + `channel_id`/`current_message` fields + `with_channel_id`/`with_current_message` builders + accessors** 1:1 with upstream `Thread.toJSON()` / `Thread.fromJSON(json, adapter)` wire shape `{ _type: "chat:Thread", id, channelId, channelVisibility: "unknown", currentMessage?, isDM, adapterName }`. `currentMessage` serializes through the existing `Message::to_serialized` / `Message::from_serialized` round-trip. Ports all 4 portable upstream `thread.test.ts > describe("serialization")` cases. **Slice 324 adds `Message::workflow_serialize` + `Message::workflow_deserialize` static methods** 1:1 with upstream `Message[WORKFLOW_SERIALIZE]` / `Message[WORKFLOW_DESERIALIZE]` symbol-named static methods. Thin wrappers over the existing `to_serialized` / `from_serialized` helpers, exposed as associated functions to mirror upstream's static-method shape. Ports both portable upstream `message.test.ts > describe("WORKFLOW_SERIALIZE / WORKFLOW_DESERIALIZE")` cases. **Slice 325 adds `Chat::open_dm(user_id)` + `Chat::infer_adapter_for_user_id` + `Adapter::open_dm` trait method** 1:1 with upstream `chat.openDM(user)` / `inferAdapterFromUserId(userId)`. Routes Slack `U.../W...`, GChat `users/...`, Teams `29:...`, Linear UUID v4, and numeric IDs (Discord snowflake/Telegram/GitHub in priority order) to the matching registered adapter. Adds `OpenDmError { UnknownUserIdFormat | AdapterError }` typed errors with upstream-shaped `Cannot infer adapter from userId "X"` message. Ports 3 of 4 portable upstream `chat.test.ts > describe("openDM")` cases (infer-slack / unknown-format-throws / posting-to-dm-thread); the Author-object case is deferred until the Chat event loop ports add the `Into<UserId>` impl. **Slice 327 adds `Chat::get_user(user_id)` + `Adapter::get_user` trait method** mirroring slice 325's open_dm pattern. Reuses `infer_adapter_for_user_id` for adapter inference. Adds `GetUserError { UnknownUserIdFormat | AdapterError }` typed errors. Ports 4 of 10 portable upstream `chat.test.ts > describe("getUser")` cases (returns-user-info / throws-when-unsupported / returns-null-when-not-found / unknown-userid-format); 6 deferred cases need Author-object Into impl or multi-adapter-inference ambiguity testing (each as its own slice). **Slice 374 ports `revive_walk(Value) -> Value` recursive reviver + 3 of 5 portable `chat.reviver()` describe-block cases** 1:1 with upstream `serialization.test.ts > describe("chat.reviver()")`: should-revive-chat:Message-objects / should-leave-non-chat-objects-unchanged / should-work-with-nested-structures (the `chat:Message` envelope nested inside `{data:{messages:[...]}}` gets promoted just like a top-level one — matches upstream's `JSON.parse(text, reviver)` recursive visit semantics). The 2 deferred cases (revive chat:Thread / revive both Thread+Message) need the Thread reviver branch gated on cross-package adapter lookup. +3 lib.rs tests (13 total reviver tests). **Slice 373 ports 2 additional Message.fromJSON describe-block cases** 1:1 with upstream `serialization.test.ts > describe("Message.fromJSON()")`: should-restore-message-from-JSON (round-trips id / text / author) / should-preserve-ISO-strings-for-dates (upstream converts to `Date` objects; Rust keeps the ISO string verbatim — observable contract identical). +2 lib.rs tests in chat-sdk-chat. **Slice 372 ports 3 additional Message.toJSON describe-block cases** 1:1 with upstream `serialization.test.ts > describe("Message.toJSON()")`: should-handle-undefined-editedAt (wire-shape verifies the `editedAt` field is omitted when `None`) / should-serialize-author-correctly (author fields preserved) / should-produce-json-serializable-output (serde round-trip). +3 lib.rs tests in chat-sdk-chat. **Slice 371 ports 4 additional Thread serialization describe-block cases** 1:1 with upstream `serialization.test.ts > describe("ThreadImpl.toJSON()")` + `describe("ThreadImpl.fromJSON()")`: serialize-DM-thread-correctly / default-isDM-to-false-when-omitted (toJSON) / default-isDM-to-false-when-not-present-in-JSON (fromJSON) / set-isDM-to-true-when-present (fromJSON). Brings Thread serialization upstream parity from 4/6 to 8/6+ (+ 4 already counted earlier). +4 lib.rs tests (chat-sdk-chat 769→773 tests total). **Slice 359 ports `Channel::start_typing` + `Channel::mention_user` + the full `describe("ChannelImpl.startTyping")` + `describe("ChannelImpl.mentionUser")` describe blocks (4 of 4 portable cases)** 1:1 with upstream `channel.test.ts` — `start_typing` delegates to `Adapter::start_typing` with the bound channel id; `mention_user` returns the upstream Slack-style `<@user_id>` wrapper. Extends the test mod's `RecordingAdapter` with `start_typing_calls: Mutex<Vec<(String, Option<String>)>>` recorder. +4 lib.rs tests (chat-sdk-chat 769 tests total). **Slice 358 ports `Channel::post_ephemeral` + the full `describe("ChannelImpl.postEphemeral")` describe block (5 of 5 portable upstream cases)** 1:1 with upstream `channel.test.ts > describe("ChannelImpl.postEphemeral")` — should-use-adapter-postEphemeral-when-available / should-extract-userId-from-Author-object / should-return-null-when-no-postEphemeral-and-fallbackToDM-false / should-fallback-to-DM-when-no-postEphemeral-and-fallbackToDM-true / should-return-null-when-no-postEphemeral-no-openDM-and-fallbackToDM-true. Mirrors the Thread::post_ephemeral runtime dispatcher (slice 354): tries native ephemeral via Adapter::post_ephemeral; on Unsupported falls back to open_dm + post_message when fallback_to_dm=true; returns Ok(None) when fallback_to_dm=false or open_dm is also Unsupported. Adds Channel::post_ephemeral + Channel::post_ephemeral_for_author Author-overload sibling methods. Uses a dedicated EphemeralAdapter test mock with supports_ephemeral/supports_open_dm boolean flags reproducing upstream's mutation pattern. +5 lib.rs tests (chat-sdk-chat 765 tests total). **Slice 354 ports the full `describe("postEphemeral")` describe block (5 of 5 portable upstream cases)** 1:1 with upstream `thread.test.ts > describe("postEphemeral")` — should-use-adapter-postEphemeral-when-available / should-extract-userId-from-Author-object / should-fallback-to-DM-when-no-postEphemeral-and-fallbackToDM-true / should-return-null-when-no-postEphemeral-and-fallbackToDM-false / should-return-null-when-no-postEphemeral-or-openDM. Adds `EphemeralMessage { id, thread_id, used_fallback, raw }` struct to `types.rs` + optional `Adapter::post_ephemeral(thread_id, user_id, text) -> AdapterResult<EphemeralMessage>` trait method (default `Err(Unsupported("post_ephemeral"))`) + `Thread::post_ephemeral(user_id, text, options) -> AdapterResult<Option<EphemeralMessage>>` runtime dispatcher + `Thread::post_ephemeral_for_author(author, ...)` Author overload. The Rust dispatcher matches upstream's `if (adapter.postEphemeral)` branch via the Unsupported-error sentinel; on missing-native falls back to `adapter.open_dm + adapter.post_message` when `fallback_to_dm = true`, builds an `EphemeralMessage` with `used_fallback: true`, returns `Ok(None)` when `fallback_to_dm = false` or when `open_dm` is also Unsupported. Tests use a dedicated `EphemeralAdapter` mock with boolean flags (`supports_ephemeral`, `supports_open_dm`) reproducing upstream's `mockAdapter.postEphemeral = undefined` mutation pattern. +5 lib.rs tests (chat-sdk-chat 760 tests total). **Slice 353 ports the 4 SentMessage capability cases via the extended RecordingAdapter** 1:1 with upstream `describe("createSentMessageFromMessage")` — should-provide-edit-capability / should-provide-delete-capability / should-provide-addReaction-capability / should-provide-removeReaction-capability. Extends the test mod's `RecordingAdapter` with `edit_message`/`delete_message`/`add_reaction`/`remove_reaction` Mutex recorders so each capability test asserts the adapter was called with the right `(thread_id, message_id[, emoji|text])` tuple. **All 5 portable `describe("createSentMessageFromMessage")` cases now ported (5/5).** +4 lib.rs tests. **Slice 352 adds `Thread::create_sent_message_from_message` + new `SentMessage` struct** 1:1 with upstream `Thread.createSentMessageFromMessage(msg)` + `interface SentMessage<TRawMessage>`. `SentMessage` wraps a `Message` + an `Arc<dyn Adapter>` and exposes `id()/text()/thread_id()/author()/metadata()/attachments()` accessors plus `edit/delete/add_reaction/remove_reaction` methods delegating to the bound adapter. Ports the 1st of 4+ portable upstream `describe("createSentMessageFromMessage")` cases (field-preservation); the remaining capability cases (edit/delete/addReaction/removeReaction) need HTTP-mocking infrastructure deferred. +1 lib.rs test. **Slice 351 ports the final deferred `describe("getUser")` case** — "should not match GitHub-style logins as Slack ids (case sensitivity)". The existing `is_slack_user_id` impl (1:1 with upstream `^[UW][A-Z0-9]+$`) already enforces case sensitivity correctly (lowercase first char fails); the missing test validates the multi-adapter (slack + github) fallback yields `UnknownUserIdFormat` for `user123` rather than misrouting to Slack. **All 10 `describe("getUser")` upstream cases now ported (10/10).** +1 lib.rs test. **Slice 350 adds AMBIGUOUS_USER_ID detection to `Chat::get_user`** 1:1 with upstream's `throw new ChatError(..., "AMBIGUOUS_USER_ID")` branch when a numeric user id matches multiple registered adapters (discord/telegram/github). Adds `GetUserError::AmbiguousUserId { user_id, candidates }` typed variant carrying the conflicting adapter names. Refactors `get_user` to enumerate numeric-id candidates first and return AmbiguousUserId when `candidates.len() > 1`. Ports the 5th of 6 previously-deferred `describe("getUser")` cases. +1 lib.rs test. **Slice 349 ports 4 of 6 deferred `describe("getUser")` adapter-inference cases** 1:1 with upstream: infer-linear-from-UUID, infer-telegram-from-numeric-only-telegram, infer-github-from-numeric-only-github, infer-discord-snowflake-only-discord. Each exercises `infer_adapter_for_user_id` via a different adapter shape registered under the matching name. The 5th (AMBIGUOUS_USER_ID multi-adapter collision) and 6th (Slack case-sensitivity vs GitHub login) cases are deferred — they need the inference helper to detect numeric-ID collisions and return a typed error rather than picking the priority-order first match. +4 lib.rs tests. **Slice 348 adds `Chat::open_dm_for_author` + `Chat::get_user_for_author` Author-object overloads** 1:1 with upstream's runtime `typeof user === "string" ? user : user.userId` argument-shape branch. Sibling methods rather than a Union/Into trait — simpler API surface and ergonomic for callers. Ports the previously-deferred upstream `chat.test.ts > describe("openDM") > it("should accept Author object and extract userId")` + `describe("getUser") > it("should accept Author object")` cases. +2 lib.rs tests. **Slice 347 ports the early-exit subset of upstream `Chat.handleIncomingMessage(adapter, threadId, message)`** as `Chat::handle_incoming_message`. Lands the 2 upstream early-exit gates 1:1: (1) `author.is_me` → return without dispatching (`describe("Chat") > it("should skip messages from self")`); (2) `setIfNotExists(dedupe:<adapter>:<message.id>, true, DEDUPE_TTL_MS)` → second-occurrence early-exit (`describe("message deduplication") > it("should skip duplicate messages with the same id")`). Returns `Ok(true)` when a message would have been dispatched, `Ok(false)` when an early-exit applied. The full lock + concurrency + handler-dispatch path is deferred until the chat-sdk-chat event-handler trait surface (on_new_mention, on_subscribed_message, etc.) lands. Ports 2 upstream cases + 1 additive (happy-path returns true). +3 lib.rs tests. **Slice 328 adds `Adapter::is_dm(thread_id) -> Option<bool>` trait method + `Thread::is_dm`/`with_is_dm` accessors + wires `Chat::open_dm` to seed `is_dm` from `adapter.is_dm(thread_id) ?? false`** 1:1 with upstream's `createThread(adapter, threadId, ...)` helper. Updates `Thread::to_json` / `Thread::from_json` to round-trip the `isDM` field. Ports 1 of 3 portable upstream `chat.test.ts > describe("isDM")` cases (true-for-DM-threads via `chat.open_dm`); 2 deferred cases need `handleIncomingMessage` to deliver a thread handle into a registered handler. **739 Rust tests total** (audited at slice 328). Phase-1. |
| `packages/adapter-shared` (`@chat-sdk/adapter-shared`) | shared adapter utilities | verified | `crates/chat-sdk-adapter-shared` | `crates/chat-sdk-adapter-shared/src/{errors,card_utils,buffer_utils,adapter_utils,crypto}.rs::tests` (24 + 39 + 7 + 21 + 15 colocated tests, 117 total). errors is 1:1 (24/24). card_utils is 1:1 (39/39): PlatformName + BUTTON_STYLE_MAPPINGS + map_button_style + escape_table_cell + render_gfm_table + create_emoji_converter + card_to_fallback_text + FallbackTextOptions (BoldFormat/LineBreak/Platform). buffer_utils ports 7 of 16 portable cases (to_buffer + to_buffer_sync + buffer_to_data_uri); the 9 remaining cases assert JS-runtime-type plumbing for Buffer/ArrayBuffer/Blob discrimination that collapses to Vec<u8> in Rust, marked js-only-documented in the module header. adapter_utils ports 17 of 25 portable cases; the 8 remaining cases assert null/undefined/Blob/ArrayBuffer runtime-type plumbing the Rust type system rejects at compile time, marked js-only-documented in the module. crypto ports encrypt_token + decrypt_token + is_encrypted_token_data + decode_key + EncryptedTokenData (AES-256-GCM via aes-gcm); upstream has no crypto.test.ts so the 15 colocated tests are additive Rust-side roundtrip/decode/shape coverage. Every portable upstream case has a matching Rust test; non-portable cases are js-only-documented in-place. | 6 src files, 4 test files upstream. All 6 source files ported (index.ts is a barrel re-export covered by lib.rs `pub mod` declarations). All 4 upstream test files fully mapped at the portable case level. Phase-1 verified. Name uses upstream-mirroring "shared" exception (already in `scripts/check-naming-conventions.sh`). |
| `packages/tests` (`@chat-sdk/tests`) | test support library | js-only-documented | none | none | 6 src files, 2 test files. Vitest factories + expect-matcher extensions for testing adapters and bots: `vi.fn()`-backed `mockLogger` / `createMockLogger` / `createMockAdapter` / `createMockState` / `createMockChatInstance` / `createTestMessage`; `toHaveCalledHandler` / `toBeValidMessage` matchers in `@vitest/expect`. The entire surface is Vitest-framework glue with no Rust equivalent - Rust uses `#[cfg(test)] mod tests` with inline fixtures and direct `assert_eq!`/`assert!` macros. There is no `vi.fn()` analogue (the closest is `mockall`'s `#[automock]` but with a totally different API), no `expect.extend` extension model, and no shared adapter-mock infrastructure needed (each chat-sdk-* crate's tests build the small fixtures they need inline). Equivalent functionality across the Rust port lives in each crate's own `#[cfg(test)] mod tests` (see e.g. `chat_sdk_state_memory::tests::fresh` for the per-test factory pattern). Phase-1. js-only-documented. |
| `packages/state-memory` (`@chat-sdk/state-memory`) | state backend (in-memory) | verified | `crates/chat-sdk-state-memory` | `crates/chat-sdk-state-memory/src/lib.rs::tests` (33 colocated tests, 30/30 upstream cases mapped 1:1 + 3 additive). MemoryStateAdapter + create_memory_state + StateError + MemoryStateAdapterOptions. Sync `&self` methods backed by interior Mutex (in-memory backend has no real I/O; production state backends Redis/ioredis/Postgres will expose async via the chat::types::StateAdapter trait once extended). Subscriptions, locks (acquire/release/force-release/extend with token validation and TTL expiry), key/value cache (get/set/set_if_not_exists/delete with TTL), lists (append_to_list/get_list with max_length + TTL), per-thread queues (enqueue/dequeue/queue_depth with max_size newest-keep semantics). | 2 src files, 1 test file upstream. All 30 upstream test cases ported 1:1 (subscriptions 2, locking 9, setIfNotExists 4, appendToList/getList 7, enqueue/dequeue/queueDepth 8, connection 2). Phase-1 verified. |
| `packages/adapter-slack` (`@chat-sdk/adapter-slack`) | adapter package | in-progress | `crates/chat-sdk-adapter-slack` | `crates/chat-sdk-adapter-slack/src/lib.rs::tests` (29 = 5 upstream channelId+isDM + 24 additive) + `crates/chat-sdk-adapter-slack/src/crypto.rs::tests` (14) + `crates/chat-sdk-adapter-slack/src/webhook.rs::tests` (17 = 11 prior + 6 get_header/get_retry additive) + `crates/chat-sdk-adapter-slack/src/api.rs::tests` (6) + `crates/chat-sdk-adapter-slack/src/format.rs::tests` (21 = 16 upstream + 5 additive) + `crates/chat-sdk-adapter-slack/src/markdown.rs::tests` (34 = 26 upstream + 8 additive) + `crates/chat-sdk-adapter-slack/src/cards.rs::tests` (37 = 36 upstream + 1 additive) = 193 colocated tests. **Slice 355 ports `SlackAdapter::post_ephemeral` via `chat.postEphemeral` Web API** 1:1 with upstream's text-path `adapter.postEphemeral` (the card/Block-Kit branch is deferred — needs `cardToBlockKit` infra). Decodes the slack: thread id, builds the payload via the new pure helper `slack_post_ephemeral_payload(channel, thread_ts, user_id, text)` (normalizes empty `thread_ts` to absent — upstream's `threadTs || undefined` semantics), POSTs to `chat.postEphemeral`, parses via the new pure helper `parse_slack_post_ephemeral_response(json, thread_id)` (preserves upstream's `result.message_ts || ""` empty-id fallback). Ports all 3 portable upstream `index.test.ts > describe("postEphemeral")` cases (posts ephemeral message / normalizes empty threadTs to undefined / handles empty message_ts in response) + 1 additive Rust-only non-slack-thread-id rejection + 1 method_url smoke test. Also adds `chat.postEphemeral` to the parametric `method_url` coverage (now 10 endpoints). +5 lib.rs tests (Slack 203 tests total). **Slice 343 ports Slack Web API method URL-shape coverage** parametrically across 9 endpoints (chat.postMessage / chat.update / chat.delete / conversations.info / reactions.add / reactions.remove / assistant.threads.setStatus / views.open / views.update) — mirrors the upstream per-method `it("calls slack <method>")` URL-shape assertions bundled into one Rust test since they all flow through the same `method_url` helper. +1 lib.rs test. **Slice 331 ports `SlackAdapter::remove_reaction`** 1:1 with upstream `adapter.removeReaction` — POSTs `{channel, timestamp: message_id, name: emoji}` to `reactions.remove` with bearer auth. Swallows `no_reaction` as the idempotent counterpart of `add_reaction`'s `already_reacted` swallow. Ports 1 portable upstream `index.test.ts > describe("removeReaction")` case (URL helper produces `reactions.remove`; thread-id-decode rejects non-slack ids) + 1 additive negative case. +2 lib.rs tests. **Slice 298 extends `SlackAdapterOptions`** with `user_name` + `bot_user_id` fields + `DEFAULT_USER_NAME = "bot"` const + `effective_user_name()` / `user_name()` / `bot_user_id()` accessors. Ports the 4 upstream `index.test.ts > describe("createSlackAdapter")` cases. **cards.test.ts now fully ported (36/36).** Slice 184 completed the regex-heavy format ports. **All 16 upstream `format/index.test.ts` cases now ported 1:1** (count corrected from 14: the two markdown-bold + bare-mention-tokens cases were already covered by tests but uncounted in the prior tally). Slice 192 added webhook HMAC-SHA256 verification (`hmac`+`sha2`+`subtle` deps). Slice 193 added `modals.rs` (`ModalMetadata` + `encode_modal_metadata` + `decode_modal_metadata`) - 12 ported upstream `modals.test.ts` cases (4 encode + 7 decode + 1 roundtrip) of 33 total. Slice 201 added `SlackFormatConverter` (to_ast/from_ast/to_markdown/extract_plain_text/render_postable_string,raw + finalize_plain/finalize_markdown helpers) with 7 toMarkdown cases ported. **Slice 202 ports the upstream `toPlainText` describe block (5 cases). Slice 203 adds `SlackTextPayload` enum (`Text { text }` / `MarkdownText { markdown_text }`), `to_slack_payload(AdapterPostableMessage)` routing (string/raw -> Text; markdown/ast -> MarkdownText with stringify_markdown; card/cardelement -> Text("")), corrected `finalize_plain` to apply both the upstream `BARE_MENTION_REGEX = /(?<![<\w])@(\w+)/g` (via new `rewrite_bare_mentions` helper - more permissive than `linkBareSlackMentions` which only matched Slack ID shapes) and `convert_emoji_placeholders(_, Slack, _)`. **All 7 upstream `mentions` describe-block cases ported** (no-double-wrap-plain, no-double-wrap-markdown, rewrite-bare-plain, rewrite-bare-markdown, preserve-email, preserve-mailto, adjacent-punctuation). Slice 204 ports the 5 `toSlackPayload` routing cases (plain-string-to-text-preserves-literal, raw-to-text, markdown-to-markdown_text, ast-via-stringifyMarkdown, ast-table-preserved-as-pipe-markdown). Also re-exported `AlignKind` from `chat_sdk_chat::markdown` to let downstream adapters build table AST without depending on the `markdown` crate directly. **Slice 205 ports the full upstream `nodeToMrkdwn` walker + `astToMrkdwn` + `to_response_url_text`** routing: paragraph/text/strong (`*x*`)/emphasis (`_x_`)/delete (`~x~`)/inline-code/code (with lang)/link (`<url|text>`)/blockquote/list (via `render_list` with `"•"` bullet)/break (`"\n"`)/thematicBreak (`"---"`)/table (`tableToAscii` in `\`\`\``). Includes the 2 upstream `toResponseUrlText` cases (markdown -> Slack mrkdwn text with bold+link, table -> ASCII code block). **All 26 of 26 upstream `markdown.test.ts` cases now ported 1:1** (7 toMarkdown + 5 toSlackPayload + 2 toResponseUrlText + 7 mentions + 5 toPlainText). **Slice 212 adds `src/cards.rs`** with `card_to_fallback_text_slack(card)` - thin wrapper over `chat_sdk_adapter_shared::card_utils::card_to_fallback_text` with Slack-shape options (`BoldFormat::Single`, `LineBreak::Single`, `PlatformName::Slack`). Ports the 2 upstream `cardToFallbackText` describe-block cases (fallback-with-fields-actions-excluded, only-title); 34 remaining cards.test.ts cases are `cardToBlockKit` which needs the Slack Block Kit JSON renderer. Foundation for full `webhook/verify.ts` + `modals.ts modalToSlackView` 1:1 ports. Slice 174 ported `webhook/utils.ts` pure helpers; slice 181 ported the pure-helper subset of `api/index.ts`: `SlackApiResponse`, `SlackApiError`, `EncodedSlackApiBody`, `SlackApiBodyEncoding`, `encode_slack_api_body` + `assert_slack_ok` + form URL-encoder matching `URLSearchParams.toString()` byte shape (alphanumerics + `-._~*` unreserved; `+` for space; `%HH` else). 1 of 13 upstream `api/index.test.ts` cases ported (`form-encodes Slack API bodies with JSON object values`). The HTTP-call wrappers (callSlackApi/postSlackMessage/...) are deferred — the underlying HTTP methods already live on `SlackAdapter`. | | 24 src files, 11 test files. Slack adapter. Phase-2. Slice 139 scaffold + slice 152 `post_message` + slice 158 `fetch_subject` (additive) + slice 160 edit/delete/react/typing + slice 169 `post_object` + slice 170 crypto module (`src/crypto.rs` re-exports `encrypt_token`/`decrypt_token`/`decode_key`/`is_encrypted_token_data`/`EncryptedTokenData` from `chat_sdk_adapter_shared::crypto` 1:1 with upstream `crypto.ts` re-export of `@chat-adapter/shared`; ports all 14 upstream `crypto.test.ts` cases: round-trip, random-IV uniqueness, wrong-key/tampered-ciphertext rejection, base64+hex+whitespace key decode, length validation, `is_encrypted_token_data` shape guard for null/object-missing-fields/non-string-fields). Remaining: Socket Mode + signature verification + Block Kit + plan/card edit branches + cards/markdown/modals test ports (33+31+36 cases) + remaining ~20 Web API methods. **Slice 230 ports `SlackAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's helpers (channelId = `slack:<channel>`, isDM = channel.startsWith('D')); +5 lib.rs tests. **Slice 234 ports `get_header(headers, name)` + `get_retry(headers)` + `SlackRetry` struct** from upstream `webhook/utils.ts`. Rust signature takes `&[(String, String)]` (the shape exposed by HTTP client header iterators); case-insensitive lookup; retry-num parses as `i64` or returns `None`. +6 additive Rust webhook.rs tests (upstream exercises these indirectly through `parse.test.ts` — no standalone upstream cases). Also fixed a stray `#[test]` attribute on the slice 230 channelId+isDM block in `lib.rs` that was causing a `duplicate_macro_attributes` warning. |
| `packages/adapter-teams` (`@chat-sdk/adapter-teams`) | adapter package | in-progress | `crates/chat-sdk-adapter-teams` | `crates/chat-sdk-adapter-teams/src/lib.rs::tests` (20) + `crates/chat-sdk-adapter-teams/src/errors.rs::tests` (12) + `crates/chat-sdk-adapter-teams/src/markdown.rs::tests` (39 = 39 upstream) + `crates/chat-sdk-adapter-teams/src/cards.rs::tests` (22 = 19 upstream + 3 additive: 2 const-shape + 1 Table renderer) + `crates/chat-sdk-adapter-teams/src/thread_id.rs::tests` (7 = 7 upstream) = 101 colocated tests. **cards.test.ts now fully ported (19/19).** **Slice 217 ports `packages/adapter-teams/src/markdown.ts` 1:1** to a new `src/markdown.rs` module: `TeamsFormatConverter` struct + Teams HTML decoder (`<at>` / `<b>` / `<strong>` / `<i>` / `<em>` / `<s>` / `<strike>` / `<a href="...">` / `<code>` / `<pre>` -> standard markdown via case-insensitive byte scanners + HTML entity decoder for `&lt;` / `&gt;` / `&amp;` / `&quot;` / `&#39;`) + `node_to_teams` walker (paragraph/text-with-mention-rewrite/strong/emphasis/delete/inline-code/code-with-lang/link/blockquote/list/break/thematicBreak/table-as-GFM-pipe-syntax) + `table_to_gfm` helper using `chat_sdk_adapter_shared::card_utils::escape_table_cell`. `render_postable` routes string/raw through `convert_mentions_to_teams` (`/@(\w+)/g -> <at>$1</at>`), markdown/ast through `from_ast`. **All 39 upstream `markdown.test.ts` cases ported 1:1** (16 fromAst + 11 toAst + 5 renderPostable + 5 extractPlainText + 2 table). **Slice 218 adds `src/cards.rs`** with `card_to_fallback_text_teams(card)` — thin wrapper over `chat_sdk_adapter_shared::card_utils::card_to_fallback_text` with Teams-shape options (`BoldFormat::Double`, `LineBreak::Double`, `PlatformName::Teams`). Ports the 2 upstream `cardToFallbackText` describe-block cases. Slice 172 ported `handleTeamsError(error, operation)`: dispatches on `innerHttpError.statusCode` / `statusCode` / `status` / `code` (401 -> Authentication, 403 -> Permission, 404 -> Network, 429 -> RateLimit w/ optional retry_after) + permission-keyword detection + message/string/null fallbacks all to NetworkError. | 16 src files, 6 test files. Microsoft Teams adapter. Phase-2. Slice 138 scaffold + slice 153 `post_message` (pre-minted bearer) + slice 167 the 4 universal trait methods on the Bot Framework: `edit_message` (PUT `/v3/conversations/<id>/activities/<msg>` with `{type: message, text}`), `delete_message` (DELETE same), `add_reaction` returns "addReaction is not yet supported by the Teams SDK" (1:1 with upstream's `NotImplementedError`), `start_typing` POSTs `{type: typing}` to the conversation. Adds `activity_url(conversation_id, activity_id)` helper. Remaining: adaptive-cards rendering + serviceUrl-from-activity bookkeeping + token-cache helper. **Slice 237 adds `src/thread_id.rs`** porting upstream `thread-id.ts` 1:1: `TeamsThreadId { conversation_id, service_url }` struct + `encode_thread_id` (`teams:<base64url(conv)>:<base64url(svc)>`) + `decode_thread_id` returning `Result<_, AdapterError::Validation>` matching upstream's `ValidationError("teams", ...)` shape + `is_dm_thread(&str)` predicate (true iff `conversation_id` doesn't start with `19:`, the Teams group/channel prefix). Adds `base64 = "0.22"` dep. Ports all 7 portable upstream tests from `index.test.ts` (Thread ID Encoding 4 + isDM 3). The new struct-based API coexists with the legacy `encode_thread_id(conversation_id, message_id)` form in `lib.rs` used by the adapter's HTTP code; migration deferred. **Slice 370 sweep — adds `Adapter::open_dm` trait impls across 3 adapters with inherent open_dm helpers** (WhatsApp, Messenger, Telegram). Same trait-impl sweep pattern as slice 366. Each delegates to the existing inherent method; WhatsApp/Messenger wrap String in `Ok(_)`, Telegram converts the `Option<String>` to `Err(InvalidPayload)` when the user id isn't numeric (matches upstream's `parseInt(userId)` failure surface). Slack/Discord/GChat/Linear/GitHub/Teams have no inherent open_dm helper (upstream uses HTTP calls); their Adapter::open_dm continues to default to `Err(Unsupported("open_dm"))`. 3 new trait method bindings (1 per adapter × 3 adapters). All workspace tests pass. **Slice 366 sweep — adds `Adapter::channel_id_from_thread_id` + `Adapter::is_dm` trait impls across all 8 remaining adapters** (Slack, Discord, GChat, WhatsApp, Messenger, Telegram, Linear, GitHub). Each trait impl delegates to the existing inherent method on the adapter struct (some returning `String`/`bool` are wrapped in `Some(_)` to match the `Option<_>` trait shape). This wires the per-adapter helpers into the cross-cutting `Channel::derive_channel_id` + Chat dispatcher paths that consume `Adapter::channel_id_from_thread_id`. Rust's inherent-method-takes-precedence resolution means the trait impl body `self.channel_id_from_thread_id(thread_id)` calls the inherent method (not the trait method itself), so no recursion. No new tests needed since both methods were already tested via inherent calls in each adapter's existing test suite; the change exposes the same helpers through the trait surface. 16 new trait methods (2 per adapter × 8 adapters), 0 new tests. **Slice 365 ports `channel_id_from_thread_id` for Teams** 1:1 with upstream `adapter.channelIdFromThreadId(threadId)`. Adds a new `pub fn channel_id_from_thread_id(thread_id) -> String` in `thread_id.rs` that strips any `;messageid=…` suffix from the decoded `conversation_id` (Teams channel threads encode the parent message id as a query-string-style suffix), then re-encodes via `encode_thread_id`. Returns the input verbatim when decoding fails (matches upstream's pass-through). Adds `Adapter::channel_id_from_thread_id` trait impl on `TeamsAdapter` delegating to the new helper. Ports both portable upstream `index.test.ts > describe("channelIdFromThreadId")` cases (should-strip-messageid-from-thread-id / should-return-same-id-when-no-messageid-present). +2 lib.rs tests (chat-sdk-adapter-teams 107 tests total). **Slice 335 ports `TeamsAdapter::remove_reaction`** 1:1 with upstream's `throw NotImplementedError("removeReaction is not yet supported by the Teams SDK", "removeReaction")`. Symmetric with the unsupported `add_reaction`. Surfaced as `AdapterError::InvalidPayload` with the same message body. Upstream has no `removeReaction` describe block — this is the symmetric not-implemented surface. +1 additive Rust test. **Slice 299 ports 1 upstream `index.test.ts > describe("TeamsAdapter") > it("should create an adapter instance")` case** asserting `adapter.name == "teams"` via the existing `TeamsAdapterOptions::new` constructor. Upstream's `it("should export createTeamsAdapter function")` is JS-only (Rust's module system makes the constructor visible at compile time). |
| `packages/adapter-gchat` (`@chat-sdk/adapter-gchat`) | adapter package | in-progress | `crates/chat-sdk-adapter-gchat` | `crates/chat-sdk-adapter-gchat/src/lib.rs::tests` (24 = 3 upstream channelId+isDM + 21 additive) + `crates/chat-sdk-adapter-gchat/src/markdown.rs::tests` (29 = 29 upstream) + `crates/chat-sdk-adapter-gchat/src/thread_utils.rs::tests` (14 = 14 upstream) + `crates/chat-sdk-adapter-gchat/src/user_info.rs::tests` (14 = 14 upstream) + `crates/chat-sdk-adapter-gchat/src/workspace_events.rs::tests` (4 = 4 upstream) + `crates/chat-sdk-adapter-gchat/src/cards.rs::tests` (28 = 27 upstream + 1 additive empty-card placeholder) = 114 colocated tests. **cards.test.ts now fully ported (27/27).** Phase-2. Slice 137 scaffold + slice 154 post_message + slice 168 edit/delete/react/typing + slice 190 markdown converter + slice 199 nested-list rendering. **Slice 213 adds `src/thread_utils.rs`** porting upstream `thread-utils.ts` 1:1: `GoogleChatThreadId { space_name, thread_name?, is_dm }` struct + `encode_thread_id(GoogleChatThreadId) -> gchat:<spaceName>[:<base64url(threadName)>][:dm]` + `decode_thread_id(&str)` returning `Result<_, InvalidGoogleChatThreadId>` + `is_dm_thread(&str)` predicate. Adds `base64 = "0.22"` dep. Ports all 14 upstream `thread-utils.test.ts` cases (4 encode + 4 decode + 3 round-trip + 3 isDMThread). Note: this new API coexists with the simpler `encode_thread_id(space, thread)`/`decode_thread_id` form in `lib.rs` which is used internally by the adapter's HTTP code; migration to the upstream-shaped struct API is deferred. **All 29 of 29 upstream `markdown.test.ts` cases + all 14 of 14 `thread-utils.test.ts` cases now ported.** **Slice 214 adds `src/user_info.rs`** porting upstream `user-info.ts` 1:1: `CachedUserInfo` struct (with `displayName`/`email`/`isBot`/`avatarUrl` matching upstream serde shape) + `UserInfoCache` struct holding `Mutex<HashMap>` for in-memory + `Option<Arc<dyn StateAdapter>>` for persistent backing; `async set` / `async get` / `async resolve_display_name` methods. Uses `chat_sdk_state_memory::MemoryStateAdapter` as the real-state test backend (instead of a JS-style vi.fn() mock). Ports all 14 upstream `user-info.test.ts` cases (4 set + 5 get + 5 resolveDisplayName). Adds `chat-sdk-state-memory` dev-dep. **Slice 215 adds `src/workspace_events.rs`** with `decode_pub_sub_message(&PubSubPushMessage) -> WorkspaceEventNotification` (base64-decodes Pub/Sub data, parses JSON payload, maps CloudEvents `ce-subject`/`ce-type`/`ce-time` attributes to top-level fields, falls back to `publishTime`). Ports all 4 upstream `decodePubSubMessage` cases. The HTTP-and-googleapis-SDK-heavy `createSpaceSubscription` / `listSpaceSubscriptions` / `deleteSpaceSubscription` paths are deferred to a follow-up slice. **Slice 220 adds `src/cards.rs`** with `card_to_fallback_text_gchat(card)` — thin wrapper over `chat_sdk_adapter_shared::card_utils::card_to_fallback_text` with GChat-shape options (`BoldFormat::Single`, `LineBreak::Single`, `PlatformName::Gchat`). Ports the 2 upstream `cardToFallbackText` describe-block cases. Remaining: card rendering + slash-command handling + service-account JWT mint + `user-info.test.ts` (14 cases) + `workspace-events.test.ts` + `index.test.ts`. **Slice 230 ports `GchatAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's helpers (channelId = `gchat:<spaceName>`, isDM delegates to `isDMThread`); +3 lib.rs tests. **Slice 300 ports 1 portable upstream `index.test.ts > describe("GoogleChatAdapter") > it("should create an adapter instance")` case** asserting `adapter.name == "gchat"`. Upstream's `it("should export createGoogleChatAdapter function")` is JS-only. **Slice 357 ports `GchatAdapter::post_ephemeral` via `spaces.messages.create` with `privateMessageViewer`** 1:1 with upstream's text-path `adapter.postEphemeral` (the cardsV2 ephemeral branch is deferred — needs `cardToGoogleCard` infra). Adds 2 new pure helpers: `gchat_post_ephemeral_payload(space_id, thread_id, user_id, text)` (sets `privateMessageViewer.name = user_id`, omits `thread.name` entirely when `thread_id` is empty — matches upstream's `threadName ? { name: threadName } : undefined`) + `parse_gchat_post_ephemeral_response(json, thread_id)` (preserves upstream's `response.data.name || ""` empty-id fallback). The dispatcher decodes the gchat: thread id, POSTs via `messages_create_url`. Ports both portable upstream `index.test.ts > describe("postEphemeral")` cases (ephemeral text with privateMessageViewer / throw-on-API-error proxied as non-gchat-id rejection) + 2 additive Rust-only cases (omit-thread-for-top-level / missing-name response fallback). +4 lib.rs tests (chat-sdk-adapter-gchat 134 tests total). **Slice 312 ports the `constructor / initialization` + `constructor env var resolution` describe blocks (12 of 14 cases)** via a new `try_create_gchat_adapter(opts, env)` factory + `GchatCreateOptions` + `GchatCreateError` + `DEFAULT_USER_NAME = "bot"` const. Extends `GchatAdapterOptions` with `user_name`, `pubsub_topic`, `impersonate_user`, `use_application_default_credentials` fields + accessors on `GchatAdapter`. Auth resolution: config.credentials > config.use_application_default_credentials > env(GOOGLE_CHAT_CREDENTIALS) > env(GOOGLE_CHAT_USE_ADC=="true") > AuthenticationRequired (config short-circuits env per "don't mix modes"). Resolution: subject_email ?? env(GOOGLE_CHAT_IMPERSONATE_USER); pubsub_topic ?? env(GOOGLE_CHAT_PUBSUB_TOPIC); api_url ?? env(GOOGLE_CHAT_API_URL). 2 deferred upstream cases need `GchatAdapter::initialize` + StateAdapter wiring (botUserId restore-on-initialize semantics). The "should default logger" case is js-only here. Env reader is `Fn(&str) -> Option<String>` closure (slice-305 pattern). All 12 portable cases pass (130 total GChat tests, +12). This **completes the env-var-resolution sweep across all 7 applicable adapters** (Discord, Linear, GitHub, Telegram, WhatsApp, Messenger, GChat — 73 upstream cases total). |
| `packages/adapter-discord` (`@chat-sdk/adapter-discord`) | adapter package | in-progress | `crates/chat-sdk-adapter-discord` | `crates/chat-sdk-adapter-discord/src/lib.rs::tests` (19) + `crates/chat-sdk-adapter-discord/src/webhook.rs::tests` (11 additive) + `crates/chat-sdk-adapter-discord/src/markdown.rs::tests` (41 = 41 upstream + 0 additive) + `crates/chat-sdk-adapter-discord/src/cards.rs::tests` (39 = 36 upstream + 3 additive) + 3 lib.rs channelId+isDM + 2 lib.rs optional-thread_id = 126 colocated tests. **cards.test.ts is now fully ported (38/38)** modulo upstream's `convertEmoji` (covered indirectly via the markdown.test.ts emoji cases). 8 src files, 4 test files. Discord adapter. Phase-2. Slice 134 scaffold + slice 149 post_message + slice 165 edit/delete/react/typing + slice 197 webhook Ed25519 signature verification + **slice 206 ports `DiscordFormatConverter` 1:1 with upstream `markdown.ts`**: rewrites Discord-specific syntax (user `<@id>` / nickname `<@!id>`, channel `<#id>`, role `<@&id>`, custom emoji `<:name:id>` / animated `<a:name:id>`, spoiler `||text||` -> `[spoiler: text]`) via lookbehind-aware byte scanners, then parses via standard markdown. `from_ast` walks the mdast via `node_to_discord_markdown` (paragraph/text-with-mention-rewrite/strong `**x**` / emphasis `*x*` / delete `~~x~~` / inline-code / code-with-lang / link `[text](url)` / blockquote `> ` / list via `render_list(_, _, _, "-")` / break / thematic-break / table-as-ascii-code-block / fallthrough). `render_postable` routes string-or-raw through `convert_mentions_to_discord(@word -> <@word>)`, markdown/ast through `from_ast`. **All 41 upstream `markdown.test.ts` cases ported 1:1** (8 fromAst + 8 toAst+extractPlainText + 10 extractPlainText + 5 renderPostable + 1 blockquote + 2 lists + 5 nested-lists + 1 thematic-break + 1 table). 1 of 4 upstream test files fully ported (markdown). **Slice 219 adds `src/cards.rs`** with `card_to_fallback_text_discord(card)` 1:1 with upstream's inline `cardToFallbackText` (not the shared helper because Discord uses different field rendering: `**label**: value` vs the shared `label: value`). Ports all 7 upstream `cardToFallbackText` cases (full card with title+fields+actions-excluded, only-title, only-subtitle, divider, section, empty card, multiple fields). cards.test.ts 7/38 ported; remaining 31 are the `cardToDiscordPayload` Embed/Action-Row renderer + CardLink branch deferred to a follow-up slice. **Slice 227 adds `encode_discord_custom_id` / `decode_discord_custom_id`** 1:1 with upstream's `\n`-delimited codec + 100-char validation via `ValidationError`. Ports 11/13 portable upstream `encodeDiscordCustomId / decodeDiscordCustomId` cases (encode-only, encode-with-value, empty-value, empty-action-id-throws, 101-char-throws, encoded-100+ throws, decode-only, decode-with-value, round-trip, embedded-delimiter, undefined-value, 100-char boundary encode, 101-char boundary throws). 2 deferred cases require the `cardToDiscordPayload` renderer. cards.test.ts now 18/38 ported (7 fallback + 11 customId). **Slice 364 ports the full Discord `describe("normalizeDiscordEmoji")` + `describe("encodeEmoji")` describe blocks (9 of 9 portable cases)** 1:1 with upstream `index.test.ts`. Adds new `pub fn normalize_discord_emoji(emoji_name) -> Arc<EmojiValue>` 1:1 with upstream's `normalizeDiscordEmoji(emojiName)` instance method — maps recognized unicode glyphs (👍→thumbs_up, 🔥→fire, etc., 18 entries total) to their standard short names then delegates to `chat_sdk_chat::emoji::get_emoji`, falls through to the input verbatim for unknown emoji names. Promotes the existing `url_encode_emoji` to `pub` so the upstream `encodeEmoji` cases can exercise it directly without HTTP-call dispatch. Ports all 7 `normalizeDiscordEmoji` cases (thumbs_up / heart-with-vs / heart-no-vs / fire / unknown-pass-through / rocket / eyes) + both `encodeEmoji` cases (URL-encodes-emoji / handles-string-emoji). +9 lib.rs tests (chat-sdk-adapter-discord 162 tests total). **Slice 363 ports the 3 missing Discord `describe("truncateContent")` cases** 1:1 with upstream `index.test.ts > describe("truncateContent")` — returns-content-unchanged-at-exactly-2000-chars / truncates-at-exactly-2001-chars / handles-empty-string. The other 2 upstream cases (under-limit / exceeding-2000-with-ellipsis) were already mapped to existing tests. Also adds a 4th additive Rust-only `first-1997-chars-preserve-input-when-exceeding-2000` test mirroring upstream's `result.slice(0, 1997)` assertion within case 3. +4 lib.rs tests (chat-sdk-adapter-discord 153 tests total). **Slice 360 splits the bundled `channelIdFromThreadId` test into 3 explicit 1:1 cases** 1:1 with upstream `index.test.ts > describe("channelIdFromThreadId")` (returns-channel-level-id-from-thread-id / returns-as-is-when-already-a-channel-id-3-parts / handles-DM-channel-ids). Previously these 3 upstream cases were bundled into a single Rust test (`channel_id_from_thread_id_takes_first_three_colon_segments`); per the brief's "every portable upstream case has a matching Rust test" rule, they're now split into one test per upstream case. Also cleans up 4 duplicated `#[test]` attribute warnings (cards.rs:493 + lib.rs:825/831/844) carried over from prior slices. +3 lib.rs tests minus 1 bundled = +2 net (chat-sdk-adapter-discord 149 tests; warnings cleared). **Slice 344 ports the missing isDM-for-sub-thread case** 1:1 with upstream `describe("isDM") > it("returns false for threads in guilds")` — sub-thread `discord:guild123:channel456:thread789` returns `Some(false)` since only `@me` is a DM (the existing `is_dm_true_for_at_me_guild_only` test covered @me=true and G1=false but missed the 3rd sub-thread case). +1 lib.rs test. **Slice 230 ports `DiscordAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's helpers (channelId = first 3 colon-segments, isDM = guild == "@me"); +3 lib.rs tests. **Slice 238 extends `DecodedDiscordThreadId` with `thread_id: Option<String>`** + fixes `decode_thread_id` to use `split(':')` (was `splitn(2, ':')` which silently glued the channel + sub-thread parts). Now captures upstream's optional 4th colon-segment matching `interface DiscordThreadId { guildId; channelId; threadId? }`; +2 lib.rs tests (captures optional sub-thread, drops trailing empty sub-thread). **Slice 297 extends `DiscordAdapterOptions`** with `public_key` (ed25519 verifying-key hex) + `user_name` fields + `DEFAULT_USER_NAME = "bot"` const + `effective_user_name()` / `user_name()` / `public_key()` accessors. Ports the 3 upstream `index.test.ts > describe("createDiscordAdapter")` cases (creates-instance / default-user-name-bot / uses-provided-user-name). **Slice 341 rewires `DiscordAdapter::post_message` through new `post_message_url(thread_id)` helper** (was hardcoded to `decoded.channel_id` via the legacy `channel_messages_url` path; now uses `decoded.thread_id ?? channel_id` routing matching upstream's `targetChannelId = discordThreadId || channelId`). Removes the now-unused `channel_messages_url` helper + its test (superseded by `post_message_url`). Ports 2 of 3 portable upstream `index.test.ts > describe("postMessage")` cases (plain-text-routing + sub-thread-routing) + 1 additive negative case; the 3rd case (cards/JSX payload) is deferred until cards renderer is wired into post_message. +3 lib.rs tests minus 1 removed = +2 net. **Slice 340 adds `DiscordAdapter` `truncate_content(content) -> String`** 1:1 with upstream's private `truncateContent` helper — slices to `DISCORD_MAX_CONTENT_LENGTH - 3` and appends `"..."` so the final length is exactly the limit when input exceeds. Char-aware (not byte-aware) so multibyte glyphs split safely. Wires `post_message` + `edit_message` to truncate `content` before sending. Ports 1 portable upstream `index.test.ts > describe("editMessage") > it("truncates content exceeding 2000 characters on edit")` case + 2 additive (under-limit pass-through, multibyte char safety). The 2 routing cases of editMessage describe block are already covered by slice 339's `message_url` tests. +3 lib.rs tests. **Slice 339 fixes `DiscordAdapter::edit_message` + `delete_message` to route through the sub-thread id when encoded** (both were hardcoded to `decoded.channel_id` via the old `channel_messages_url` path; now use a new `message_url(thread_id, message_id)` helper that does `decoded.thread_id ?? channel_id` routing matching upstream's `targetChannelId = discordThreadId || channelId`). Ports both portable upstream `index.test.ts > describe("deleteMessage")` cases (channel-id-routing + sub-thread-routing) via the shared message-url helper that also powers edit_message + post_message. +3 lib.rs tests (2 upstream + 1 additive negative). **Slice 338 fixes `DiscordAdapter::add_reaction` to route through the sub-thread id when encoded** (was hardcoded to `decoded.channel_id` via the old `channel_messages_url` path; now reuses the slice-330 `reaction_url(thread_id, message_id, emoji)` helper that does `decoded.thread_id ?? channel_id` routing). Ports both portable upstream `index.test.ts > describe("addReaction")` cases (channel-id-routing + sub-thread-routing) via the shared reaction-url helper that already powers remove_reaction. +2 lib.rs tests. **Slice 337 fixes `DiscordAdapter::start_typing` to route through the sub-thread id when encoded** (was hardcoded to `decoded.channel_id`; now uses `decoded.thread_id ?? channel_id` matching upstream `targetChannelId = discordThreadId || channelId`). Adds pure `typing_url(thread_id)` helper for unit-testable URL construction. Ports both portable upstream `index.test.ts > describe("startTyping")` cases (channel-id-routing + sub-thread-routing) + 1 additive negative case (non-Discord thread id returns None). +3 lib.rs tests. **Slice 330 adds `DiscordAdapter::remove_reaction` + `DiscordAdapter::reaction_url` pure helper** 1:1 with upstream `adapter.removeReaction`. DELETE `/channels/<target>/messages/<msg>/reactions/<emoji>/@me` where `target` is the sub-thread id when the thread id encodes one (`discord:<guild>:<channel>:<sub-thread>`), otherwise the channel id. Ports both portable upstream `index.test.ts > describe("removeReaction")` cases (channel-routing + sub-thread-routing) + 1 additive negative case (non-Discord thread id returns None from the URL helper). **Slice 304 adds `DiscordCreateOptions` + `DiscordCreateError` + `try_create_discord_adapter(opts, env)`** factory that mirrors upstream's `new DiscordAdapter(config)` env-var-resolution path. Adds `mention_role_ids: Vec<String>` field on `DiscordAdapterOptions` + `mention_role_ids()` accessor. Resolution order matches upstream: `config` ?? `env(DISCORD_*)` ?? defaults; missing required fields yield typed errors with upstream-compatible messages. The env reader is a `Fn(&str) -> Option<String>` closure instead of touching `std::env` (Rust 2024 makes `set_var` `unsafe` and parallel tests would race). Ports all 9 upstream `describe("constructor env var resolution")` cases (missing-bot-token / missing-public-key / missing-application-id / resolves-all-fields / resolves-mention-role-ids / default-logger / config-prefers-env / resolves-api-url / api-url-config-prefers-env). |
| `packages/adapter-linear` (`@chat-sdk/adapter-linear`) | adapter package | in-progress | `crates/chat-sdk-adapter-linear` | `crates/chat-sdk-adapter-linear/src/lib.rs::tests` (23 = 19 prior + 3 channel_id/is_dm + 1 additive) + `crates/chat-sdk-adapter-linear/src/utils.rs::tests` (13 = 7 prior + 6 additive) + `crates/chat-sdk-adapter-linear/src/cards.rs::tests` (17) + `crates/chat-sdk-adapter-linear/src/markdown.rs::tests` (14 = 13 upstream + 1 additive) + `crates/chat-sdk-adapter-linear/src/thread_id.rs::tests` (18 = 18 upstream) = 86 colocated tests. Slice 185 ported `to_ast` + string/raw `render_postable`; slice 186 added `chat_sdk_chat::markdown::stringify_markdown` and finished LinearFormatConverter with `from_ast`, `render_postable_markdown`, `render_postable_ast`. **All 13 upstream `markdown.test.ts` cases now ported 1:1.** Slice 171 ported `utils.ts`; slice 177 ported `cards.ts` (`cardToLinearMarkdown` + `cardToPlainText`) + all 12 upstream `cards.test.ts` cases. Linear cards.ts is essentially identical to GitHub's cards.ts (same renderer pattern with renamed function); the Rust port mirrors that overlap with a near-identical structure. **Slice 216 adds `src/thread_id.rs`** porting upstream `encodeThreadId` / `decodeThreadId` 1:1: `LinearThreadId { issue_id, comment_id?, agent_session_id? }` struct with 4 wire formats (`linear:<issue>`, `linear:<issue>:c:<comment>`, `linear:<issue>:s:<session>`, `linear:<issue>:c:<comment>:s:<session>`). `decode_thread_id` returns `Result<_, InvalidLinearThreadId>` matching upstream's `ValidationError("linear", ...)` shape. Ports all 18 upstream tests in the `encodeThreadId` (6) + `decodeThreadId` (9) + `roundtrip` (3) describe blocks. This new struct-based API coexists with the simpler `encode_thread_id(team_key, issue_id)` form in `lib.rs` used by the adapter's HTTP code; migration is deferred. **Slice 239 ports `LinearAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's helpers using the slice-216 `thread_id.rs` decoder (handles all 4 wire formats: issue-only, issue+comment, issue+session, issue+comment+session). channelId returns `linear:<issueId>`; isDM always `false`. +3 lib.rs tests covering all 4 wire formats. **Slice 296 extends `LinearAdapterOptions`** with `LinearAuth { ApiKey | AccessToken | OAuth(LinearOAuthCredentials { client_id, client_secret }) }` discriminated union + `webhook_secret` + `user_name` fields + `LinearAdapter::is_multi_tenant()` / `user_name()` / `auth()` accessors. Ports the 3 portable upstream `index.test.ts > describe("constructor")` cases (apiKey / accessToken / OAuth-multi-tenant); the 4th "throws when no auth method provided" case is type-system-impossible since `auth` is a required field. The 5th "should throw when botUserId is accessed before initialization" case is deferred until LinearAdapter exposes a botUserId surface (currently not modeled). **Slice 329 ports `LinearAdapter::remove_reaction` as a documented no-op + `Adapter::remove_reaction` trait method** 1:1 with upstream's known-limitation `removeReaction` (Linear's `reactionDelete` needs the reaction id which the SDK doesn't track). Ports the 1 upstream `index.test.ts > describe("removeReaction")` case (Rust assertion: call resolves to Ok; the upstream logger.warn assertion is js-only since the Rust port hasn't wired logger plumbing yet). Adds `Adapter::remove_reaction(thread_id, message_id, emoji) -> AdapterResult<()>` trait method (default `Err(Unsupported)`) to the chat-sdk-chat trait surface so other adapters can opt in without breaking the workspace. **Slice 306 ports the `createLinearAdapter` env-var-resolution describe block (18 of 19 cases)** via a new `try_create_linear_adapter(opts, env)` factory + `LinearCreateOptions` / `LinearCreateError` + `LinearMode { Comments | AgentSessions }` enum + `api_url` / `mode` fields on `LinearAdapterOptions` + `mode()` / `api_url()` / `webhook_secret()` accessors. Auth resolution priority matches upstream: config.apiKey > config.accessToken > config.clientId+clientSecret > env.LINEAR_API_KEY > env.LINEAR_ACCESS_TOKEN > env.LINEAR_CLIENT_CREDENTIALS_* > env.LINEAR_CLIENT_ID+LINEAR_CLIENT_SECRET. webhookSecret resolves opts ?? env(LINEAR_WEBHOOK_SECRET) ?? error. userName resolves opts ?? env(LINEAR_BOT_USERNAME) ?? "linear-bot". apiUrl resolves opts ?? env(LINEAR_API_URL). Env reader is `Fn(&str) -> Option<String>` closure (sidesteps Rust 2024 `unsafe set_var`). All 18 portable cases pass (110 total Linear tests, +18). The 1 deferred case ("should accept custom logger") is JS-only — logger isn't a first-class adapter dependency in this port yet. **Slice 235 ports `render_message_to_linear_markdown(message, converter)` + `assert_agent_session_thread(thread)`** 1:1 with upstream `utils.ts` (the 2 helpers documented as deferred in the slice 171 port comment). The render helper dispatches `AdapterPostableMessage` variants through `extract_card` -> `card_to_linear_markdown` for cards, or the format converter's `render_postable_*` for text/raw/markdown/ast, then applies emoji-placeholder conversion. The assert helper returns `Result<&str, AdapterError::Validation>` matching upstream's `ValidationError("Expected a Linear agent session thread")`. +6 additive Rust tests. | 9 src files, 4 test files. Linear issue-comment thread adapter. Phase-2. Slice 136 scaffold + slice 151 `post_message` (GraphQL `commentCreate`) + slice 166 the 4 universal trait methods: `edit_message` (`commentUpdate` mutation), `delete_message` (`commentDelete` mutation), `add_reaction` (`reactionCreate` mutation, commentId + emoji), `start_typing` (no-op for the comment thread; the agent-session `createAgentActivity` Thought path is deferred). Adds 3 mutation constants + a private `linear_graphql_call` helper that centralises auth + status + GraphQL-error envelope handling. Distinct from REST adapters: GraphQL 200-status `errors[]` is parsed in the helper. Remaining: real `fetchSubject` (the only upstream adapter that has it - returns rich MessageSubject with issue title/status/assignee/labels), agent-session ports, sub-issue handling, OAuth flow. |
| `packages/adapter-github` (`@chat-sdk/adapter-github`) | adapter package | in-progress | `crates/chat-sdk-adapter-github` | `crates/chat-sdk-adapter-github/src/lib.rs::tests` (28 = 5 upstream channelId+isDM + 23 additive) + `crates/chat-sdk-adapter-github/src/cards.rs::tests` (17) + `crates/chat-sdk-adapter-github/src/markdown.rs::tests` (18) + `crates/chat-sdk-adapter-github/src/webhook.rs::tests` (8 additive) = 72 colocated tests. Slice 187 ported `GitHubFormatConverter`. Slice 196 added `verify_github_signature(body, webhook_secret, signature)` HMAC-SHA256 over `X-Hub-Signature-256` = `sha256=<hex>` format (same shape as WhatsApp but with the **webhook secret** instead of app secret; constant-time compare via `subtle`). Added `hmac`/`sha2`/`subtle` deps. All 18 upstream `markdown.test.ts` cases ported 1:1, all 12 `cards.test.ts` cases ported. 2 of 3 upstream test files fully ported; index.test.ts remains. **Slice 229 ports `GithubAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's helpers (channelId collapses `github:<owner>/<repo>:<n>...` to `github:<owner>/<repo>`; isDM always `false`); +3 lib.rs tests. **Slice 240 reworks `channel_id_from_thread_id`** to walk colon-segments directly instead of delegating to `decode_thread_id` (which only parses 2-segment `<owner>/<repo>:<number>` forms and would reject the 4-segment `<owner>/<repo>:<n>:rc:<m>` review-comment thread). Now both wire formats collapse to the same `github:<owner>/<repo>` channel id. +2 lib.rs tests matching upstream's `channelIdFromThreadId` describe block (PR-level + review-comment threads). **Slice 295 extends `GithubAdapterOptions`** with `GithubAuth { Token | App(GithubAppCredentials { app_id, private_key, installation_id? }) }` discriminated union + `webhook_secret` + `user_name` + `bot_user_id` fields + `GithubAdapter::is_multi_tenant()` / `user_name()` / `bot_user_id()` accessors. Ports the 5 upstream `index.test.ts > describe("constructor")` cases (PAT auth, App-single-tenant, App-multi-tenant, bot_user_id stringified, bot_user_id None). **Slice 368 splits the bundled `emoji_to_github_reaction_maps_well_known_names` test into 16 explicit 1:1 cases** matching upstream `index.test.ts > describe("emojiToGitHubReaction (via addReaction)")` (15 mapping cases + 1 unknown-fallback). Previously these 16 upstream `it()` cases were bundled into a single Rust test (covered the helper's mappings correctly but violated the brief's "every portable upstream case has a matching Rust test" rule). +15 lib.rs tests net (16 new − 1 removed). Per the brief's slice-360 cleanup pattern. chat-sdk-adapter-github 106 tests total. **Slice 307 ports the `createGitHubAdapter` env-var resolution describe block (13/13 cases)** via a new `try_create_github_adapter(opts, env)` factory + `GithubCreateOptions` + `GithubCreateError` + `DEFAULT_USER_NAME = "github-bot"` const + `webhook_secret()` / `api_url()` accessors on `GithubAdapter`. Auth-resolution priority matches upstream: opts.token > opts.app_id+private_key+installation_id? > env(GITHUB_TOKEN) > env(GITHUB_APP_ID)+env(GITHUB_PRIVATE_KEY)+env(GITHUB_INSTALLATION_ID?). "Don't mix auth modes" is enforced — partial config (e.g. only app_id) yields `AuthenticationRequired` rather than falling through to env. webhookSecret resolves opts ?? env(GITHUB_WEBHOOK_SECRET) ?? error. userName resolves opts ?? env(GITHUB_BOT_USERNAME) ?? "github-bot". apiUrl resolves opts ?? env(GITHUB_API_URL). botUserId is stringified to match upstream's `botUserId: string` shape. Env reader is `Fn(&str) -> Option<String>` closure (slice-305 pattern). All 13 portable cases pass (91 total GitHub tests, +13). |
| `packages/adapter-messenger` (`@chat-sdk/adapter-messenger`) | adapter package | in-progress | `crates/chat-sdk-adapter-messenger` | `crates/chat-sdk-adapter-messenger/src/lib.rs::tests` (18 = 6 upstream thread-id + 12 additive) + `crates/chat-sdk-adapter-messenger/src/cards.rs::tests` (46 = 44 upstream + 2 additive) + `crates/chat-sdk-adapter-messenger/src/markdown.rs::tests` (10) + `crates/chat-sdk-adapter-messenger/src/webhook.rs::tests` (10 additive) = 92 colocated tests. **cards.test.ts now fully ported (44/44).** Facebook Messenger adapter. Phase-2. Slice 132 scaffold + slice 147 post_message + slice 163 edit/delete/react/typing + slice 173 wire-format correction + slice 180 cards callback codec + slice 188 markdown converter + slice 195 webhook HMAC-SHA256 verifySignature. **Slice 209 ports all 13 upstream `text fallback rendering` cases**: extended `render_child` to handle `Link` (`<label>: <url>`) and `Table` variants (added `render_table` helper producing pipe-separated rows). Ports 13 upstream cases: simple-title, title+subtitle, text-content, fields, link-buttons, action-buttons, inline-image, image-without-alt, divider, section, link-element, table, image-url-header. Remaining: template-message rendering (Generic Template ~25 cases + Button Template ~30 cases + constraint handling) + post_object + integration. **Slice 228 ports `MessengerAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's DM-only inline helpers (channel === thread, isDM always `true`); +2 lib.rs tests. **Slice 334 ports `MessengerAdapter::remove_reaction`** 1:1 with upstream's `removeReaction` throw — Messenger has no reactions API; both `add_reaction` and `remove_reaction` raise `ValidationError` ("Messenger does not support reactions via API") which the Rust port surfaces as `AdapterError::InvalidPayload`. Ports the 1 portable upstream `index.test.ts > it("throws on removeReaction")` case. +1 lib.rs test. **Slice 301 extends `MessengerAdapterOptions`** with `app_secret` + `user_name` fields + `DEFAULT_USER_NAME = "bot"` const + `effective_user_name()` / `user_name()` / `app_secret()` accessors. Ports 2 portable upstream `createMessengerAdapter > describe("factory function")` cases (create-instance / uses-provided-user-name-and-app-secret). **Slice 369 ports `normalize_thread_id` helper + the 5th `thread ID encoding` describe-block case** 1:1 with upstream `index.test.ts > describe("thread ID encoding") > it("resolves raw thread ID without messenger: prefix")`. The upstream behavior allows `adapter.postMessage("USER_123", ...)` to implicitly prepend the `messenger:` prefix; the Rust port exposes this normalization as a pure `pub fn normalize_thread_id(thread_id) -> String` helper that callers (HTTP dispatcher path) can route through. Passes through already-prefixed inputs unchanged. +2 lib.rs tests (1 1:1 upstream + 1 pass-through). chat-sdk-adapter-messenger 102 tests total. **Slice 310 ports the remaining 4 upstream `factory function` describe-block cases** via a new `try_create_messenger_adapter(opts, env)` factory + `MessengerCreateOptions` + `MessengerCreateError`. Resolution rules: app_secret / page_access_token / verify_token ?? non-empty env(FACEBOOK_*) ?? typed error; user_name passed through. Empty env strings treated as missing per upstream `process.env.X = ""` → throw. Env reader is `Fn(&str) -> Option<String>` closure (slice-305 pattern). 99 Messenger tests pass (+4). |
| `packages/adapter-telegram` (`@chat-sdk/adapter-telegram`) | adapter package | in-progress | `crates/chat-sdk-adapter-telegram` | `crates/chat-sdk-adapter-telegram/src/lib.rs::tests` (39 = 16 upstream channelId+isDM+applyTelegramEntities + 23 additive) + `crates/chat-sdk-adapter-telegram/src/cards.rs::tests` (9) + `crates/chat-sdk-adapter-telegram/src/markdown.rs::tests` (62 = 54 upstream + 8 additive) = 116 colocated tests. Slice 210 ports 16 additional upstream markdown.test.ts cases (links/images, block structures, nested formatting, edge cases). **Slice 211 ports the 2 final MarkdownV2-validity-corpus invariant tests** (`produces non-empty output covering every structural element` + `escapes every in-text MarkdownV2 special character outside code and link URLs`) using the upstream LLM_CORPUS verbatim — added a `strip_code_blocks_inline_and_link_urls` helper for the second assertion's invariant sweep. **All ~57 portable upstream `markdown.test.ts` cases now ported 1:1** (parametric special-char loop covers all 18 individual `it("escapes special X")` cases as a single Rust test). Slice 182 ported the pure-helper subset of `markdown.ts`. Slice 191 added `TelegramFormatConverter` + the recursive `render_markdown_v2(&Node)` walker covering all 20+ mdast variants 1:1 with upstream `renderMarkdownV2`: root → "\n\n" join, paragraph → concat, text → escapeMarkdownV2, strong → `*x*`, emphasis → `_x_`, delete → `~x~`, inlineCode → `\`escapeCodeBlock\``, code → ` ```lang\n... ``` `, link → `[label](escapeLinkUrl)`, blockquote → `>` prefix per line, list (ordered=`escapeMarkdownV2("N.")` + content; unordered=`\\- ` content), heading → bold, thematicBreak → escaped "———", break → "\n", image → `[alt](url)`, html → escaped, linkRef/imageRef/footnoteRef → degraded labels, definition/footnoteDefinition/yaml/toml → empty, table → preprocessed to code block in `from_ast` via walk_ast. Adds `escape_code_block` + `escape_link_url` pub helpers. Ports 13 upstream `markdown.test.ts` cases (8 inline-formatting + 3 code blocks + 1 link + 4 renderPostable + 2 escape-helper-additive). Phase-2. Slice 130 scaffold + slice 145 post_message + slice 155 fetch_subject + slice 161 edit/delete/react/typing. cards.test.ts 9/9. Remaining: post_object + nested list/link tests + index.test.ts. **Slice 228 ports `TelegramAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's `resolveThreadId`-based helpers: channelId strips any `:<message_thread_id>` suffix, isDM returns true iff the underlying Telegram `chat_id` is non-negative (Telegram convention: groups/supergroups/channels have negative chat ids). +5 lib.rs tests. **Slice 236 ports `apply_telegram_entities(text, &[TelegramMessageEntity])` + `TelegramMessageEntity` struct + private `escape_markdown_in_entity` helper** 1:1 with upstream's inbound entity → standard markdown converter. Sort-desc-by-offset/asc-by-length to make right-to-left splicing safe; handles `text_link` (with escape of `[]()\\` in label) / `bold` / `italic` / `code` / `pre` (with optional `language`) / `strikethrough`; leaves `url`/`mention`/`bot_command`/etc. unchanged. All 11 portable upstream `applyTelegramEntities` cases ported (no-op-empty, text_link, bold, italic, code, pre, pre-with-language, strikethrough, url-unchanged, mention-unchanged, multiple-entities, special-chars-in-text_link). The 12th upstream case is `parseMessage`-with-entities integration which needs the full adapter parseMessage path; deferred. **Slice 232 ports `truncate_for_telegram(text, limit, parse_mode)` + private helpers `find_unescaped_positions_outside_code` + `trim_to_markdown_v2_safe_boundary`** 1:1 with upstream `truncateForTelegram`. The MarkdownV2 path uses `\.\.\.` escaped ellipsis and trims back past orphan backslashes / unbalanced `*` `_` `~` `` ` `` / unmatched `[` to keep the result parseable. All 9 upstream `truncateForTelegram` cases ported (under-limit, plain-truncate-with-literal-ellipsis, markdown-v2-with-escaped-ellipsis, strip-orphan-backslash, strip-unclosed-bold, all-special-chars, strip-unpaired-markers-under-limit, preserve-code-fences-with-literal-asterisks, no-modify-plain, preserve-balanced-markdown-under-limit). All ~57 + 9 = 66 portable upstream `markdown.test.ts` cases now ported. **Slice 345 ports the upstream `describe("TelegramAdapter") > it("encodes and decodes thread IDs")` named case** 1:1 with upstream's specific value-pair assertions: `encodeThreadId({chatId: "-100123"}) -> "telegram:-100123"`, `encodeThreadId({chatId: "-100123", messageThreadId: 42}) -> "telegram:-100123:42"`, `decodeThreadId("telegram:-100123:42") -> {chatId: -100123, messageThreadId: 42}`. The existing generic round-trip test (slice 145 era) covers preservation but not the upstream's specific value assertions. +1 lib.rs test. **Slice 342 ports the URL-shape assertions** of upstream's `index.test.ts > it("posts, edits, deletes, and sends typing events")` describe via a parametric `adapter_method_url_produces_telegram_endpoints_for_all_runtime_methods` test asserting the `method_url` helper produces correct URLs for the 5 runtime endpoints (sendMessage / editMessageText / deleteMessage / sendChatAction / setMessageReaction). Body-shape assertions stay covered by the existing per-method tests. +1 lib.rs test. **Slice 332 ports `TelegramAdapter::remove_reaction`** 1:1 with upstream — uses the same `setMessageReaction` endpoint as `add_reaction` but sends `reaction: []` to clear the bot's reactions on the message. Ports 1 portable upstream `index.test.ts` case (round-trip add+remove asserts `reaction: []`) + 1 additive negative case (non-telegram thread id rejected). +2 lib.rs tests. **Slice 302 extends `TelegramAdapterOptions`** with `secret_token` + `user_name` fields + `DEFAULT_USER_NAME = "bot"` const + `effective_user_name()` / `user_name()` / `secret_token()` accessors. Ports 2 portable upstream `createTelegramAdapter` cases (create-instance / uses-provided-secret-token-and-user-name). **Slice 308 ports the `createTelegramAdapter` + `constructor env var resolution` describe blocks (11/11 cases)** via a new `try_create_telegram_adapter(opts, env)` factory + `TelegramCreateOptions` + `TelegramCreateError`. Resolution rules: bot_token ← opts ?? non-empty env(TELEGRAM_BOT_TOKEN); secret_token ← opts ?? env(TELEGRAM_WEBHOOK_SECRET_TOKEN); user_name ← opts ?? env(TELEGRAM_BOT_USERNAME) ?? "bot"; base_url ← opts.api_url ?? opts.api_base_url ?? env(TELEGRAM_API_BASE_URL) ?? DEFAULT_BASE_URL (1:1 with upstream's `apiUrl ?? apiBaseUrl`). Empty `TELEGRAM_BOT_TOKEN=""` treated as missing per upstream. Env reader is `Fn(&str) -> Option<String>` closure (slice-305 pattern). 129 Telegram tests pass (+11). |
| `packages/adapter-whatsapp` (`@chat-sdk/adapter-whatsapp`) | adapter package | in-progress | `crates/chat-sdk-adapter-whatsapp` | `crates/chat-sdk-adapter-whatsapp/src/lib.rs::tests` (32 = 17 upstream thread-id+splitMessage+renderFormatted + 15 additive) + `crates/chat-sdk-adapter-whatsapp/src/cards.rs::tests` (25 = 23 upstream + 2 additive) + `crates/chat-sdk-adapter-whatsapp/src/markdown.rs::tests` (28 = 23 upstream + 5 additive) + `crates/chat-sdk-adapter-whatsapp/src/webhook.rs::tests` (7 additive) = 94 colocated tests. Slice 189 ported `WhatsAppFormatConverter`. Slice 194 added `verify_whatsapp_signature` HMAC-SHA256. **Slice 207 ports the final 4 upstream `markdown.test.ts` cases**: escaped asterisks/tildes (added `(?<![\\])` lookbehind to `upgrade_single_to_double`); headings with mixed text+bold (already worked); tables -> code block (already worked); complex AI-style markdown response (already worked). Also extended `chat_sdk_chat::markdown::stringify_markdown` to escape `*` `_` `~` `` ` `` `\` `[` `]` in Text nodes (1:1 with upstream remark-stringify behavior), so escape-round-trips work. **All 23 of 23 upstream `markdown.test.ts` cases now ported 1:1**. Slice 208 ports the 10 upstream `cardToWhatsAppText` describe-block cases (simple title, title+subtitle, text content, fields with WhatsApp bold, link-button rendering, action-button bracketed text, image with alt:url, divider, section, multi-style text). cards.test.ts 18/23 now ported (8 callback codec + 1 plain text + 10 cardToText - 1 since "card_to_whatsapp_text_uses_single_asterisk_bold_for_title" overlaps with the simple-title case); remaining 5 are `cardToWhatsApp` interactive-message branch. **Slice 221 ports the `cardToWhatsApp` interactive-button payload renderer** (WhatsAppCardResult enum + WhatsAppInteractiveMessage / Header / Body / Action / ReplyButton types + find_actions/extract_reply_buttons/build_body_text/truncate_chars helpers). Ports all 5 upstream `cardToWhatsApp` describe-block cases (interactive for 1-3 buttons, truncate >3, fall-back for link-only, fall-back for no-actions, truncate-button-title-to-20-chars). **All 23 of 23 upstream `cards.test.ts` cases now ported 1:1**. **Slice 228 ports `WhatsappAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's DM-only inline helpers (channel === thread, isDM always `true`); +2 lib.rs tests. **Slice 231 ports `WHATSAPP_MESSAGE_LIMIT = 4096` + `split_message(text)`** 1:1 with upstream's `splitMessage(text)` paragraph→line→hard-break chunker with `< limit/2` guard preventing tiny "early" chunks. Ports all 8 upstream `splitMessage` describe-block cases (single chunk for short, exact 4096, paragraph break, line break, hard break, three chunks, skip too-early break, preserve content). **Slice 241 ports `WhatsappAdapter::render_formatted(&Node) -> String`** 1:1 with upstream's `adapter.renderFormatted(content)` which delegates to `formatConverter.fromAst(content)`. +1 lib.rs test (render markdown from AST). **Slice 233 tightens `decode_thread_id`** to reject extra colon-separated segments (was `splitn(2, ':')` which silently accepted them; now `split(':')` + exact-length check, matching upstream `parts.length !== 2 → throw`). +4 lib.rs tests: extra-segments rejected, completely-wrong-format rejected, encode-with-different-phone-numbers, round-trip-with-international-numbers. **Slice 333 ports `WhatsappAdapter::remove_reaction`** 1:1 with upstream — same Cloud API endpoint as `add_reaction` (`POST /{phone_number_id}/messages`) but with `reaction: { message_id, emoji: "" }` to clear the bot's reaction on `message_id`. The `emoji` parameter is intentionally ignored (matches upstream's `_emoji` signature). Ports the 1 portable upstream `index.test.ts > describe("addReaction / removeReaction") > it("removeReaction sends reaction with empty emoji")` case + 1 additive negative case (mismatched phone_number_id) + 1 additive negative case (non-whatsapp thread id). +2 lib.rs tests. **Slice 303 extends `WhatsappAdapterOptions`** with `app_secret` + `user_name` fields + `DEFAULT_USER_NAME = "bot"` const + `effective_user_name()` / `user_name()` / `app_secret()` accessors. Ports 2 portable upstream `createWhatsAppAdapter` cases (create-instance / uses-provided-user-name-and-app-secret). **Slice 309 ports the `createWhatsAppAdapter` env-var resolution describe block (6/6 cases)** via a new `try_create_whatsapp_adapter(opts, env)` factory + `WhatsappCreateOptions` + `WhatsappCreateError`. Adds `api_version: Option<String>` field to `WhatsappAdapterOptions` + `DEFAULT_FACTORY_USER_NAME = "whatsapp-bot"` const + `api_version()` + `graph_api_url()` accessors on `WhatsappAdapter`. Resolution: access_token/app_secret/phone_number_id/verify_token ?? env(WHATSAPP_*) ?? typed error; user_name ?? env(WHATSAPP_BOT_USERNAME) ?? "whatsapp-bot"; api_url ?? env(WHATSAPP_API_URL); api_version ?? DEFAULT_API_VERSION ("v21.0"). Env reader is `Fn(&str) -> Option<String>` closure (slice-305 pattern). All 6 portable upstream cases pass (104 total WhatsApp tests, +6).
| `packages/adapter-web` (`@chat-sdk/adapter-web`) | adapter package | js-only-documented | n/a | rationale below | 9 src files, 1 test file: `react/index.ts`, `svelte/index.ts`, `vue/index.ts` are 100% browser-framework UI integrations (Next/React/Svelte/Vue components binding browser DOM events) with no Rust analogue. The server-side `adapter.ts` is tightly coupled to the JavaScript AI SDK UI message streaming protocol (`createUIMessageStream` / `UIMessage` from the upstream `ai` npm package), `als.ts` uses Node `AsyncLocalStorage`, and `format-converter.ts` produces JSX-only output. The whole package is web-platform glue. Rust adopters integrate with the AI SDK chat-streaming protocol through a separate Rust-side surface (the `ai-sdk-rust` workspace owns that contract); duplicating the browser/UI integration would not be useful. Phase-2. |
| `packages/state-redis` (`@chat-sdk/state-redis`) | state backend (Redis) | in-progress | `crates/chat-sdk-state-redis` | `crates/chat-sdk-state-redis/src/lib.rs::tests` (17 colocated tests). | 2 src files, 1 test file. Production Redis state. Phase-3. Slice 140 scaffolds the crate: `RedisStateAdapter` impl-ing `chat_sdk_chat::types::StateAdapter` with all 5 required methods returning `StateAdapterError::NotConnected` (placeholder until `redis-rs` + `bb8-redis` are wired in per the port-chat-sdk.md "Phase 2/3 prep" runtime decision). `RedisStateAdapterOptions` (Redis URL + key prefix) + `prefixed_key()` namespace helper. Lock primitive + pub/sub deferred. **Slice 246 ports `generate_token() -> String`** 1:1 with upstream's private `generateToken()` (`redis_<unix_ms>_<13-char-base36-lowercase>`) — exposed at module scope (rather than private as upstream) so the suffix shape can be unit-tested without needing a live Redis connection. Adds `rand = "0.8"` dep. +3 additive tests: prefix+underscore-count shape, 13-char-base36-lowercase suffix, 1000-call uniqueness. **Slice 361 maps the upstream `should have X method` describe-block cases to the existing NotConnected smoke tests** + adds 3 new NotConnected smoke tests for `set_if_not_exists` / `connect` / `disconnect`. Documents the 1:1 mapping: upstream's `appendToList`/`getList` method-existence tests map to the existing NotConnected smoke tests (calling a method proves it exists in Rust), and the new tests cover `set_if_not_exists` / `connect` / `disconnect`. The 3 upstream `enqueue`/`dequeue`/`queueDepth` method-existence tests remain deferred until the chat-sdk-chat `StateAdapter` trait gets extended with the queue-primitive methods (currently only `state-memory` has them as inherent methods). Also strengthens `channel_basic_properties_should_have_correct_id_and_adapter` to assert `name().is_none()` matching upstream's full assertion list. +3 lib.rs tests in state-redis. |
| `packages/state-ioredis` (`@chat-sdk/state-ioredis`) | state backend (ioredis) | in-progress | `crates/chat-sdk-state-ioredis` | `crates/chat-sdk-state-ioredis/src/lib.rs::tests` (18 colocated tests). | 2 src files, 1 test file. Production ioredis state. Phase-3. Slice 141 scaffolds the crate: `IoredisStateAdapter` (Redis cluster + Sentinel support) impl-ing `chat_sdk_chat::types::StateAdapter` with the 5 required methods returning `NotConnected` placeholders. `IoredisStateAdapterOptions` (cluster nodes + optional Sentinel master name + key prefix). The Rust port collapses upstream's two Redis backends (`state-redis` single-node + this `state-ioredis` cluster/sentinel) into the same family. `redis::cluster_async::ClusterClient` wire-up deferred until workspace runtime commits to tokio. **Slice 362 mirrors slice 361** — maps the upstream `should have X method` describe-block cases to existing NotConnected smoke tests + adds 3 new NotConnected smoke tests for `set_if_not_exists` / `connect` / `disconnect`. +3 lib.rs tests. |
| `packages/state-pg` (`@chat-sdk/state-pg`) | state backend (Postgres) | in-progress | `crates/chat-sdk-state-pg` | `crates/chat-sdk-state-pg/src/lib.rs::tests` (16 colocated tests). | 2 src files, 1 test file. Production Postgres state. Phase-3. Slice 142 scaffolds the crate: `PgStateAdapter` impl-ing `chat_sdk_chat::types::StateAdapter` with the 5 required methods returning `NotConnected` placeholders. `PgStateAdapterOptions` (database_url + table prefix), `state_table()` / `lists_table()` helpers for the upstream two-table schema (`chat_state` kv + `chat_state_lists` for list-typed values). `tokio-postgres`/`sqlx` client wire-up + schema migrations + `pg_try_advisory_lock` deferred until workspace runtime commits to tokio. **Slice 362 mirrors slice 361** — maps upstream `describe("ensureConnected")` cases that throw `Error("not connected")` to the existing NotConnected smoke tests (the Rust port surfaces the same pre-connect contract via `StateAdapterError::NotConnected`) + adds 3 new NotConnected smoke tests for `set_if_not_exists` / `connect` / `disconnect`. +3 lib.rs tests. |
| `packages/integration-tests` (`@chat-sdk/integration-tests`) | integration tests | js-only-documented | n/a | rationale below | 54 src files, 41 test files (~15k lines). Vitest-based cross-package suite covering: live `*.test.ts` files against real Slack/Discord/Teams/GChat/Messenger/Telegram/WhatsApp APIs, replay tests reading recorded JSON snapshots from `fixtures/` and `emulator/`, and documentation tests that parse upstream MDX. The suite uses Vitest's `vi.fn()`, fetch interception, recorded-network replay (HAR-style), and Node-only test orchestration. There is no Rust analogue - Rust adopters write their own integration tests against the Rust adapter implementations as each adapter lands (the per-crate `tests/` directory hosts integration tests in the cargo idiom; recorded scenarios stay upstream as a reference for WHAT to assert, not as a literal port target). Phase-4. See JavaScript-only Exceptions table for the formal entry. |

### Non-package upstream surfaces

| Item | Kind | Status | Rust path | Evidence | Notes |
| --- | --- | --- | --- | --- | --- |
| `apps/docs` | documentation site | js-only-documented | n/a | rationale below | Next.js documentation site (`next.config.ts`, React `app/`, `components/`, MDX `content/`). Whole-app surface is Next.js + React rendering, not portable to Rust. The user-facing content (`content/*.mdx`) is portable as plain markdown if the docs are rebuilt under a Rust static-site generator, but that is intentionally out of scope - Rust adopters consume the same MDX upstream renders. See JavaScript-only Exceptions table for the formal entry. |
| `examples/nextjs-chat` | Next.js example | js-only-documented | n/a | rationale below | Next.js web UI example built on `adapter-web`'s browser chat protocol. Both the example and `adapter-web`'s React/DOM portions are Next.js/React-only; the example has no portable Rust counterpart. The protocol/server half of adapter-web stays in scope (see the `adapter-web` row above). |
| `examples/telegram-chat` | runnable example | js-only-documented | n/a | rationale below | 8 source files including 2 `.tsx` (JSX-authored) card/menu definitions: `src/menu.tsx`, `src/demos/cards.tsx`. The example demonstrates upstream `chat-sdk` usage with JSX card authoring - a TypeScript/React-only DSL with no Rust counterpart (per the existing JSX runtime js-only-documented exception). When `chat-sdk-adapter-telegram` lands in Rust, the canonical Rust example will use Rust-idiomatic builder code (e.g. `card(CardOptions { ... })`) rather than JSX. The upstream `.tsx` files are not a literal port target. See JavaScript-only Exceptions table. |
| `skills/chat` | Anthropic skill spec | js-only-documented | n/a | `SKILL.md` describes the upstream `@chat-sdk` JavaScript packages by NPM identifier and TypeScript API shape. The spec content is content-only (Markdown), not code, and it documents JS-only authoring surfaces (JSX runtime, NPM imports). Rust adopters consume the same upstream skill spec via the upstream repo; carrying a Rust-specific clone is out of scope. |
| `scripts/` (upstream) | build/release tooling | js-only-documented | n/a | Single file: `scripts/sync-resources.ts`. Node-only file-system tooling that pulls `apps/docs/resources-edge-config.json` into `packages/chat/resources/`. Build/release tooling specific to the upstream pnpm/turbo monorepo - no Rust port needed (the Rust workspace uses cargo build/test directly). |

## Next Unported Work Queue

**State as of slice 59 (post-merge `dfbd07e`):**

- 2 packages verified: `packages/adapter-shared`, `packages/state-memory`.
- 7 surfaces marked js-only-documented at the row level: `packages/tests`, `packages/adapter-web`, `packages/integration-tests`, `apps/docs`, `examples/nextjs-chat`, `examples/telegram-chat`, `skills/chat`, `scripts/` (upstream Node tooling).
- `packages/chat` in-progress at 74%: 11 modules portable-mapped (errors 17, logger 13, chat_singleton 5, emoji 42/42, modals 25, markdown 122/122 (1:1 COMPLETE), cards 29/28 incl fallback, callback_url 5/17, message 12/19, plan 10 additive, transcripts 9 additive, postable_object 4 additive, reviver 6 additive). 284 colocated Rust tests. Sub-file js-only-adjacent: `jsx-*` runtime files, `mock-adapter.ts`, `message-history.ts`.
- The 12 remaining real-implementation rows: `packages/chat` finish (channel ~600 LOC + thread ~1100 + chat.ts ~2700 + from-full-stream + streaming-markdown + serialization + transcripts-wiring), 9 Phase-2 adapters (adapter-slack, -teams, -gchat, -discord, -linear, -github, -messenger, -telegram, -whatsapp), 3 Phase-3 state backends (state-redis, -ioredis, -pg).

**Pick-up plan for the next session:**

1. Extend `chat::types::Adapter` trait with the concrete async method set (`post_message`, `edit_message`, `delete_message`, `add_reaction`, `remove_reaction`, `start_typing`, `fetch_messages`, `fetch_thread`, `fetch_message`, `encode_thread_id`, `decode_thread_id`, `parse_message`, `render_formatted`, `open_dm`, `is_dm`, `get_channel_visibility`, `open_modal`, `channel_id_from_thread_id`, `fetch_channel_messages`, `list_threads`, `fetch_channel_info`, `post_channel_message`, `post_object`, `fetch_subject`). Use `async-trait` for dyn safety. This unblocks: message::subject getter, callback_url stateful path, transcripts::TranscriptsApiImpl, postable_object::post_postable_object, reviver Thread/Channel branches, AND every Phase-2 adapter.
2. Extend `chat::types::StateAdapter` trait with the async method set already proven by `chat_sdk_state_memory::MemoryStateAdapter`'s inherent methods (connect/disconnect/subscribe/.../enqueue/dequeue/queue_depth). This unblocks state-redis/ioredis/pg as ports of the same trait against external client crates.
3. Port `chat::channel` (~600 LOC, 1420 LOC of tests) - ChannelImpl + SerializedChannel. Largest remaining structural piece in `packages/chat`.
4. Port `chat::thread` (~1100 LOC, 3257 LOC of tests) - ThreadImpl + SerializedThread + ThreadHistoryCache.
5. Port `chat::chat` (~2700 LOC, 4907 LOC of tests) - top-level Chat + ChatConfig + ChatInstance.
6. Begin Phase-2 adapter porting once chat verifies. Order adapters by contract complexity: smallest first (adapter-github 7 src / 3 tests, adapter-messenger 7 / 3, adapter-telegram 7 / 3, adapter-whatsapp 7 / 3), then adapter-discord 8 / 4, adapter-linear 9 / 4, adapter-gchat 13 / 6, adapter-teams 16 / 6, adapter-slack 24 / 11.
7. After all Phase-2 adapters verified, port Phase-3 state backends (state-redis/ioredis/pg) sequentially.

Realistic remaining slice budget: ~200-300 chat-finishing slices + ~150-300 per adapter (× 9) + ~30 per state backend (× 3) + final integration verification. Multi-week effort across many sessions.

## JavaScript-only Exceptions

Tracked here as they are confirmed. Each entry must cite the upstream file and the reason the surface is not portable to Rust.

| Upstream surface | Reason |
| --- | --- |
| `packages/chat/src/jsx-runtime.ts`, `jsx-runtime.test.ts`, `jsx-runtime.test.tsx`, `jsx-react.test.tsx`, `jsx-dev-runtime` export | JSX runtime is a TypeScript/React-only authoring surface; Rust has no equivalent template compiler binding. Card/modal data shapes ARE portable (see `cards.ts`, `modals.ts`) - only the JSX authoring layer is excluded. |
| `packages/chat/src/mock-adapter.ts` | Vitest-glue: `mockLogger` + `createMockAdapter` factory built on `vi.fn()`. No test file (used only by other test files). Rust analogue is inline fixtures inside each crate's `#[cfg(test)] mod tests`; see e.g. `chat_sdk_state_memory::tests::fresh` for the per-test factory pattern. |
| `packages/chat/src/message-history.ts` | Deprecated re-export shim that aliases `MessageHistoryCache` -> `ThreadHistoryCache` and `MessageHistoryConfig` -> `ThreadHistoryConfig` for backwards compatibility with pre-rename callers. The upstream file's only purpose is the JS deprecation `@deprecated` JSDoc tag - the Rust port has never shipped the old name, so no shim is needed. |
| `apps/docs/**` (Next.js documentation site) | Whole-app surface: `next.config.ts`, React `app/`, hooks, components, MDX rendering pipeline. Not portable to Rust. The MDX content under `apps/docs/content/` is portable as raw markdown if reused under a Rust SSG, but that is intentionally out of scope - Rust adopters consume the same upstream-rendered docs. |
| `examples/nextjs-chat/**` (Next.js web chat example) | Browser-only React app built on `adapter-web`. Not portable as a Rust example. The protocol/server half of `adapter-web` itself remains in scope and is classified per-subfile during the `adapter-web` slice. |
| `packages/adapter-web/**` (browser chat UI adapter) | React/Svelte/Vue UI integrations are 100% browser-framework code. The server-side `adapter.ts` is tightly coupled to the JavaScript `ai` package's `createUIMessageStream`/`UIMessage` protocol and Node `AsyncLocalStorage`. Rust adopters integrate with the AI SDK chat-streaming protocol through the separate `ai-sdk-rust` workspace; duplicating the browser/UI integration would not be useful. Per-file classification confirmed all 9 source files are web-platform glue. |
| `packages/integration-tests/**` (Vitest live + replay suite) | ~15k LOC Vitest-orchestrated cross-package suite: live HTTP scenarios against Slack/Discord/Teams/GChat/Messenger/Telegram/WhatsApp APIs, HAR-style replay snapshots under `fixtures/` and `emulator/`, and MDX documentation tests. The whole suite is Node-only test orchestration around Vitest `vi.fn()` and fetch interception. Rust adopters write their own integration tests inside each crate's `tests/` directory (cargo's standard integration-tests location) against the Rust adapter implementations. The upstream scenarios stay as a reference for assertions; the recorded fixtures may be reusable byte-for-byte when Rust adapters land. |
| `examples/telegram-chat/**` (JSX-authored Telegram bot example) | 8 source files including `src/menu.tsx` and `src/demos/cards.tsx` that author cards via JSX - a TypeScript/React-only DSL with no Rust counterpart. When `chat-sdk-adapter-telegram` lands in Rust, the canonical example will use Rust-idiomatic builder code (`card(CardOptions { ... })`) rather than JSX. The upstream `.tsx` files are not a literal port target. |

## Chat Package Test-File Triage

Per the slice-59 refinement, classify each `packages/chat/src/*.test.ts` by dependency to make future slice planning mechanical. `pure` = no external deps; `state` = needs `StateAdapter` trait extension; `adapter` = needs `Adapter` trait extension; `class` = needs `Message`/`Channel`/`Thread` class ports; `stream` = needs async stream infra; `js-only` = JSX runtime or other framework-bound.

| Test file | Status | Triage | Rust crate location |
| --- | --- | --- | --- |
| `errors.test.ts` | 1:1 (17/17) | pure | `chat-sdk-chat::errors` |
| `logger.test.ts` | 1:1 (13/13) | pure | `chat-sdk-chat::logger` |
| `chat-singleton.test.ts` | 1:1 (5/5) | pure | `chat-sdk-chat::chat_singleton` |
| `emoji.test.ts` | 1:1 (42/42) | pure | `chat-sdk-chat::emoji` |
| `cards.test.ts` | 29/28 (fallback added) | pure | `chat-sdk-chat::cards` |
| `modals.test.ts` | 25/29 (20/20 portable + 5 additive; 9 JSX-only) | pure (portable subset) | `chat-sdk-chat::modals` |
| `markdown.test.ts` | 122/122 (1:1 COMPLETE) | pure | `chat-sdk-chat::markdown` |
| `callback-url.test.ts` | 5/17 portable pure helpers + 5 additive (is_callback_value, callback_cache_key, empty-token round-trip) | state (12 remaining cases) | `chat-sdk-chat::callback_url` |
| `message.test.ts` | 12/19 portable subset (incl. buffer-strip) | adapter (5 subject getter cases) + js-only (2 WORKFLOW_SERIALIZE Symbol cases) | `chat-sdk-chat::message` |
| `from-full-stream.test.ts` | 16/16 portable 1:1 + 1 additive | pure | `chat-sdk-chat::from_full_stream` |
| `thread-history.test.ts` | constants + key formatter only | state (all 7 cases) + class (Message instances) | `chat-sdk-chat::thread_history` |
| `transcripts.test.ts` | parseDuration + constants additive | state + class (Message/Postable) | `chat-sdk-chat::transcripts` |
| `transcripts-wiring.test.ts` | not started | state + adapter (Chat-instance wiring) | n/a |
| `channel.test.ts` | not started | class (Channel) + adapter | n/a |
| `chat.test.ts` | not started | class (Chat singleton + ChatInstance) + adapter | n/a |
| `thread.test.ts` | not started | class (Thread) + adapter + state | n/a |
| `serialization.test.ts` | not started | class (all serialized types) | n/a |
| `streaming-markdown.test.ts` | not started | external (depends on `remend` npm package; needs a Rust streaming markdown renderer or skip) | n/a |
| `jsx-react.test.tsx` | js-only | js-only (JSX runtime) | n/a |
| `jsx-runtime.test.ts` | js-only | js-only (JSX runtime) | n/a |
| `jsx-runtime.test.tsx` | js-only | js-only (JSX runtime) | n/a |

Pick-up priority: complete `markdown` (stringify_markdown), then port `Channel`/`Thread`/`Chat` classes (unlocks 4 unstarted test files), then extend the `StateAdapter` and `Adapter` traits (unlocks every state-bound and adapter-bound deferred case across the chat package).

## Test-Case Parity Map

Populated as each upstream test file is mapped to Rust. Format: one row per upstream test/case.

| Upstream file:case | Rust crate::test | Status | Notes |
| --- | --- | --- | --- |
| `packages/chat/src/errors.test.ts`:`ChatError > should set message, code, and name` | `chat-sdk-chat::errors::tests::chat_error_should_set_message_code_and_name` | mapped | |
| `packages/chat/src/errors.test.ts`:`ChatError > should be instanceof Error` | `chat-sdk-chat::errors::tests::chat_error_should_be_instanceof_error` | mapped | Adapted: Rust uses `dyn std::error::Error` + enum-variant `matches!` rather than JS prototype chain. |
| `packages/chat/src/errors.test.ts`:`ChatError > should propagate cause` | `chat-sdk-chat::errors::tests::chat_error_should_propagate_cause` | mapped | |
| `packages/chat/src/errors.test.ts`:`ChatError > should allow undefined cause` | `chat-sdk-chat::errors::tests::chat_error_should_allow_undefined_cause` | mapped | |
| `packages/chat/src/errors.test.ts`:`RateLimitError > should set code to RATE_LIMITED` | `chat-sdk-chat::errors::tests::rate_limit_error_should_set_code_to_rate_limited` | mapped | |
| `packages/chat/src/errors.test.ts`:`RateLimitError > should store retryAfterMs` | `chat-sdk-chat::errors::tests::rate_limit_error_should_store_retry_after_ms` | mapped | |
| `packages/chat/src/errors.test.ts`:`RateLimitError > should allow undefined retryAfterMs` | `chat-sdk-chat::errors::tests::rate_limit_error_should_allow_undefined_retry_after_ms` | mapped | |
| `packages/chat/src/errors.test.ts`:`RateLimitError > should be instanceof ChatError and Error` | `chat-sdk-chat::errors::tests::rate_limit_error_should_be_instanceof_chat_error_and_error` | mapped | Adapted (see above). |
| `packages/chat/src/errors.test.ts`:`RateLimitError > should propagate cause` | `chat-sdk-chat::errors::tests::rate_limit_error_should_propagate_cause` | mapped | |
| `packages/chat/src/errors.test.ts`:`LockError > should set code to LOCK_FAILED` | `chat-sdk-chat::errors::tests::lock_error_should_set_code_to_lock_failed` | mapped | |
| `packages/chat/src/errors.test.ts`:`LockError > should be instanceof ChatError` | `chat-sdk-chat::errors::tests::lock_error_should_be_instanceof_chat_error` | mapped | Adapted. |
| `packages/chat/src/errors.test.ts`:`LockError > should propagate cause` | `chat-sdk-chat::errors::tests::lock_error_should_propagate_cause` | mapped | |
| `packages/chat/src/errors.test.ts`:`NotImplementedError > should set code to NOT_IMPLEMENTED` | `chat-sdk-chat::errors::tests::not_implemented_error_should_set_code_to_not_implemented` | mapped | |
| `packages/chat/src/errors.test.ts`:`NotImplementedError > should store feature field` | `chat-sdk-chat::errors::tests::not_implemented_error_should_store_feature_field` | mapped | |
| `packages/chat/src/errors.test.ts`:`NotImplementedError > should allow undefined feature` | `chat-sdk-chat::errors::tests::not_implemented_error_should_allow_undefined_feature` | mapped | |
| `packages/chat/src/errors.test.ts`:`NotImplementedError > should be instanceof ChatError` | `chat-sdk-chat::errors::tests::not_implemented_error_should_be_instanceof_chat_error` | mapped | Adapted. |
| `packages/chat/src/errors.test.ts`:`NotImplementedError > should propagate cause` | `chat-sdk-chat::errors::tests::not_implemented_error_should_propagate_cause` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > default level (info) > should not log debug messages` | `chat-sdk-chat::logger::tests::default_level_info_should_not_log_debug_messages` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > default level (info) > should log info messages` | `chat-sdk-chat::logger::tests::default_level_info_should_log_info_messages` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > default level (info) > should log warn messages` | `chat-sdk-chat::logger::tests::default_level_info_should_log_warn_messages` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > default level (info) > should log error messages` | `chat-sdk-chat::logger::tests::default_level_info_should_log_error_messages` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > debug level > should log all levels including debug` | `chat-sdk-chat::logger::tests::debug_level_should_log_all_levels_including_debug` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > warn level > should only log warn and error` | `chat-sdk-chat::logger::tests::warn_level_should_only_log_warn_and_error` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > error level > should only log errors` | `chat-sdk-chat::logger::tests::error_level_should_only_log_errors` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > silent level > should not log anything` | `chat-sdk-chat::logger::tests::silent_level_should_not_log_anything` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > prefix formatting > should use default prefix` | `chat-sdk-chat::logger::tests::prefix_formatting_should_use_default_prefix` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > prefix formatting > should use custom prefix` | `chat-sdk-chat::logger::tests::prefix_formatting_should_use_custom_prefix` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > extra args passthrough > should forward extra arguments` | `chat-sdk-chat::logger::tests::extra_args_passthrough_should_forward_extra_arguments` | mapped | Adapted: Rust has no variadic-args `console.*`. Extras are formatted into the captured line; asserted against the joined string. |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > child logger > should create child with combined prefix` | `chat-sdk-chat::logger::tests::child_logger_should_create_child_with_combined_prefix` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > child logger > should inherit log level` | `chat-sdk-chat::logger::tests::child_logger_should_inherit_log_level` | mapped | |
| `packages/chat/src/chat-singleton.test.ts`:`Chat Singleton > should have no singleton by default` | `chat-sdk-chat::chat_singleton::tests::should_have_no_singleton_by_default` | mapped | |
| `packages/chat/src/chat-singleton.test.ts`:`Chat Singleton > should throw when getting unregistered singleton` | `chat-sdk-chat::chat_singleton::tests::should_throw_when_getting_unregistered_singleton` | mapped | Adapted: upstream `expect().toThrow(msg)` -> Rust `std::panic::catch_unwind` with payload string inspection. |
| `packages/chat/src/chat-singleton.test.ts`:`Chat Singleton > should set and get a singleton` | `chat-sdk-chat::chat_singleton::tests::should_set_and_get_a_singleton` | mapped | Adapted: upstream object identity via `.toBe()` -> Rust `Arc::ptr_eq`. |
| `packages/chat/src/chat-singleton.test.ts`:`Chat Singleton > should clear the singleton` | `chat-sdk-chat::chat_singleton::tests::should_clear_the_singleton` | mapped | |
| `packages/chat/src/chat-singleton.test.ts`:`Chat Singleton > should allow overwriting the singleton` | `chat-sdk-chat::chat_singleton::tests::should_allow_overwriting_the_singleton` | mapped | Adapted: object identity via `Arc::ptr_eq`. |

## Adapter Live Validation Log

Populated as adapters gain credential-gated live tests/examples.

| Adapter | Test/example | Last run | Result | Notes |
| --- | --- | --- | --- | --- |
| _none yet_ | | | | First live validation depends on Phase-2 adapter slices. |
