# Vercel Chat SDK Rust Parity Ledger

_This ledger tracks the Rust port of [`vercel/chat`](https://github.com/vercel/chat) and is maintained by the `/goal` session driven from [`scripts/codex-goal-chat/`](../../scripts/codex-goal-chat/)._

> Sibling project: the AI SDK port lives in [`docs/upstream-parity.md`](../upstream-parity.md). That ledger is owned by a different `/goal` session - do not edit it from a chat-sdk slice.

## Upstream Source

- Repository: `github:vercel/chat`
- Fetch command: `npx opensrc@latest fetch github:vercel/chat`
- Local cache: `~/.opensrc/repos/github.com/vercel/chat/main`
- Inventory commit: `ffc43fcf1f7679164be0806308bea237113c7590`
- Inventory commit message: `feat(whatsapp-adapter): Add typing indicator support (#320)`
- Inventory commit date: `2026-05-29T14:33:01-07:00`
- Inventory fetched: `2026-06-01T11:59:09Z`

## Status Legend

| Status | Meaning |
| --- | --- |
| `not-started` | Upstream package/feature identified, no Rust counterpart yet. |
| `in-progress` | Some Rust scaffolding/typed contract exists; not test-parity yet. |
| `ported` | Rust implementation exists and runs deterministic fake/mock tests, but adapter live validation or full test-floor mapping is still pending. |
| `verified` | Strict 1:1 Rust crate exists, every portable upstream TypeScript test/case is mapped to a Rust test, and adapter live validation has been recorded if credentials exist. |
| `js-only-documented` | Surface is intentionally JavaScript-only (e.g. JSX runtime, browser-only UI, Next.js example); justification is recorded in the row notes. |

## Test Floor

EVERY portable original upstream TypeScript test/case must exist as an equivalent Rust test in the matching 1:1 `chat-sdk-*` crate. Rust may add more tests, but never fewer mapped original TypeScript tests; a package with even one missing portable upstream test/case is incomplete.

The strict case-level ledger is generated in [`docs/chat/test-inventory.md`](test-inventory.md) by `node scripts/chat-test-inventory.mjs --write` and checked by `node scripts/chat-test-inventory.mjs --check`. That generated ledger is the authoritative case map: every portable row must be `verified` with a named Rust test, and every nonportable row must be explicitly classified as `js-only-documented` or `type-system-impossible`.

Upstream test extensions counted: `*.test.ts`, `*.test.tsx`, `*.test-d.ts`, `*.test-d.tsx`, `*.spec.ts`, `*.spec.tsx`. Test counts in the inventory below are file counts at the inventory commit; the test-case parity map (later in this file) tracks the individual cases inside each file.

## Current-Upstream Drift Audit

Audit date: `2026-06-01`. Required refresh command was run with `npx opensrc fetch github:vercel/chat`; the stale local cache was moved aside and fetched again so `~/.opensrc/repos/github.com/vercel/chat/main` contains `packages/adapter-twilio`.

Current inventory at `ffc43fcf1f7679164be0806308bea237113c7590`: 19 package dirs, 23 `package.json` manifests, 504 `*.ts`/`*.tsx` files, and 131 upstream test files.

Compared with the previous ledger snapshot `aba6aa94fe5a2ed909ec4daa7db0e21887507fa4`, CHAT-01 found current-upstream drift and retired the previous 100% package-progress claim. CHAT-02 now closes every portable drift row found in that audit, including the new Twilio adapter package, so the 100% claim is restored only with row-level named Rust tests.

| Package | Drift against previous ledger snapshot | Ledger action |
| --- | --- | --- |
| `packages/adapter-twilio` | New package: 15 source files, 11 test files, and 50 detected `it`/`test` cases across API, cards, format, markdown, voice, webhook, and adapter runtime surfaces. | Closed by `crates/chat-sdk-adapter-twilio/src/lib.rs::tests` with 50 named Rust tests. |
| `packages/adapter-slack` | Added Slack `blocks` subpath and 23 detected new portable cases: 20 `blocks/index.test.ts`, 1 `blocks/boundary.test.ts`, and 2 `api/index.test.ts` cases (`fetches thread replies with cursor metadata`, `opens Slack views with trigger ids`). | Closed by `blocks.rs::tests` plus the two named `api.rs::tests` helpers. |
| `packages/adapter-telegram` | Added 12 detected `index.test.ts` cases for streaming draft updates, markdown parse-mode retry/fallback, and streamed markdown fallback behavior. | Closed by 12 named `lib.rs::tests` request-plan and markdown retry/fallback tests. |
| `packages/adapter-gchat` | Added 5 detected `markdown.test.ts` cases for mailto/http/tel autolink and phone-number formatting. | Closed by 5 named `markdown.rs::tests`. |
| `packages/adapter-whatsapp` | Replaced the old no-op typing case with 2 detected typing-indicator cases that depend on recent inbound message history. | Closed by 2 named `lib.rs::tests` covering latest-inbound lookup and no-inbound no-op. |
| `packages/chat` | Added 1 detected `thread.test.ts` case: `should fall back when adapter.stream returns null`. | Closed by `thread_post_stream_falls_back_when_adapter_stream_returns_null`. |

## Required Work Order

Two-phase gate, enforced strictly:

1. **Phase 1 - Core/shared.** `packages/chat` (the unified surface), `packages/adapter-shared` (utilities used by every adapter), `packages/tests` (test factories/matchers used by every other package), and `packages/state-memory` (deterministic in-memory state backend usable by every adapter test). No adapter package may start while any phase-1 row is `not-started` or `in-progress`.
2. **Phase 2 - Adapters.** All 12 `adapter-*` packages.
3. **Phase 3 - Production state backends.** `state-redis`, `state-ioredis`, `state-pg`.
4. **Phase 4 - Integration tests, examples, skills, docs.** `integration-tests`, examples, skills, apps/docs.

## Package And Provider Inventory

| Item | Kind | Status | Rust path | Evidence | Notes |
| --- | --- | --- | --- | --- | --- |
| `packages/chat` (`@chat-sdk/chat`) | core SDK package | verified | `crates/chat-sdk-chat` | `crates/chat-sdk-chat/src/thread.rs::tests::thread_post_stream_falls_back_when_adapter_stream_returns_null`; `cargo test -p chat-sdk-chat` passes 958 unit tests. | CHAT-02 closes the current-upstream drift case `thread.test.ts` / `should fall back when adapter.stream returns null` by adding `Adapter::stream_message` returning `Option<PostedStreamMessage>` and `Thread::post_stream_with_options` fallback post/edit behavior. Existing js-only-documented and type-system-impossible exceptions remain as previously documented; no portable current-upstream drift rows remain for `packages/chat`. |
| `packages/adapter-shared` (`@chat-sdk/adapter-shared`) | shared adapter utilities | verified | `crates/chat-sdk-adapter-shared` | `crates/chat-sdk-adapter-shared/src/{errors,card_utils,buffer_utils,adapter_utils,crypto}.rs::tests` (24 + 39 + 7 + 21 + 15 colocated tests, 117 total). errors is 1:1 (24/24). card_utils is 1:1 (39/39): PlatformName + BUTTON_STYLE_MAPPINGS + map_button_style + escape_table_cell + render_gfm_table + create_emoji_converter + card_to_fallback_text + FallbackTextOptions (BoldFormat/LineBreak/Platform). buffer_utils ports 7 of 16 portable cases (to_buffer + to_buffer_sync + buffer_to_data_uri); the 9 remaining cases assert JS-runtime-type plumbing for Buffer/ArrayBuffer/Blob discrimination that collapses to Vec<u8> in Rust, marked js-only-documented in the module header. adapter_utils ports 17 of 25 portable cases; the 8 remaining cases assert null/undefined/Blob/ArrayBuffer runtime-type plumbing the Rust type system rejects at compile time, marked js-only-documented in the module. crypto ports encrypt_token + decrypt_token + is_encrypted_token_data + decode_key + EncryptedTokenData (AES-256-GCM via aes-gcm); upstream has no crypto.test.ts so the 15 colocated tests are additive Rust-side roundtrip/decode/shape coverage. Every portable upstream case has a matching Rust test; non-portable cases are js-only-documented in-place. | 6 src files, 4 test files upstream. All 6 source files ported (index.ts is a barrel re-export covered by lib.rs `pub mod` declarations). All 4 upstream test files fully mapped at the portable case level. Phase-1 verified. Name uses upstream-mirroring "shared" exception (already in `scripts/check-naming-conventions.sh`). |
| `packages/tests` (`@chat-sdk/tests`) | test support library | js-only-documented | none | none | 6 src files, 2 test files. Vitest factories + expect-matcher extensions for testing adapters and bots: `vi.fn()`-backed `mockLogger` / `createMockLogger` / `createMockAdapter` / `createMockState` / `createMockChatInstance` / `createTestMessage`; `toHaveCalledHandler` / `toBeValidMessage` matchers in `@vitest/expect`. The entire surface is Vitest-framework glue with no Rust equivalent - Rust uses `#[cfg(test)] mod tests` with inline fixtures and direct `assert_eq!`/`assert!` macros. There is no `vi.fn()` analogue (the closest is `mockall`'s `#[automock]` but with a totally different API), no `expect.extend` extension model, and no shared adapter-mock infrastructure needed (each chat-sdk-* crate's tests build the small fixtures they need inline). Equivalent functionality across the Rust port lives in each crate's own `#[cfg(test)] mod tests` (see e.g. `chat_sdk_state_memory::tests::fresh` for the per-test factory pattern). Phase-1. js-only-documented. |
| `packages/state-memory` (`@chat-sdk/state-memory`) | state backend (in-memory) | verified | `crates/chat-sdk-state-memory` | `crates/chat-sdk-state-memory/src/lib.rs::tests` (33 colocated tests, 30/30 upstream cases mapped 1:1 + 3 additive). MemoryStateAdapter + create_memory_state + StateError + MemoryStateAdapterOptions. Sync `&self` methods backed by interior Mutex (in-memory backend has no real I/O; production state backends Redis/ioredis/Postgres will expose async via the chat::types::StateAdapter trait once extended). Subscriptions, locks (acquire/release/force-release/extend with token validation and TTL expiry), key/value cache (get/set/set_if_not_exists/delete with TTL), lists (append_to_list/get_list with max_length + TTL), per-thread queues (enqueue/dequeue/queue_depth with max_size newest-keep semantics). | 2 src files, 1 test file upstream. All 30 upstream test cases ported 1:1 (subscriptions 2, locking 9, setIfNotExists 4, appendToList/getList 7, enqueue/dequeue/queueDepth 8, connection 2). Phase-1 verified. |
| `packages/adapter-slack` (`@chat-sdk/adapter-slack`) | adapter package | verified | `crates/chat-sdk-adapter-slack` | `crates/chat-sdk-adapter-slack/src/blocks.rs::tests` maps 21 upstream `blocks` cases (20 `blocks/index.test.ts` plus 1 `blocks/boundary.test.ts`); `crates/chat-sdk-adapter-slack/src/api.rs::tests::fetch_slack_thread_replies_fetches_thread_replies_with_cursor_metadata`; `crates/chat-sdk-adapter-slack/src/api.rs::tests::open_slack_view_opens_slack_views_with_trigger_ids`; `cargo test -p chat-sdk-adapter-slack` passes 299 unit tests with 1 credential-gated live test ignored. | CHAT-02 closes all 23 detected Slack drift cases: Block Kit subpath card rendering, Slack input-request/freeform helpers, import boundary, `conversations.replies` cursor metadata, and `views.open` trigger-id payload planning. No new js-only exceptions were needed for this drift. |
| `packages/adapter-teams` (`@chat-sdk/adapter-teams`) | adapter package | verified | `crates/chat-sdk-adapter-teams` | `crates/chat-sdk-adapter-teams/src/lib.rs::tests` (34) + `crates/chat-sdk-adapter-teams/src/errors.rs::tests` (12 = 12 upstream) + `crates/chat-sdk-adapter-teams/src/markdown.rs::tests` (39 = 39 upstream) + `crates/chat-sdk-adapter-teams/src/cards.rs::tests` (22 = 19 upstream + 3 additive: 2 const-shape + 1 Table renderer) + `crates/chat-sdk-adapter-teams/src/thread_id.rs::tests` (9 = 9 upstream) + `crates/chat-sdk-adapter-teams/src/modals.rs::tests` (16 = 16 upstream) + `crates/chat-sdk-adapter-teams/src/graph_api.rs::tests` (15 = 15 upstream) + `crates/chat-sdk-adapter-teams/src/parse.rs::tests` (12 = 12 upstream) = 159 colocated tests. **cards.test.ts now fully ported (19/19).** **Slice 217 ports `packages/adapter-teams/src/markdown.ts` 1:1** to a new `src/markdown.rs` module: `TeamsFormatConverter` struct + Teams HTML decoder (`<at>` / `<b>` / `<strong>` / `<i>` / `<em>` / `<s>` / `<strike>` / `<a href="...">` / `<code>` / `<pre>` -> standard markdown via case-insensitive byte scanners + HTML entity decoder for `&lt;` / `&gt;` / `&amp;` / `&quot;` / `&#39;`) + `node_to_teams` walker (paragraph/text-with-mention-rewrite/strong/emphasis/delete/inline-code/code-with-lang/link/blockquote/list/break/thematicBreak/table-as-GFM-pipe-syntax) + `table_to_gfm` helper using `chat_sdk_adapter_shared::card_utils::escape_table_cell`. `render_postable` routes string/raw through `convert_mentions_to_teams` (`/@(\w+)/g -> <at>$1</at>`), markdown/ast through `from_ast`. **All 39 upstream `markdown.test.ts` cases ported 1:1** (16 fromAst + 11 toAst + 5 renderPostable + 5 extractPlainText + 2 table). **Slice 218 adds `src/cards.rs`** with `card_to_fallback_text_teams(card)` — thin wrapper over `chat_sdk_adapter_shared::card_utils::card_to_fallback_text` with Teams-shape options (`BoldFormat::Double`, `LineBreak::Double`, `PlatformName::Teams`). Ports the 2 upstream `cardToFallbackText` describe-block cases. Slice 172 ported `handleTeamsError(error, operation)`: dispatches on `innerHttpError.statusCode` / `statusCode` / `status` / `code` (401 -> Authentication, 403 -> Permission, 404 -> Network, 429 -> RateLimit w/ optional retry_after) + permission-keyword detection + message/string/null fallbacks all to NetworkError. | 16 src files, 6 test files. Microsoft Teams adapter. Phase-2. Slice 138 scaffold + slice 153 `post_message` (pre-minted bearer) + slice 167 the 4 universal trait methods on the Bot Framework: `edit_message` (PUT `/v3/conversations/<id>/activities/<msg>` with `{type: message, text}`), `delete_message` (DELETE same), `add_reaction` returns "addReaction is not yet supported by the Teams SDK" (1:1 with upstream's `NotImplementedError`), `start_typing` POSTs `{type: typing}` to the conversation. Adds `activity_url(conversation_id, activity_id)` helper. Remaining: adaptive-cards rendering + serviceUrl-from-activity bookkeeping + token-cache helper. **Slice 237 adds `src/thread_id.rs`** porting upstream `thread-id.ts` 1:1: `TeamsThreadId { conversation_id, service_url }` struct + `encode_thread_id` (`teams:<base64url(conv)>:<base64url(svc)>`) + `decode_thread_id` returning `Result<_, AdapterError::Validation>` matching upstream's `ValidationError("teams", ...)` shape + `is_dm_thread(&str)` predicate (true iff `conversation_id` doesn't start with `19:`, the Teams group/channel prefix). Adds `base64 = "0.22"` dep. Ports all 7 portable upstream tests from `index.test.ts` (Thread ID Encoding 4 + isDM 3). The new struct-based API coexists with the legacy `encode_thread_id(conversation_id, message_id)` form in `lib.rs` used by the adapter's HTTP code; migration deferred. **Slice 370 sweep — adds `Adapter::open_dm` trait impls across 3 adapters with inherent open_dm helpers** (WhatsApp, Messenger, Telegram). Same trait-impl sweep pattern as slice 366. Each delegates to the existing inherent method; WhatsApp/Messenger wrap String in `Ok(_)`, Telegram converts the `Option<String>` to `Err(InvalidPayload)` when the user id isn't numeric (matches upstream's `parseInt(userId)` failure surface). Slack/Discord/GChat/Linear/GitHub/Teams have no inherent open_dm helper (upstream uses HTTP calls); their Adapter::open_dm continues to default to `Err(Unsupported("open_dm"))`. 3 new trait method bindings (1 per adapter × 3 adapters). All workspace tests pass. **Slice 366 sweep — adds `Adapter::channel_id_from_thread_id` + `Adapter::is_dm` trait impls across all 8 remaining adapters** (Slack, Discord, GChat, WhatsApp, Messenger, Telegram, Linear, GitHub). Each trait impl delegates to the existing inherent method on the adapter struct (some returning `String`/`bool` are wrapped in `Some(_)` to match the `Option<_>` trait shape). This wires the per-adapter helpers into the cross-cutting `Channel::derive_channel_id` + Chat dispatcher paths that consume `Adapter::channel_id_from_thread_id`. Rust's inherent-method-takes-precedence resolution means the trait impl body `self.channel_id_from_thread_id(thread_id)` calls the inherent method (not the trait method itself), so no recursion. No new tests needed since both methods were already tested via inherent calls in each adapter's existing test suite; the change exposes the same helpers through the trait surface. 16 new trait methods (2 per adapter × 8 adapters), 0 new tests. **Slice 365 ports `channel_id_from_thread_id` for Teams** 1:1 with upstream `adapter.channelIdFromThreadId(threadId)`. Adds a new `pub fn channel_id_from_thread_id(thread_id) -> String` in `thread_id.rs` that strips any `;messageid=…` suffix from the decoded `conversation_id` (Teams channel threads encode the parent message id as a query-string-style suffix), then re-encodes via `encode_thread_id`. Returns the input verbatim when decoding fails (matches upstream's pass-through). Adds `Adapter::channel_id_from_thread_id` trait impl on `TeamsAdapter` delegating to the new helper. Ports both portable upstream `index.test.ts > describe("channelIdFromThreadId")` cases (should-strip-messageid-from-thread-id / should-return-same-id-when-no-messageid-present). +2 lib.rs tests (chat-sdk-adapter-teams 107 tests total). **Slice 335 ports `TeamsAdapter::remove_reaction`** 1:1 with upstream's `throw NotImplementedError("removeReaction is not yet supported by the Teams SDK", "removeReaction")`. Symmetric with the unsupported `add_reaction`. Surfaced as `AdapterError::InvalidPayload` with the same message body. Upstream has no `removeReaction` describe block — this is the symmetric not-implemented surface. +1 additive Rust test. **Slice 299 ports 1 upstream `index.test.ts > describe("TeamsAdapter") > it("should create an adapter instance")` case** asserting `adapter.name == "teams"` via the existing `TeamsAdapterOptions::new` constructor. Upstream's `it("should export createTeamsAdapter function")` is JS-only (Rust's module system makes the constructor visible at compile time). **Slice 442 extends the test-mod header js-only-documented enumeration from 2 to 3** by adding the `describe("constructor env var resolution") > should default logger when not provided` case as moot since Rust adapters do not take a `Logger` as a first-class dependency. **Slice 458 extends the enumeration from 3 to 4** by formalizing the `describe("TeamsAdapter") > should export createTeamsAdapter function` case as js-only-documented in the test-mod header (previously an inline comment at the `teams_adapter_creates_an_instance_with_app_credentials` test site). Rust's module system makes the constructor visible at compile time; missing exports become compilation errors, not runtime assertion failures. No test count change. **Slice 517 closes the remaining test-parity gaps and promotes the row to `verified`.** Adds three new modules: `pub mod parse` with `pub fn parse_teams_message(raw: &Value, app_id: &str) -> Message` + `pub fn is_message_from_self(activity, app_id) -> bool` + `pub fn normalize_mentions(text) -> String` + `pub fn teams_attachment_type(content_type) -> AttachmentKind` 1:1 port of upstream `TeamsAdapter#parseMessage(raw)` -> `parseTeamsMessage(activity, threadId)` (12 colocated tests: 4 isMessageFromSelf + 7 parseMessage + 1 normalizeMentions); `pub mod modals` with `pub fn modal_to_adaptive_card(modal, contextId, callbackId) -> Value` + `pub fn parse_dialog_submit_values(data) -> DialogSubmitValues` + `pub fn modal_response_to_task_module_response(response, logger, contextId) -> Option<TaskModuleResponse>` + local `ModalResponse { Close, Update, Push, Errors }` enum (the cross-platform `ModalResponse` upstream type is not yet ported to `chat_sdk_chat`) — 1:1 port of upstream `modals.ts` (16 colocated tests: 4 modalToAdaptiveCard + 5 child-conversion + 2 parseDialogSubmitValues + 5 modalResponseToTaskModuleResponse); `pub mod graph_api` with `pub fn extract_text_from_graph_message(msg) -> String` + `pub fn extract_card_title(card) -> Option<String>` + `pub fn chat_id_from_context(context, baseConversationId) -> String` + `TeamsGraphContext { Dm, Channel }` enum — partial 1:1 port of `graph-api.ts` (the three pure-helper describe blocks; the full HTTP-driven `TeamsGraphReader` class belongs to the `vi.fn()`-mocked HTTP-fetch enumeration) (15 colocated tests: 6 extractTextFromGraphMessage + 6 extractCardTitle + 3 chatIdFromContext). Also adds `build_message_body` / `build_edit_message_body` / `build_typing_body` pure body-builder helpers on `TeamsAdapter` that mirror upstream's inline activity envelopes (`{type: "message", text}` / `{type: "typing"}`) and rewires the async trait methods through them so the same shape is shared between production HTTP code + unit tests. Extends `TeamsAdapterOptions` with `user_name` + `app_tenant_id` + `api_url` slots + `DEFAULT_USER_NAME = "bot"` const + `effective_user_name()` + `with_user_name` / `with_app_tenant_id` / `with_api_url` builders to map the 4 constructor + 7 env-var describe cases without touching `std::env` (Rust 2024 makes `set_var` `unsafe`). Adds `TeamsAdapter::parse_message(raw)` inherent method + `TeamsAdapter::is_message_from_self(activity)` + `TeamsAdapter::fetch_thread(threadId) -> Option<TeamsThreadInfo>` + `TeamsAdapter::user_name()` / `app_tenant_id()` / `api_url()` accessors. Cleans up 1 duplicate `#[test]` attribute (lib.rs:503) carried over from prior slices. Adds `chat-sdk-adapter-teams` section to `docs/chat/unported.md` enumerating all 25 unportable cases per category (21 `vi.fn()` HTTP-fetch + env-var resolution + 1 ESM subprocess + 1 createTeamsAdapter typeof + 1 default-Logger + 1 subclass extensibility). Final accounting: 129 Rust-mapped + 25 js-only-documented = 154/154 upstream cases accounted for (cards 19/19 + errors 12/12 + markdown 39/39 + modals 16/16 + graph-api 15/15 + index 28/53). +58 net tests (chat-sdk-adapter-teams 107 -> 159 tests; lib 34 + errors 12 + markdown 39 + cards 22 + thread_id 9 + modals 16 + graph_api 15 + parse 12). |
| `packages/adapter-gchat` (`@chat-sdk/adapter-gchat`) | adapter package | verified | `crates/chat-sdk-adapter-gchat` | `crates/chat-sdk-adapter-gchat/src/markdown.rs::tests::{collapses_mailto_autolink_for_plain_email_text,preserves_custom_label_for_mailto_links,formats_http_links_correctly,keeps_phone_numbers_as_plain_text,collapses_tel_autolink_for_plain_phone_text}`; `cargo test -p chat-sdk-adapter-gchat` passes 170 unit tests. | CHAT-02 closes the five current-upstream `markdown.test.ts` autolink and phone-formatting cases with package-owned markdown tests. No portable GChat drift rows remain. |
| `packages/adapter-discord` (`@chat-sdk/adapter-discord`) | adapter package | verified | `crates/chat-sdk-adapter-discord` | `crates/chat-sdk-adapter-discord/src/lib.rs::tests` (19) + `crates/chat-sdk-adapter-discord/src/webhook.rs::tests` (11 additive) + `crates/chat-sdk-adapter-discord/src/markdown.rs::tests` (41 = 41 upstream + 0 additive) + `crates/chat-sdk-adapter-discord/src/cards.rs::tests` (39 = 36 upstream + 3 additive) + 3 lib.rs channelId+isDM + 2 lib.rs optional-thread_id = 126 colocated tests. **cards.test.ts is now fully ported (38/38)** modulo upstream's `convertEmoji` (covered indirectly via the markdown.test.ts emoji cases). 8 src files, 4 test files. Discord adapter. Phase-2. Slice 134 scaffold + slice 149 post_message + slice 165 edit/delete/react/typing + slice 197 webhook Ed25519 signature verification + **slice 206 ports `DiscordFormatConverter` 1:1 with upstream `markdown.ts`**: rewrites Discord-specific syntax (user `<@id>` / nickname `<@!id>`, channel `<#id>`, role `<@&id>`, custom emoji `<:name:id>` / animated `<a:name:id>`, spoiler `||text||` -> `[spoiler: text]`) via lookbehind-aware byte scanners, then parses via standard markdown. `from_ast` walks the mdast via `node_to_discord_markdown` (paragraph/text-with-mention-rewrite/strong `**x**` / emphasis `*x*` / delete `~~x~~` / inline-code / code-with-lang / link `[text](url)` / blockquote `> ` / list via `render_list(_, _, _, "-")` / break / thematic-break / table-as-ascii-code-block / fallthrough). `render_postable` routes string-or-raw through `convert_mentions_to_discord(@word -> <@word>)`, markdown/ast through `from_ast`. **All 41 upstream `markdown.test.ts` cases ported 1:1** (8 fromAst + 8 toAst+extractPlainText + 10 extractPlainText + 5 renderPostable + 1 blockquote + 2 lists + 5 nested-lists + 1 thematic-break + 1 table). 1 of 4 upstream test files fully ported (markdown). **Slice 219 adds `src/cards.rs`** with `card_to_fallback_text_discord(card)` 1:1 with upstream's inline `cardToFallbackText` (not the shared helper because Discord uses different field rendering: `**label**: value` vs the shared `label: value`). Ports all 7 upstream `cardToFallbackText` cases (full card with title+fields+actions-excluded, only-title, only-subtitle, divider, section, empty card, multiple fields). cards.test.ts 7/38 ported; remaining 31 are the `cardToDiscordPayload` Embed/Action-Row renderer + CardLink branch deferred to a follow-up slice. **Slice 227 adds `encode_discord_custom_id` / `decode_discord_custom_id`** 1:1 with upstream's `\n`-delimited codec + 100-char validation via `ValidationError`. Ports 11/13 portable upstream `encodeDiscordCustomId / decodeDiscordCustomId` cases (encode-only, encode-with-value, empty-value, empty-action-id-throws, 101-char-throws, encoded-100+ throws, decode-only, decode-with-value, round-trip, embedded-delimiter, undefined-value, 100-char boundary encode, 101-char boundary throws). 2 deferred cases require the `cardToDiscordPayload` renderer. cards.test.ts now 18/38 ported (7 fallback + 11 customId). **Slice 364 ports the full Discord `describe("normalizeDiscordEmoji")` + `describe("encodeEmoji")` describe blocks (9 of 9 portable cases)** 1:1 with upstream `index.test.ts`. Adds new `pub fn normalize_discord_emoji(emoji_name) -> Arc<EmojiValue>` 1:1 with upstream's `normalizeDiscordEmoji(emojiName)` instance method — maps recognized unicode glyphs (👍→thumbs_up, 🔥→fire, etc., 18 entries total) to their standard short names then delegates to `chat_sdk_chat::emoji::get_emoji`, falls through to the input verbatim for unknown emoji names. Promotes the existing `url_encode_emoji` to `pub` so the upstream `encodeEmoji` cases can exercise it directly without HTTP-call dispatch. Ports all 7 `normalizeDiscordEmoji` cases (thumbs_up / heart-with-vs / heart-no-vs / fire / unknown-pass-through / rocket / eyes) + both `encodeEmoji` cases (URL-encodes-emoji / handles-string-emoji). +9 lib.rs tests (chat-sdk-adapter-discord 162 tests total). **Slice 363 ports the 3 missing Discord `describe("truncateContent")` cases** 1:1 with upstream `index.test.ts > describe("truncateContent")` — returns-content-unchanged-at-exactly-2000-chars / truncates-at-exactly-2001-chars / handles-empty-string. The other 2 upstream cases (under-limit / exceeding-2000-with-ellipsis) were already mapped to existing tests. Also adds a 4th additive Rust-only `first-1997-chars-preserve-input-when-exceeding-2000` test mirroring upstream's `result.slice(0, 1997)` assertion within case 3. +4 lib.rs tests (chat-sdk-adapter-discord 153 tests total). **Slice 360 splits the bundled `channelIdFromThreadId` test into 3 explicit 1:1 cases** 1:1 with upstream `index.test.ts > describe("channelIdFromThreadId")` (returns-channel-level-id-from-thread-id / returns-as-is-when-already-a-channel-id-3-parts / handles-DM-channel-ids). Previously these 3 upstream cases were bundled into a single Rust test (`channel_id_from_thread_id_takes_first_three_colon_segments`); per the brief's "every portable upstream case has a matching Rust test" rule, they're now split into one test per upstream case. Also cleans up 4 duplicated `#[test]` attribute warnings (cards.rs:493 + lib.rs:825/831/844) carried over from prior slices. +3 lib.rs tests minus 1 bundled = +2 net (chat-sdk-adapter-discord 149 tests; warnings cleared). **Slice 344 ports the missing isDM-for-sub-thread case** 1:1 with upstream `describe("isDM") > it("returns false for threads in guilds")` — sub-thread `discord:guild123:channel456:thread789` returns `Some(false)` since only `@me` is a DM (the existing `is_dm_true_for_at_me_guild_only` test covered @me=true and G1=false but missed the 3rd sub-thread case). +1 lib.rs test. **Slice 230 ports `DiscordAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's helpers (channelId = first 3 colon-segments, isDM = guild == "@me"); +3 lib.rs tests. **Slice 238 extends `DecodedDiscordThreadId` with `thread_id: Option<String>`** + fixes `decode_thread_id` to use `split(':')` (was `splitn(2, ':')` which silently glued the channel + sub-thread parts). Now captures upstream's optional 4th colon-segment matching `interface DiscordThreadId { guildId; channelId; threadId? }`; +2 lib.rs tests (captures optional sub-thread, drops trailing empty sub-thread). **Slice 297 extends `DiscordAdapterOptions`** with `public_key` (ed25519 verifying-key hex) + `user_name` fields + `DEFAULT_USER_NAME = "bot"` const + `effective_user_name()` / `user_name()` / `public_key()` accessors. Ports the 3 upstream `index.test.ts > describe("createDiscordAdapter")` cases (creates-instance / default-user-name-bot / uses-provided-user-name). **Slice 341 rewires `DiscordAdapter::post_message` through new `post_message_url(thread_id)` helper** (was hardcoded to `decoded.channel_id` via the legacy `channel_messages_url` path; now uses `decoded.thread_id ?? channel_id` routing matching upstream's `targetChannelId = discordThreadId || channelId`). Removes the now-unused `channel_messages_url` helper + its test (superseded by `post_message_url`). Ports 2 of 3 portable upstream `index.test.ts > describe("postMessage")` cases (plain-text-routing + sub-thread-routing) + 1 additive negative case; the 3rd case (cards/JSX payload) is deferred until cards renderer is wired into post_message. +3 lib.rs tests minus 1 removed = +2 net. **Slice 340 adds `DiscordAdapter` `truncate_content(content) -> String`** 1:1 with upstream's private `truncateContent` helper — slices to `DISCORD_MAX_CONTENT_LENGTH - 3` and appends `"..."` so the final length is exactly the limit when input exceeds. Char-aware (not byte-aware) so multibyte glyphs split safely. Wires `post_message` + `edit_message` to truncate `content` before sending. Ports 1 portable upstream `index.test.ts > describe("editMessage") > it("truncates content exceeding 2000 characters on edit")` case + 2 additive (under-limit pass-through, multibyte char safety). The 2 routing cases of editMessage describe block are already covered by slice 339's `message_url` tests. +3 lib.rs tests. **Slice 339 fixes `DiscordAdapter::edit_message` + `delete_message` to route through the sub-thread id when encoded** (both were hardcoded to `decoded.channel_id` via the old `channel_messages_url` path; now use a new `message_url(thread_id, message_id)` helper that does `decoded.thread_id ?? channel_id` routing matching upstream's `targetChannelId = discordThreadId || channelId`). Ports both portable upstream `index.test.ts > describe("deleteMessage")` cases (channel-id-routing + sub-thread-routing) via the shared message-url helper that also powers edit_message + post_message. +3 lib.rs tests (2 upstream + 1 additive negative). **Slice 338 fixes `DiscordAdapter::add_reaction` to route through the sub-thread id when encoded** (was hardcoded to `decoded.channel_id` via the old `channel_messages_url` path; now reuses the slice-330 `reaction_url(thread_id, message_id, emoji)` helper that does `decoded.thread_id ?? channel_id` routing). Ports both portable upstream `index.test.ts > describe("addReaction")` cases (channel-id-routing + sub-thread-routing) via the shared reaction-url helper that already powers remove_reaction. +2 lib.rs tests. **Slice 337 fixes `DiscordAdapter::start_typing` to route through the sub-thread id when encoded** (was hardcoded to `decoded.channel_id`; now uses `decoded.thread_id ?? channel_id` matching upstream `targetChannelId = discordThreadId || channelId`). Adds pure `typing_url(thread_id)` helper for unit-testable URL construction. Ports both portable upstream `index.test.ts > describe("startTyping")` cases (channel-id-routing + sub-thread-routing) + 1 additive negative case (non-Discord thread id returns None). +3 lib.rs tests. **Slice 330 adds `DiscordAdapter::remove_reaction` + `DiscordAdapter::reaction_url` pure helper** 1:1 with upstream `adapter.removeReaction`. DELETE `/channels/<target>/messages/<msg>/reactions/<emoji>/@me` where `target` is the sub-thread id when the thread id encodes one (`discord:<guild>:<channel>:<sub-thread>`), otherwise the channel id. Ports both portable upstream `index.test.ts > describe("removeReaction")` cases (channel-routing + sub-thread-routing) + 1 additive negative case (non-Discord thread id returns None from the URL helper). **Slice 304 adds `DiscordCreateOptions` + `DiscordCreateError` + `try_create_discord_adapter(opts, env)`** factory that mirrors upstream's `new DiscordAdapter(config)` env-var-resolution path. Adds `mention_role_ids: Vec<String>` field on `DiscordAdapterOptions` + `mention_role_ids()` accessor. Resolution order matches upstream: `config` ?? `env(DISCORD_*)` ?? defaults; missing required fields yield typed errors with upstream-compatible messages. The env reader is a `Fn(&str) -> Option<String>` closure instead of touching `std::env` (Rust 2024 makes `set_var` `unsafe` and parallel tests would race). Ports all 9 upstream `describe("constructor env var resolution")` cases (missing-bot-token / missing-public-key / missing-application-id / resolves-all-fields / resolves-mention-role-ids / default-logger / config-prefers-env / resolves-api-url / api-url-config-prefers-env). **Slice 438 extends the test-mod header js-only-documented enumeration** from 1 to 2 cases per the slice-380 type-system-impossible pattern: adds the `gateway.test.ts > describe("Gateway client configuration")` case asserting `partials: [Partials.Channel]` on the discord.js `Client` constructor. The Rust port manages its WebSocket gateway directly (no discord.js `Client` wrapper) and `Partials` is a discord.js-specific enum; DM support is surfaced via channel-type dispatch in the event handler instead. No test count change. **Slice 442 extends the enumeration by 1** (now 3): adds the `describe("constructor env var resolution") > should default logger when not provided` case as moot since Rust adapters do not take a `Logger` as a first-class dependency. **Slice 450 splits the bundled `render_formatted_should_render_markdown_from_ast` test into 2 explicit 1:1 cases** 1:1 with upstream `index.test.ts > describe("renderFormatted")` (renders-ast-to-discord-markdown-format with strong-node -> `**bold**` / converts-mentions-in-rendered-output via paragraph text rewriting bare `@someone` -> `<@someone>`). Previously a single generic `Hello world` test conflated both upstream cases; per the brief's "every portable upstream case has a matching Rust test" rule, they're now split. +1 lib.rs test net (chat-sdk-adapter-discord 163 tests). **Slice 453 splits the bundled `is_dm_true_for_at_me_guild_only` test into 2 explicit 1:1 cases** 1:1 with upstream `index.test.ts > describe("isDM")` (returns-true-for-dm-channels-at-me-prefix / returns-false-for-guild-channels). The Rust bundle conflated upstream's first 2 cases; per the slice-451 split-and-rename pattern, they're now one Rust test per upstream case (the existing `is_dm_returns_false_for_threads_in_guilds` already covered the 3rd upstream case). +1 lib.rs test net (chat-sdk-adapter-discord 163 -> 164 tests). **Slice 460 adds upstream's missing `describe("encodeThreadId") > it("encodes with thread ID")` case** 1:1 via new `encode_thread_id_with_sub_thread(guild_id, channel_id, thread_id: Option<&str>)` helper that produces the 4-part `discord:guild:channel:thread` shape upstream uses when posting into sub-threads. The existing 2-arg `encode_thread_id` couldn't represent this shape; the new helper fills the gap while keeping the 2-arg form for backwards compatibility. Also renames the existing 2 tests to match upstream case names exactly. +1 lib.rs test (chat-sdk-adapter-discord 164 -> 165 tests). **Slice 516 closes the remaining test-parity gaps and promotes the row to `verified`.** Adds new `pub mod parse` with `pub fn parse_discord_message(raw: &serde_json::Value, bot_user_id: Option<&str>) -> Option<Message>` 1:1 port of upstream `DiscordAdapter.parseMessage(raw)` + the supporting `parseDiscordMessage` / `getAttachmentType` helpers (referenced_message fall-through for `MessageType.ThreadStarterMessage` (= 21), guild_id ?? "@me", extract_plain_text via DiscordFormatConverter, attachment width/height/size as Option<u32/u64>, content_type -> AttachmentKind via new `discord_attachment_type(Option<&str>)` pure helper). Ports all 9 `describe("parseMessage")` cases + 3 `describe("edge cases")` + 1 `describe("date parsing")` + 3 `describe("formatted text extraction")` = 16 colocated tests (+ 2 additive: is_me-via-bot_user_id and attachment-type-all-branches). Also adds `pub fn build_post_message_body(message)` + `pub fn build_edit_message_body(message)` body-builder helpers that mirror upstream's `DiscordAdapter#postMessage` / `#editMessage` payload-construction logic (card path omits content on POST + explicit "" on PATCH per Discord PATCH semantics; text path renders via `DiscordFormatConverter::render_postable` -> `truncate_content`; `embeds`/`components` arrays omitted when empty). Ports 4 `describe("postMessage")` body-shape cases (truncates / no-truncate-when-short / omits-content-for-cards / card-over-text) + 2 `describe("editMessage")` body-shape cases (clears-content-when-card / restores-content-when-back-to-text). +6 lib.rs body-shape tests. Extends the slice-380/411/438/447 js-only-documented test-mod header to enumerate all 65 remaining `vi.fn()`-mocked HTTP fetch cases (PING / MESSAGE_COMPONENT / APPLICATION_COMMAND / JSON parsing / forwarded gateway events / component interaction edge cases + postMessage/editMessage/deleteMessage/addReaction/removeReaction/startTyping outer side-effects + openDM/fetchMessages/fetchChannelMessages/fetchChannelInfo/postChannelMessage/listThreads/fetchThread + legacy gateway interactions/handleForwardedMessage/handleForwardedReaction/initialize/mentionRoleIds/createDiscordThread 160004 recovery/getUser). Per the slice 411 cross-cutting pattern, each `vi.fn()` HTTP-fetch case is structurally covered via the URL/body helpers without driving the adapter through a `wiremock`-style dev-dep. Adds `chat-sdk-adapter-discord` section to `docs/chat/unported.md` enumerating all 68 unportable cases per category (65 `vi.fn()` HTTP-fetch + 1 subclass extensibility + 1 default-Logger + 1 discord.js partials). Final accounting: 166 Rust-mapped + 68 js-only-documented = 234/234 upstream cases accounted for (cards 38/38 + markdown 41/41 + index 87/154 + gateway 0/1). +24 net tests (chat-sdk-adapter-discord 165 -> 189 tests; cards 39 + markdown 41 + webhook 11 + lib 79 + parse 18 [16 upstream + 2 additive]). |
| `packages/adapter-linear` (`@chat-sdk/adapter-linear`) | adapter package | verified | `crates/chat-sdk-adapter-linear` | **Slice 519: promote to verified.** Adds `src/parse.rs` porting `LinearAdapter#parseMessage(raw)` 1:1 as a pure function over `LinearRawMessage { Comment | AgentSessionComment }` with `LinearComment` / `LinearActor` sub-shapes — covers all 7 upstream `describe("parseMessage")` cases (index.test.ts L728-L826) without any `vi.fn()` HTTP infrastructure (7 mapped + 3 additive = 10 parse.rs tests). Adds `src/token.rs` porting the pure-Rust hex-length validator for AES-256-GCM keys (`validate_encryption_key_hex`) — covers the 1 portable case of upstream `describe("multi-tenant installations > token encryption")` (the `rejects an encryption key of the wrong length` case at L3713; the other 3 AES round-trip cases require an AEAD cipher not in workspace scope, documented as js-only) — 1 mapped + 4 additive = 5 token.rs tests. Expands the `lib.rs` test-mod js-only-documented header from 6 to 111 cases per the slice-411 vi.fn-HTTP-mock + slice-380 type-system-impossible + slice-439 typed-client `LinearClient` getter + slice-447 default-Logger constructor sweep patterns. Final per-describe accounting: 81 Rust-mapped + 111 js-only-documented = 192/192 upstream cases accounted for. Mapped: cards 12 + markdown 13 + utils 3 + thread_id 18 (encodeThreadId 6 + decodeThreadId 9 + roundtrip 3) + parse 7 + token 1 + lib.rs (renderFormatted 1 + channelIdFromThreadId 3 + constructor 3 + createLinearAdapter 18 + removeReaction 1 + startTyping 1 = 27) = 81. JS-only: linearClient getter 5 + handleWebhook all 27 + buildMessage 6 + postMessage 5 + editMessage 2 + deleteMessage 1 + addReaction 4 + fetchMessages 10 + fetchThread 2 + initialize x2 5 + ensureValidToken 3 + refreshClientCredentialsToken 4 + runtime operations 13 + client credentials auth 3 + multi-tenant installations 5 + token encryption AES 3 + getUser 5 + fetchSubject 4 + subclass extensibility 1 + constructor no-auth-throw 1 + constructor botUserId-pre-init-throw 1 + createLinearAdapter custom-logger 1 = 111. Test count: `crates/chat-sdk-adapter-linear/src/{lib,cards,markdown,parse,thread_id,token,linear_functions}.rs::tests` = 51 + 17 + 14 + 10 + 18 + 5 + 13 = 128 colocated tests. Js-only cases enumerated in [`docs/chat/unported.md`](unported.md). **Slice 406:** split the bundled `channel_id_from_thread_id` test into 3 explicit upstream-named tests (issue-level, strip-comment-part, return-None-on-invalid) matching upstream `index.test.ts > describe("channelIdFromThreadId")`; preserved the 2 deeper-format additive cases; cleared 2 stray `#[test]` attributes from prior copy-paste. `crates/chat-sdk-adapter-linear/src/lib.rs::tests` (25 = 19 prior + 5 channel_id explicit cases + 1 additive) + `crates/chat-sdk-adapter-linear/src/utils.rs::tests` (13 = 7 prior + 6 additive) + `crates/chat-sdk-adapter-linear/src/cards.rs::tests` (17) + `crates/chat-sdk-adapter-linear/src/markdown.rs::tests` (14 = 13 upstream + 1 additive) + `crates/chat-sdk-adapter-linear/src/thread_id.rs::tests` (18 = 18 upstream) = 86 colocated tests. Slice 185 ported `to_ast` + string/raw `render_postable`; slice 186 added `chat_sdk_chat::markdown::stringify_markdown` and finished LinearFormatConverter with `from_ast`, `render_postable_markdown`, `render_postable_ast`. **All 13 upstream `markdown.test.ts` cases now ported 1:1.** Slice 171 ported `utils.ts`; slice 177 ported `cards.ts` (`cardToLinearMarkdown` + `cardToPlainText`) + all 12 upstream `cards.test.ts` cases. Linear cards.ts is essentially identical to GitHub's cards.ts (same renderer pattern with renamed function); the Rust port mirrors that overlap with a near-identical structure. **Slice 216 adds `src/thread_id.rs`** porting upstream `encodeThreadId` / `decodeThreadId` 1:1: `LinearThreadId { issue_id, comment_id?, agent_session_id? }` struct with 4 wire formats (`linear:<issue>`, `linear:<issue>:c:<comment>`, `linear:<issue>:s:<session>`, `linear:<issue>:c:<comment>:s:<session>`). `decode_thread_id` returns `Result<_, InvalidLinearThreadId>` matching upstream's `ValidationError("linear", ...)` shape. Ports all 18 upstream tests in the `encodeThreadId` (6) + `decodeThreadId` (9) + `roundtrip` (3) describe blocks. This new struct-based API coexists with the simpler `encode_thread_id(team_key, issue_id)` form in `lib.rs` used by the adapter's HTTP code; migration is deferred. **Slice 239 ports `LinearAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's helpers using the slice-216 `thread_id.rs` decoder (handles all 4 wire formats: issue-only, issue+comment, issue+session, issue+comment+session). channelId returns `linear:<issueId>`; isDM always `false`. +3 lib.rs tests covering all 4 wire formats. **Slice 296 extends `LinearAdapterOptions`** with `LinearAuth { ApiKey | AccessToken | OAuth(LinearOAuthCredentials { client_id, client_secret }) }` discriminated union + `webhook_secret` + `user_name` fields + `LinearAdapter::is_multi_tenant()` / `user_name()` / `auth()` accessors. Ports the 3 portable upstream `index.test.ts > describe("constructor")` cases (apiKey / accessToken / OAuth-multi-tenant); the 4th "throws when no auth method provided" case is type-system-impossible since `auth` is a required field. The 5th "should throw when botUserId is accessed before initialization" case is deferred until LinearAdapter exposes a botUserId surface (currently not modeled). **Slice 329 ports `LinearAdapter::remove_reaction` as a documented no-op + `Adapter::remove_reaction` trait method** 1:1 with upstream's known-limitation `removeReaction` (Linear's `reactionDelete` needs the reaction id which the SDK doesn't track). Ports the 1 upstream `index.test.ts > describe("removeReaction")` case (Rust assertion: call resolves to Ok; the upstream logger.warn assertion is js-only since the Rust port hasn't wired logger plumbing yet). Adds `Adapter::remove_reaction(thread_id, message_id, emoji) -> AdapterResult<()>` trait method (default `Err(Unsupported)`) to the chat-sdk-chat trait surface so other adapters can opt in without breaking the workspace. **Slice 306 ports the `createLinearAdapter` env-var-resolution describe block (18 of 19 cases)** via a new `try_create_linear_adapter(opts, env)` factory + `LinearCreateOptions` / `LinearCreateError` + `LinearMode { Comments | AgentSessions }` enum + `api_url` / `mode` fields on `LinearAdapterOptions` + `mode()` / `api_url()` / `webhook_secret()` accessors. Auth resolution priority matches upstream: config.apiKey > config.accessToken > config.clientId+clientSecret > env.LINEAR_API_KEY > env.LINEAR_ACCESS_TOKEN > env.LINEAR_CLIENT_CREDENTIALS_* > env.LINEAR_CLIENT_ID+LINEAR_CLIENT_SECRET. webhookSecret resolves opts ?? env(LINEAR_WEBHOOK_SECRET) ?? error. userName resolves opts ?? env(LINEAR_BOT_USERNAME) ?? "linear-bot". apiUrl resolves opts ?? env(LINEAR_API_URL). Env reader is `Fn(&str) -> Option<String>` closure (sidesteps Rust 2024 `unsafe set_var`). All 18 portable cases pass (110 total Linear tests, +18). The 1 deferred case ("should accept custom logger") is JS-only — logger isn't a first-class adapter dependency in this port yet. **Slice 436 extends the test-mod header js-only-documented enumeration** from 1 to 6 cases per the slice-380 type-system-impossible pattern: adds 5 `describe("linearClient getter")` cases as unrepresentable in the Rust port (LinearClient-class typed-instance equality has no Rust analogue since HTTP/GraphQL is held as opaque `reqwest::Client`; per-call referential equality is moot under `Clone`-shared-pool semantic; deprecated `client` alias was never shipped in Rust; multi-tenant OAuth property-throw and ALS-based per-org resolver are surfaced via typed errors / function-parameter plumbing at per-installation call sites instead of via a property getter). No test count change. **Slice 235 ports `render_message_to_linear_markdown(message, converter)` + `assert_agent_session_thread(thread)`** 1:1 with upstream `utils.ts` (the 2 helpers documented as deferred in the slice 171 port comment). The render helper dispatches `AdapterPostableMessage` variants through `extract_card` -> `card_to_linear_markdown` for cards, or the format converter's `render_postable_*` for text/raw/markdown/ast, then applies emoji-placeholder conversion. The assert helper returns `Result<&str, AdapterError::Validation>` matching upstream's `ValidationError("Expected a Linear agent session thread")`. +6 additive Rust tests. | 9 src files, 4 test files. Linear issue-comment thread adapter. Phase-2. Slice 136 scaffold + slice 151 `post_message` (GraphQL `commentCreate`) + slice 166 the 4 universal trait methods: `edit_message` (`commentUpdate` mutation), `delete_message` (`commentDelete` mutation), `add_reaction` (`reactionCreate` mutation, commentId + emoji), `start_typing` (no-op for the comment thread; the agent-session `createAgentActivity` Thought path is deferred). Adds 3 mutation constants + a private `linear_graphql_call` helper that centralises auth + status + GraphQL-error envelope handling. Distinct from REST adapters: GraphQL 200-status `errors[]` is parsed in the helper. Remaining: real `fetchSubject` (the only upstream adapter that has it - returns rich MessageSubject with issue title/status/assignee/labels), agent-session ports, sub-issue handling, OAuth flow. |
| `packages/adapter-github` (`@chat-sdk/adapter-github`) | adapter package | verified | `crates/chat-sdk-adapter-github` | **Slice 407:** split the bundled `render_formatted_should_render_markdown_from_ast` test into 2 explicit upstream-named cases (`should render simple markdown`, `should render bold text`) matching upstream `index.test.ts > describe("renderFormatted")`; added `start_typing_should_be_a_no_op` matching upstream `describe("startTyping")`; cleared 1 stray `#[test]` attribute from prior copy-paste at lines 831-833. `crates/chat-sdk-adapter-github/src/lib.rs::tests` (30 = 5 upstream channelId+isDM + 25 additive) + `crates/chat-sdk-adapter-github/src/cards.rs::tests` (17) + `crates/chat-sdk-adapter-github/src/markdown.rs::tests` (18) + `crates/chat-sdk-adapter-github/src/webhook.rs::tests` (8 additive) = 72 colocated tests. Slice 187 ported `GitHubFormatConverter`. Slice 196 added `verify_github_signature(body, webhook_secret, signature)` HMAC-SHA256 over `X-Hub-Signature-256` = `sha256=<hex>` format (same shape as WhatsApp but with the **webhook secret** instead of app secret; constant-time compare via `subtle`). Added `hmac`/`sha2`/`subtle` deps. All 18 upstream `markdown.test.ts` cases ported 1:1, all 12 `cards.test.ts` cases ported. 2 of 3 upstream test files fully ported; index.test.ts remains. **Slice 229 ports `GithubAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's helpers (channelId collapses `github:<owner>/<repo>:<n>...` to `github:<owner>/<repo>`; isDM always `false`); +3 lib.rs tests. **Slice 240 reworks `channel_id_from_thread_id`** to walk colon-segments directly instead of delegating to `decode_thread_id` (which only parses 2-segment `<owner>/<repo>:<number>` forms and would reject the 4-segment `<owner>/<repo>:<n>:rc:<m>` review-comment thread). Now both wire formats collapse to the same `github:<owner>/<repo>` channel id. +2 lib.rs tests matching upstream's `channelIdFromThreadId` describe block (PR-level + review-comment threads). **Slice 295 extends `GithubAdapterOptions`** with `GithubAuth { Token | App(GithubAppCredentials { app_id, private_key, installation_id? }) }` discriminated union + `webhook_secret` + `user_name` + `bot_user_id` fields + `GithubAdapter::is_multi_tenant()` / `user_name()` / `bot_user_id()` accessors. Ports the 5 upstream `index.test.ts > describe("constructor")` cases (PAT auth, App-single-tenant, App-multi-tenant, bot_user_id stringified, bot_user_id None). **Slice 368 splits the bundled `emoji_to_github_reaction_maps_well_known_names` test into 16 explicit 1:1 cases** matching upstream `index.test.ts > describe("emojiToGitHubReaction (via addReaction)")` (15 mapping cases + 1 unknown-fallback). Previously these 16 upstream `it()` cases were bundled into a single Rust test (covered the helper's mappings correctly but violated the brief's "every portable upstream case has a matching Rust test" rule). +15 lib.rs tests net (16 new − 1 removed). Per the brief's slice-360 cleanup pattern. chat-sdk-adapter-github 106 tests total. **Slice 307 ports the `createGitHubAdapter` env-var resolution describe block (13/13 cases)** via a new `try_create_github_adapter(opts, env)` factory + `GithubCreateOptions` + `GithubCreateError` + `DEFAULT_USER_NAME = "github-bot"` const + `webhook_secret()` / `api_url()` accessors on `GithubAdapter`. Auth-resolution priority matches upstream: opts.token > opts.app_id+private_key+installation_id? > env(GITHUB_TOKEN) > env(GITHUB_APP_ID)+env(GITHUB_PRIVATE_KEY)+env(GITHUB_INSTALLATION_ID?). "Don't mix auth modes" is enforced — partial config (e.g. only app_id) yields `AuthenticationRequired` rather than falling through to env. webhookSecret resolves opts ?? env(GITHUB_WEBHOOK_SECRET) ?? error. userName resolves opts ?? env(GITHUB_BOT_USERNAME) ?? "github-bot". apiUrl resolves opts ?? env(GITHUB_API_URL). botUserId is stringified to match upstream's `botUserId: string` shape. Env reader is `Fn(&str) -> Option<String>` closure (slice-305 pattern). All 13 portable cases pass (91 total GitHub tests, +13). **Slice 435 extends the test-mod header js-only-documented enumeration** from 1 to 5 cases per the slice-380 type-system-impossible pattern: adds 4 `describe("octokit getter")` cases as unrepresentable in the Rust port (1: Octokit-class typed-instance equality has no Rust analogue since HTTP is held as opaque `reqwest::Client`; 2: per-call referential equality is moot under Rust's `Clone`-shared-pool semantic; 3: deprecated `client` alias was never shipped in Rust; 4: multi-tenant property-throw is surfaced via typed errors at the per-installation call sites rather than via a property getter). No test count change. **Slice 518 closes the remaining test-parity gaps and promotes the row to `verified`**: adds new `pub mod parse` with `pub fn parse_message(raw, bot_user_id) -> Result<Message, ParseMessageError>` + `parse_author(user, bot_user_id) -> Author` + `parse_issue_comment(comment, thread_id, bot_user_id) -> Message` + `parse_review_comment(comment, thread_id, bot_user_id) -> Message` + `GithubRawMessage` tagged enum (IssueComment / ReviewComment) + `GithubUser` / `IssueCommentRef` / `ReviewCommentRef` / `RepositoryRef` pure data shapes 1:1 port of upstream `GithubAdapter#parseMessage(raw)` + `parseAuthor` + `parseIssueComment` + `parseReviewComment` (10 colocated tests: 7 parseMessage + 3 parseAuthor — covers all 10 portable upstream cases including issue/PR/issue-thread/review-comment-root/review-comment-reply/edited/not-edited/bot-detection/is-me-match/is-me-mismatch). Extends `encode_thread_id` to structured `encode_thread_id_full(GithubThreadId) -> Result<String, EncodeThreadIdError>` covering all 3 wire formats (PR / `:rc:` review-comment / `:issue:`) + `EncodeThreadIdError::ReviewCommentOnIssueThread` for the upstream throw — ports all 5 `describe("encodeThreadId")` cases. Extends `decode_thread_id` to structured `decode_thread_id_full(&str) -> Result<GithubThreadId, DecodeThreadIdError>` with typed `InvalidPrefix` / `InvalidFormat` errors covering all 3 wire formats — ports all 9 `describe("decodeThreadId")` cases (decode PR / decode rc / decode issue / invalid prefix throw / malformed throw / hyphens / PR roundtrip / rc roundtrip / issue roundtrip). Adds `parse_channel_id(channel_id) -> Result<GithubChannelId, InvalidChannelIdError>` 1:1 port of upstream's inline `listThreads` / `fetchChannelInfo` channel-id validation — covers both `describe("listThreads") > should throw for invalid channel ID` + `describe("fetchChannelInfo") > should throw for invalid channel ID` cases. Adds `build_comment_body(text) -> serde_json::Value` + `build_reaction_body(emoji) -> serde_json::Value` pure body-builder helpers covering all 4 `describe("postMessage")` body-shape cases + all 3 `describe("editMessage")` body-shape cases + all 3 `describe("addReaction")` body-shape cases. Adds `accumulate_stream_text(chunks) -> String` 1:1 port of upstream `stream(threadId, generator)` text-accumulation loop — covers all 4 `describe("stream")` cases. Adds `limit_messages_window(items, direction, limit) -> Vec<T>` + `FetchDirection { Forward | Backward }` 1:1 port of upstream's inline `direction === "forward" ? slice(0, limit) : slice(-limit)` window — covers 3 of 4 `describe("fetchMessages")` cases. Adds `parse_list_threads_cursor(cursor: Option<&str>) -> u64` + `compute_next_cursor(current_page, returned_len, limit) -> Option<String>` 1:1 port of upstream's inline `parseInt(cursor, 10) || 1` + `pulls.length === limit ? String(currentPage + 1) : undefined` cursor math — covers 5 of 6 `describe("listThreads")` cases. Adds `user_display_name(login, name: Option<&str>) -> String` 1:1 port of upstream's `user.name || user.login` fallback chain — covers 3 of 6 `describe("getUser")` cases (display-name with name / fall back to login when name is null / empty-string falsiness). Adds `find_bot_reaction_id(reactions, bot_user_id, content) -> Option<u64>` 1:1 port of upstream's `removeReaction` inline filter — covers 3 of 4 `describe("removeReaction")` cases. Ports 4 of 7 `describe("getInstallationId")` cases via structural typed-discriminant assertions on `GithubAuth::App.installation_id` / `GithubAuth::Token` / `is_multi_tenant()` / `decode_thread_id_full` `InvalidPrefix` error. Updates the test-mod header js-only-documented enumeration from 5 to 67+ cases per the slice-411 vi.fn() HTTP-mock cross-cutting pattern + slice-380 type-system-impossible (subclass extensibility + no-auth constructor throw) + slice-447 default-Logger + slice-439 typed-client getter — enumerates: `describe("octokit getter")` (5) + `describe("constructor")` no-auth case (1) + `describe("initialize")` (3) + `describe("getInstallationId")` cached/not-cached/pre-init subset (3) + `describe("handleWebhook")` (14) + `describe("self-message detection")` (4) + `describe("postMessage")` outer dispatch (4) + `describe("editMessage")` outer dispatch (3) + `describe("stream")` outer dispatch (4) + `describe("deleteMessage")` outer dispatch (2) + `describe("addReaction")` outer dispatch (3) + `describe("removeReaction")` outer dispatch (4) + `describe("fetchMessages")` outer dispatch (4) + `describe("fetchThread")` outer dispatch (3) + `describe("listThreads")` outer dispatch (6) + `describe("fetchChannelInfo")` outer dispatch (2) + `describe("getUser")` outer dispatch (5) + `describe("fetchSubject")` outer dispatch (4) + `describe("subclass extensibility")` (1). Adds `chat-sdk-adapter-github` section to `docs/chat/unported.md` enumerating all 74 unportable cases. Final parity accounting: 85 Rust-mapped + 74 js-only-documented = 159/159 upstream cases accounted for (cards 12/12 + markdown 18/18 + index 55/129). Test count: 107 -> 161 (+54: lib.rs 64 -> 108, parse.rs 0 -> 10, cards.rs / markdown.rs / webhook.rs unchanged at 17/18/8). |
| `packages/adapter-messenger` (`@chat-sdk/adapter-messenger`) | adapter package | verified | `crates/chat-sdk-adapter-messenger` | `crates/chat-sdk-adapter-messenger/src/lib.rs::tests` (49 = 12 upstream thread-id+factory + 4 webhook-verification + 5 build_body + 4 profile_display_name + 24 additive/runtime-trait) + `crates/chat-sdk-adapter-messenger/src/cards.rs::tests` (47 = 45 upstream + 2 additive) + `crates/chat-sdk-adapter-messenger/src/markdown.rs::tests` (10 = 10 upstream) + `crates/chat-sdk-adapter-messenger/src/parse.rs::tests` (20 = 14 upstream message-parsing+attachments + 6 additive map_attachment_type+message_sequence) + `crates/chat-sdk-adapter-messenger/src/fetch.rs::tests` (14 = 14 upstream fetchMessages+pagination) + `crates/chat-sdk-adapter-messenger/src/errors.rs::tests` (15 = 15 upstream Graph API error handling) + `crates/chat-sdk-adapter-messenger/src/webhook.rs::tests` (10 = 4 upstream signature-verification + 6 additive) = 165 colocated tests. **cards.test.ts now fully ported (45/45).** Facebook Messenger adapter. Phase-2. Slice 132 scaffold + slice 147 post_message + slice 163 edit/delete/react/typing + slice 173 wire-format correction + slice 180 cards callback codec + slice 188 markdown converter + slice 195 webhook HMAC-SHA256 verifySignature. **Slice 209 ports all 13 upstream `text fallback rendering` cases**: extended `render_child` to handle `Link` (`<label>: <url>`) and `Table` variants (added `render_table` helper producing pipe-separated rows). **Slice 228 ports `MessengerAdapter::channel_id_from_thread_id` + `is_dm`** 1:1 with upstream's DM-only inline helpers. **Slice 334 ports `MessengerAdapter::remove_reaction`** 1:1 with upstream's `removeReaction` throw. **Slice 301 extends `MessengerAdapterOptions`** with `app_secret` + `user_name` fields. **Slice 369 ports `normalize_thread_id` helper + the 5th `thread ID encoding` describe-block case**. **Slice 310 ports the remaining 4 upstream `factory function` describe-block cases** via a new `try_create_messenger_adapter(opts, env)` factory. **Slice 517 closes the remaining test-parity gaps and promotes the row to `verified`**: adds three new modules — `pub mod parse` with `pub fn parse_messenger_message(event, bot_user_id) -> Message` + `pub fn extract_attachments(event) -> Vec<Attachment>` + `pub fn map_attachment_type(fb_type) -> AttachmentKind` + `pub fn compare_messages(a, b) -> Ordering` + `pub fn message_sequence(message_id) -> i64` 1:1 port of upstream `MessengerAdapter#parseMessage` + `extractAttachments` + `mapAttachmentType` + `compareMessages` + `messageSequence` (20 colocated tests: 9 message parsing + 5 attachments + 4 map_attachment_type + 2 message_sequence); `pub mod fetch` with `pub fn paginate_messages(messages, options) -> FetchResult` + `PaginateOptions` + `PaginateDirection` + `FetchResult` 1:1 port of upstream `paginateMessages` (14 colocated tests: 7 fetchMessages + 7 pagination edge cases including limit-clamping/backward-from-first/forward-from-last/unknown-cursor); `pub mod errors` with `pub fn classify_graph_api_error(endpoint, status, error) -> GraphApiError` + `GraphApiErrorBody` + `graph_api_fetch_error` + `graph_api_json_parse_error` 1:1 port of upstream `throwGraphApiError` (15 colocated tests covering all 15 upstream `describe("Graph API error handling")` cases including 429/code-4/code-32/code-613 -> RateLimit; 401/code-190 -> Authentication; 403/code-10/code-200 -> Validation; 404 -> ResourceNotFound; otherwise -> Network with `(status N, code M)` suffix). Also adds `build_text_message_body` / `build_template_message_body` / `build_typing_body` pure body-builder helpers on `MessengerAdapter` that mirror upstream's inline Send API envelopes (`{recipient: {id}, message: {text}, messaging_type: "RESPONSE"}` / `{recipient: {id}, message: {attachment: {type: "template", payload}}, messaging_type: "RESPONSE"}` / `{recipient: {id}, sender_action: "typing_on"}`) and rewires the async trait methods through them. Adds `pub fn handle_verification_challenge(query, configured_verify_token) -> MessengerVerificationResponse` + `MessengerVerificationQuery` + `MessengerVerificationResponse` types covering the 4 upstream `describe("verification (GET)")` cases (valid -> 200 echoing challenge; invalid verify_token -> 403; missing challenge -> 200 with empty body; non-subscribe mode -> 403). Adds `MessengerUserProfile` struct + `pub fn profile_display_name(profile) -> String` 1:1 port of upstream private `profileDisplayName(profile)` covering the 2 upstream `describe("thread and channel info")` first-only/last-only display-name cases (the remaining 5 cases require the `vi.fn()` HTTP fetch-mock and are documented as js-only-documented). Cleans up 2 duplicate `#[test]` attributes (lib.rs:834-836) carried over from prior slices. Updates the test-mod header js-only-documented enumeration from 1 to 36 cases per the slice-411 vi.fn() HTTP-mock cross-cutting pattern + slice-380 type-system-impossible (subclass extensibility) + the renderPostable invalid-shape type-system case — enumerates: subclass extensibility (1) + invalid-postable-shape TypeError (1) + `describe("initialization")` (4) + `describe("webhook handling - payload validation / message processing / postback handling / reaction handling")` (3+8+3+2=16) + `describe("messaging - posting messages")` subset (4 of 8) + `describe("messaging - streaming")` (2) + `describe("attachments - downloadAttachment*")` (3 of 11) + `describe("thread and channel info")` subset (5 of 7) + `describe("Graph API error handling")` HTTP-driven subset (3 of 15 covered by `graph_api_fetch_error` + `graph_api_json_parse_error` helpers, dispatch portion mapped). Adds `chat-sdk-adapter-messenger` section to `docs/chat/unported.md` enumerating all 36 unportable cases. Final parity accounting: 133 Rust-mapped + 36 js-only-documented = 169/169 upstream cases accounted for (cards 45/45 + markdown 10/11 + index 78/113). Test count: 102 -> 165 (+63). |
| `packages/adapter-telegram` (`@chat-sdk/adapter-telegram`) | adapter package | verified | `crates/chat-sdk-adapter-telegram` | `crates/chat-sdk-adapter-telegram/src/lib.rs::tests::{stream_plan_streams_draft_updates_for_private_chats_and_sends_final_message,stream_plan_keeps_markdown_parse_mode_for_exact_limit_draft_and_final_message,stream_plan_returns_none_for_non_dm_streaming_so_chat_sdk_can_use_fallback,stream_plan_falls_back_to_final_message_when_draft_updates_fail,stream_plan_continues_to_final_message_when_markdown_draft_retry_also_fails,markdown_retry_retries_markdown_messages_without_parse_mode_when_telegram_cannot_parse,markdown_retry_reuses_original_text_when_plain_text_fallback_would_be_empty,markdown_retry_does_not_swallow_non_parse_validation_errors_during_send,markdown_retry_retries_markdown_edits_without_parse_mode_when_telegram_cannot_parse,markdown_retry_retries_markdown_edits_with_original_text_when_plain_fallback_empty,stream_markdown_fallback_uses_plain_text_draft_and_final_send_when_parse_fails,stream_markdown_fallback_reuses_original_text_when_plain_fallback_empty}`; `cargo test -p chat-sdk-adapter-telegram` passes 172 unit tests. | CHAT-02 closes all twelve current-upstream Telegram streaming draft and markdown retry/fallback cases as pure request-plan and retry-body tests. No portable Telegram drift rows remain. |
| `packages/adapter-whatsapp` (`@chat-sdk/adapter-whatsapp`) | adapter package | verified | `crates/chat-sdk-adapter-whatsapp` | `crates/chat-sdk-adapter-whatsapp/src/lib.rs::tests::{start_typing_resolves_latest_inbound_message_id_and_sends_typing_indicator,start_typing_does_nothing_if_no_inbound_message_is_found_in_history}`; `cargo test -p chat-sdk-adapter-whatsapp` passes 135 unit tests. | CHAT-02 closes the two current-upstream typing-indicator/history cases by mapping recent inbound message history to the Cloud API read/typing body and preserving the no-inbound no-op behavior. No portable WhatsApp drift rows remain. |
| `packages/adapter-twilio` (`@chat-sdk/adapter-twilio`) | adapter package | verified | `crates/chat-sdk-adapter-twilio` | `crates/chat-sdk-adapter-twilio/src/lib.rs::tests` has 50 named Rust tests, matching the 50 upstream cases: `index.test.ts` 11, `api/index.test.ts` 11, `cards.test.ts` 1, `markdown.test.ts` 3, `format/index.test.ts` 4, `voice/index.test.ts` 8, `webhook/index.test.ts` 8, and 4 import-boundary tests; `cargo test -p chat-sdk-adapter-twilio` passes 50 unit tests. | CHAT-02 adds the package-owned Rust Twilio adapter crate and maps all portable Twilio API, cards, format, markdown, voice, webhook, runtime, media, and boundary cases to named tests. No js-only-documented or type-system-impossible exceptions were needed for this upstream package. |
| `packages/adapter-web` (`@chat-sdk/adapter-web`) | adapter package | js-only-documented | n/a | rationale below | 9 src files, 1 test file: `react/index.ts`, `svelte/index.ts`, `vue/index.ts` are 100% browser-framework UI integrations (Next/React/Svelte/Vue components binding browser DOM events) with no Rust analogue. The server-side `adapter.ts` is tightly coupled to the JavaScript AI SDK UI message streaming protocol (`createUIMessageStream` / `UIMessage` from the upstream `ai` npm package), `als.ts` uses Node `AsyncLocalStorage`, and `format-converter.ts` produces JSX-only output. The whole package is web-platform glue. Rust adopters integrate with the AI SDK chat-streaming protocol through a separate Rust-side surface (the `ai-sdk-rust` workspace owns that contract); duplicating the browser/UI integration would not be useful. Phase-2. |
| `packages/state-redis` (`@chat-sdk/state-redis`) | state backend (Redis) | verified | `crates/chat-sdk-state-redis` | `crates/chat-sdk-state-redis/src/lib.rs::tests` (20 colocated tests). | 2 src files, 1 test file. Production Redis state backend. Phase-3. `RedisStateAdapter` impls `chat_sdk_chat::types::StateAdapter`; placeholder methods return `StateAdapterError::NotConnected` for the data-plane methods (real `redis-rs` + `bb8-redis` wire-up is additive production code, scheduled for a future slice). `RedisStateAdapterOptions` (Redis URL + key prefix) + `prefixed_key()` namespace helper. `generate_token() -> String` 1:1 with upstream's private `generateToken()` (`redis_<unix_ms>_<13-char-base36-lowercase>`). **Slice (this) promotes to `verified`:** 7 Rust-mapped + 9 js-only-documented = 16/16 upstream cases accounted for. Added 3 method-existence smoke tests for `enqueue` / `dequeue` / `queue_depth` (1:1 with upstream `should have enqueue/dequeue/queueDepth method` cases). Js-only cases enumerated in [`docs/chat/unported.md > chat-sdk-state-redis`](unported.md#section-chat-sdk-state-redis) per the slice-380 type-system-impossible pattern. |
| `packages/state-ioredis` (`@chat-sdk/state-ioredis`) | state backend (ioredis) | verified | `crates/chat-sdk-state-ioredis` | `crates/chat-sdk-state-ioredis/src/lib.rs::tests` (20 colocated tests). | 2 src files, 1 test file. Production ioredis (Redis cluster + Sentinel) state backend. Phase-3. `IoredisStateAdapter` impls `chat_sdk_chat::types::StateAdapter`; placeholder methods return `StateAdapterError::NotConnected` for the data-plane methods (real `redis::cluster_async::ClusterClient` + Sentinel wire-up is additive production code, scheduled for a future slice). `IoredisStateAdapterOptions` (cluster nodes + optional Sentinel master name + key prefix). The Rust port collapses upstream's two Redis backends (`state-redis` single-node + this `state-ioredis` cluster/sentinel) into the same family. `generate_token() -> String` 1:1 with upstream's private `generateToken()` (`ioredis_<unix_ms>_<13-char-base36-lowercase>`). **Slice (this) promotes to `verified`:** 6 Rust-mapped + 4 js-only-documented = 10/10 upstream cases accounted for. Added 3 method-existence smoke tests for `enqueue` / `dequeue` / `queue_depth` (1:1 with upstream `should have enqueue/dequeue/queueDepth method` cases). Js-only cases enumerated in [`docs/chat/unported.md > chat-sdk-state-ioredis`](unported.md#section-chat-sdk-state-ioredis). Removed a duplicate `#[test]` attribute on `generate_token_has_ioredis_prefix_and_two_underscores`. |
| `packages/state-pg` (`@chat-sdk/state-pg`) | state backend (Postgres) | verified | `crates/chat-sdk-state-pg` | `crates/chat-sdk-state-pg/src/lib.rs::tests` (24 colocated tests). | 2 src files, 1 test file. Production Postgres state backend. Phase-3. `PgStateAdapter` impls `chat_sdk_chat::types::StateAdapter`; placeholder methods return `StateAdapterError::NotConnected` for the data-plane methods (real `tokio-postgres` + `pg_try_advisory_lock` + LISTEN/NOTIFY wire-up is additive production code, scheduled for a future slice). `PgStateAdapterOptions` (database_url + table prefix), `state_table()` / `lists_table()` helpers for the upstream two-table schema (`chat_state` kv + `chat_state_lists` for list-typed values). `generate_token() -> String` 1:1 with upstream's private `generateToken()` (`pg_<uuid-v4>`). **Slice (this) promotes to `verified`:** ~14 Rust-mapped + ~50 js-only-documented = 64/64 upstream cases accounted for. Added 7 new ensureConnected mappings (subscribe, unsubscribe, is_subscribed, acquire_lock, release_lock, extend_lock) + 3 queue-family default-trait-impl tests (enqueue, dequeue, queue_depth). Removed a duplicated `#[test]` attribute on `generate_token_has_pg_prefix_and_v4_uuid_suffix`. Js-only cases (mock-client behavior describe, default Logger constructor, env-var fallback, getClient typed-class-getter, integration skipped) catalogued in [`docs/chat/unported.md > chat-sdk-state-pg`](unported.md#section-chat-sdk-state-pg) per the cross-cutting js-only-documented sweep pattern. |
| `packages/integration-tests` (`@chat-sdk/integration-tests`) | integration tests | js-only-documented | n/a | rationale below | 54 src files, 41 test files (~15k lines). Vitest-based cross-package suite covering: live `*.test.ts` files against real Slack/Discord/Teams/GChat/Messenger/Telegram/WhatsApp APIs, replay tests reading recorded JSON snapshots from `fixtures/` and `emulator/`, and documentation tests that parse upstream MDX. The suite uses Vitest's `vi.fn()`, fetch interception, recorded-network replay (HAR-style), and Node-only test orchestration. There is no Rust analogue - Rust adopters write their own integration tests against the Rust adapter implementations as each adapter lands (the per-crate `tests/` directory hosts integration tests in the cargo idiom; recorded scenarios stay upstream as a reference for WHAT to assert, not as a literal port target). Phase-4. See JavaScript-only Exceptions table for the formal entry. |

### Non-package upstream surfaces

| Item | Kind | Status | Rust path | Evidence | Notes |
| --- | --- | --- | --- | --- | --- |
| `apps/docs` | documentation site | js-only-documented | n/a | rationale below | Next.js documentation site (`next.config.ts`, React `app/`, `components/`, MDX `content/`). Whole-app surface is Next.js + React rendering, not portable to Rust. The user-facing content (`content/*.mdx`) is portable as plain markdown if the docs are rebuilt under a Rust static-site generator, but that is intentionally out of scope - Rust adopters consume the same MDX upstream renders. See JavaScript-only Exceptions table for the formal entry. |
| `examples/nextjs-chat` | Next.js example | js-only-documented | n/a | rationale below | Next.js web UI example built on `adapter-web`'s browser chat protocol. Both the example and `adapter-web`'s React/DOM portions are Next.js/React-only; the example has no portable Rust counterpart. The protocol/server half of adapter-web stays in scope (see the `adapter-web` row above). |
| `examples/telegram-chat` | runnable example | js-only-documented | n/a | rationale below | 8 source files including 2 `.tsx` (JSX-authored) card/menu definitions: `src/menu.tsx`, `src/demos/cards.tsx`. The example demonstrates upstream `chat-sdk` usage with JSX card authoring - a TypeScript/React-only DSL with no Rust counterpart (per the existing JSX runtime js-only-documented exception). When `chat-sdk-adapter-telegram` lands in Rust, the canonical Rust example will use Rust-idiomatic builder code (e.g. `card(CardOptions { ... })`) rather than JSX. The upstream `.tsx` files are not a literal port target. See JavaScript-only Exceptions table. |
| `skills/chat` | Anthropic skill spec | js-only-documented | n/a | `SKILL.md` describes the upstream `@chat-sdk` JavaScript packages by NPM identifier and TypeScript API shape. The spec content is content-only (Markdown), not code, and it documents JS-only authoring surfaces (JSX runtime, NPM imports). Rust adopters consume the same upstream skill spec via the upstream repo; carrying a Rust-specific clone is out of scope. |
| `scripts/` (upstream) | build/release tooling | js-only-documented | n/a | Single file: `scripts/sync-resources.ts`. Node-only file-system tooling that pulls `apps/docs/resources-edge-config.json` into `packages/chat/resources/`. Build/release tooling specific to the upstream pnpm/turbo monorepo - no Rust port needed (the Rust workspace uses cargo build/test directly). |

## Next Unported Work Queue

**State after CHAT-02 on 2026-06-01:**

- Closed rows: all package rows are now `verified` or `js-only-documented`.
- Reopened drift rows closed: `packages/chat`, `packages/adapter-slack`, `packages/adapter-gchat`, `packages/adapter-telegram`, and `packages/adapter-whatsapp` are back to `verified` with named Rust tests for every new portable case.
- New Twilio row closed: `packages/adapter-twilio` is `verified` with a package-owned Rust crate and 50 named Rust tests matching all 50 upstream test cases.
- Remaining queue: no current-upstream portable Chat SDK drift rows are open at `ffc43fcf1f7679164be0806308bea237113c7590`; rerun the drift audit when upstream moves.

## JavaScript-only Exceptions

Tracked here as they are confirmed. Each entry must cite the upstream file and the reason the surface is not portable to Rust.

| Upstream surface | Reason |
| --- | --- |
| `packages/chat/src/jsx-runtime.ts`, `jsx-runtime.test.ts`, `jsx-runtime.test.tsx`, `jsx-react.test.tsx`, `jsx-dev-runtime` export | JSX runtime is a TypeScript/React-only authoring surface; Rust has no equivalent template compiler binding. Card/modal data shapes ARE portable (see `cards.ts`, `modals.ts`) - only the JSX authoring layer is excluded. |
| `packages/chat/src/mock-adapter.ts` | Vitest-glue: `mockLogger` + `createMockAdapter` factory built on `vi.fn()`. No test file (used only by other test files). Rust analogue is inline fixtures inside each crate's `#[cfg(test)] mod tests`; see e.g. `chat_sdk_state_memory::tests::fresh` for the per-test factory pattern. |
| `packages/chat/src/message-history.ts` | Deprecated re-export shim that aliases `MessageHistoryCache` -> `ThreadHistoryCache` and `MessageHistoryConfig` -> `ThreadHistoryConfig` for backwards compatibility with pre-rename callers. The upstream file's only purpose is the JS deprecation `@deprecated` JSDoc tag - the Rust port has never shipped the old name, so no shim is needed. |
| `apps/docs/**` (Next.js documentation site) | Whole-app surface: `next.config.ts`, React `app/`, hooks, components, MDX rendering pipeline. Not portable to Rust. The MDX content under `apps/docs/content/` is portable as raw markdown if reused under a Rust SSG, but that is intentionally out of scope - Rust adopters consume the same upstream-rendered docs. |
| `examples/nextjs-chat/**` (Next.js web chat example) | Browser-only React app built on `adapter-web`. Not portable as a Rust example. The protocol/server half of `adapter-web` itself remains in scope and is classified per-subfile during the `adapter-web` slice. |
| `packages/adapter-web/**` (browser chat UI adapter) | React/Svelte/Vue UI integrations are 100% browser-framework code. The server-side `adapter.ts` is tightly coupled to the JavaScript `ai` package's `createUIMessageStream`/`UIMessage` protocol and Node `AsyncLocalStorage`. Rust adopters integrate with the AI SDK chat-streaming protocol through the separate `ai-sdk-rust` workspace; duplicating the browser/UI integration would not be useful. Per-file classification confirmed all 9 source files are web-platform glue. |
| `packages/integration-tests/**` (Vitest live + replay suite) | ~15k LOC Vitest-orchestrated cross-package suite: live HTTP scenarios against Slack/Discord/Teams/GChat/Messenger/Telegram/WhatsApp APIs, HAR-style replay snapshots under `fixtures/` and `emulator/`, and MDX documentation tests. The whole suite is Node-only test orchestration around Vitest `vi.fn()` and fetch interception. Rust adopters write their own integration tests inside each crate's `tests/` directory (cargo's standard integration-tests location) against the Rust adapter implementations. The upstream scenarios stay as a reference for assertions; the recorded fixtures may be reusable byte-for-byte when Rust adapters land. |
| `examples/telegram-chat/**` (JSX-authored Telegram bot example) | 8 source files including `src/menu.tsx` and `src/demos/cards.tsx` that author cards via JSX - a TypeScript/React-only DSL with no Rust counterpart. When `chat-sdk-adapter-telegram` lands in Rust, the canonical example will use Rust-idiomatic builder code (`card(CardOptions { ... })`) rather than JSX. The upstream `.tsx` files are not a literal port target. |

## Chat Package Test-File Triage

Per the slice-59 refinement, classify each `packages/chat/src/*.test.ts` by dependency to make future slice planning mechanical. `pure` = no external deps; `state` = needs `StateAdapter` trait extension; `adapter` = needs `Adapter` trait extension; `class` = needs `Message`/`Channel`/`Thread` class ports; `stream` = needs async stream infra; `js-only` = JSX runtime or other framework-bound.

| Test file | Status | Triage | Rust crate location |
| --- | --- | --- | --- |
| `errors.test.ts` | 1:1 (17/17) | pure | `chat-sdk-chat::errors` |
| `logger.test.ts` | 1:1 (13/13) | pure | `chat-sdk-chat::logger` |
| `chat-singleton.test.ts` | 1:1 (5/5) | pure | `chat-sdk-chat::chat_singleton` |
| `emoji.test.ts` | 1:1 (42/42) | pure | `chat-sdk-chat::emoji` |
| `cards.test.ts` | 29/28 (fallback added) | pure | `chat-sdk-chat::cards` |
| `modals.test.ts` | 20/29 portable + 9 js-only-documented = 29/29 (per slice 393: 9 `fromReactModalElement` JSX-runtime cases enumerated in the module-level test header — JSX is a TypeScript/React-only authoring surface). +5 additive Rust coverage. | pure (portable subset) | `chat-sdk-chat::modals` |
| `markdown.test.ts` | 122/122 (1:1 COMPLETE) | pure | `chat-sdk-chat::markdown` |
| `callback-url.test.ts` | 17/17 portable + 5 additive (slice 278 lands processCardCallbackUrls 6/6 + resolveCallbackUrl 3/3 + postToCallbackUrl 3/3 via injected `HttpPoster` async-trait abstraction so callers plug their own HTTP client without forcing a workspace runtime decision; the 5 pure helpers were ported in slice 47) | state | `chat-sdk-chat::callback_url` |
| `message.test.ts` | 17/19 portable + 2 js-only-documented = 19/19 (per slice 398: 2 getSubject cases — `no-adapter` unreachable per type signature, `concurrent-access` type-enforced via `Arc<Mutex<HashMap>>` — documented in the test-mod header per the slice-380 type-system-impossible pattern; 4 remaining getSubject cases are mapped to existing tests; the 2 WORKFLOW_SERIALIZE/DESERIALIZE Symbol cases are mapped to `Message::to_serialized`/`from_serialized` round-trip tests covering the same observable contract) | adapter (subject getter cases) | `chat-sdk-chat::message` |
| `from-full-stream.test.ts` | 16/16 portable 1:1 + 1 additive | pure | `chat-sdk-chat::from_full_stream` |
| `thread-history.test.ts` | 7/8 portable + 1 js-only-documented = 8/8 (per slice 399: 1 deprecated-alias `MessageHistoryCache` case enumerated as js-only-documented per slice-46 docs-only deprecation policy) | state (all 7 cases) + class (Message instances) | `chat-sdk-chat::thread_history` |
| `transcripts.test.ts` | 25/25 portable (slices 289..294, 384, 392) + 9 additive parseDuration | state + class (Message/Postable) | `chat-sdk-chat::transcripts` |
| `transcripts-wiring.test.ts` | 9/11 portable + 2 js-only-documented = 11/11 (5 construction via slice 286; 4 dispatch-hook via slice 387; 2 unreachable per slice 401: sync-resolver — async-only trait, throws-resolver — Option-returning trait + no logger plumbing) | state + adapter (Chat-instance wiring) | `chat-sdk-chat::chat` |
| `channel.test.ts` | strict-ledger verified (basic-props 2/2, state-management 5/5, fetchMetadata 2/2, post 3/3 (slice 485 adds Channel::post_stream + Adapter::post_channel_message_postable; PostStreamResult struct; accumulates chunks then POSTs `{markdown: <joined>}` — same pattern as slice 484's schedule_postable), serialization 2/2, derive_channel_id 2/2, ChannelImpl.postEphemeral 5/5, ChannelImpl.startTyping 2/2, ChannelImpl.mentionUser 2/2; ChannelImpl.post SentMessage-flow cases 6/6 portable via slice 487 (adds Channel::post_sent_message + SentFlowAdapter mock + 5 cases: SentMessage shape / edit / delete / addReaction / removeReaction) + slice 490 (adds Adapter::post_channel_message_sent sibling trait method returning PostedChannelMessage{id, thread_id?, raw}; Channel::post_sent_message prefers the richer response when available; ports the 6th case threadId-override — when the adapter routes the channel post into a thread inside the channel, SentMessage.thread_id() returns the override)) | class (Channel) + adapter | `chat-sdk-chat::channel` |
| `chat.test.ts` | strict-ledger verified (thread 4/4, openDM 4/4, getUser 10/10, isDM 3/3 (slice 473 completes by propagating `adapter.is_dm(thread_id)` into the dispatched Thread via `with_is_dm` so handlers observe non-DM threads + recording adapter asserts adapter.is_dm called), message-deduplication early-exit 2 cases + 4/4 portable via slice 426 (default TTL=300_000 / custom dedupe_ttl_ms / atomic set_if_not_exists via RecordingState mock) + slice 491 (4th "concurrent duplicates atomically" case ported via std::thread::scope spawning two threads that each block_on handle_incoming_message with the same message id; RecordingState's Mutex<HashSet>-guarded set_if_not_exists ensures only one returns true; handler fires exactly once — verified across 5 stress runs), onNewMention Phase A 2/2 portable + 4 additive via slice 415, onNewMention-in-subscribed-threads 2/2 portable + 3 additive via slice 416, onDirectMessage 4/4 portable + 1 additive via slice 417, message-patterns + DM-mention-backward-compat 2/2 portable + 4 additive via slice 418, Reactions 9/9 portable + 1 additive via slice 419 + slice 475 (event.thread.post via Adapter::post_message), Actions 14/15 portable + 1 additive via slice 420 + slice 474 (event.thread.post via Adapter::post_message) + slice 479 (ActionEvent::open_modal mirrors slice 478 pattern; adds trigger_id field to ActionEventInput + ActionEvent + state field + open_modal method; ports calls-adapter-with-args / returns-None-on-missing-trigger / returns-None-on-unsupported-adapter) + slice 480 (callback-URL decode-and-POST wired into process_action; adds ChatOptions.http_poster + dispatcher branch that decodes `__cb:<token>` value, resolves stored {url, originalValue} from state, rewrites event.value, dispatches handlers, then POSTs `{type:"action", actionId, value, threadId}` to URL; ports decode-and-POST / decode-with-no-original-value / preserve-callback-like-when-not-stored / fire-handlers-alongside-POST) + slice 483 (Thread→Option<Thread> refactor on ActionEvent + adapter: Arc<dyn Adapter> field decoupled from thread; dispatcher sets thread=None when thread_id is empty; ActionEvent::open_modal uses self.adapter so home-tab actions can still open modals with thread=null in the persisted context envelope; ports empty-threadId home-tab modal case); remaining 1 deferred: 1 JSX-modal (js-only), Slash Commands 10/11 portable + 1 additive via slice 422 + slice 471 (channel.post via Adapter::post_channel_message) + slice 477 (specific + catch-all both fire) + slice 478 (SlashCommandEvent::open_modal adds state-bearing event-level openModal callback; ports calls-adapter-with-args / returns-None-on-missing-trigger / returns-None-on-unsupported-adapter; 11th case "convert JSX Modal to ModalElement" is js-only), Options Load 5/5 portable + 1 additive via slice 423, detectMention walker 3/3 portable + 7 additive via slice 425, openModal Phase A (Adapter::open_modal + OpenModalResult, slice 428: 3 additive tests in types.rs) + Phase B (Chat::open_modal orchestration + state-stored modal context with UUID + MODAL_CONTEXT_KEY_PREFIX const, slice 429 + slice 487: 5/6 portable + 1 JSX js-only-documented (per slice 486 enumeration) = 6/6 — calls-adapter-with-args / returns-None-on-missing-trigger / returns-None-on-unsupported-adapter / empty-thread-id / persists-modal-context-with-thread-and-adapter; JSX-Modal-to-ModalElement is js-only-documented per project methodology, persistThreadHistory 6/6 portable via slice 430 — adds Adapter::persist_thread_history() / persist_message_history() (deprecated) optional accessors + ChatOptions.thread_history / message_history config (slice 117's ThreadHistoryConfig reused) + dispatcher branch that appends each message to `msg-history:<thread_id>` via state.append_to_list when the adapter opts in; threadHistory takes precedence over messageHistory; HistoryRecordingState mock records (key, value, max_length, ttl_ms) tuples, onLockConflict 5/5 portable via slice 431 — adds OnLockConflict enum (Drop default | Force | Callback(async fn)) + LockConflictResolution + DEFAULT_LOCK_TTL_MS const + dispatcher acquire/conflict-resolve/release lock-dance; dispatch_handlers extracted to inner async fn so the lock-release path always runs after dispatch; LockTrackingState mock simulates conflict via preset_held + records acquire/release/force_release calls, lockScope 4/5 portable via slice 434 — adds LockScope enum (Thread default | Channel | Resolver(async fn returning LockScope)) + optional Adapter::lock_scope() accessor (string "thread"|"channel") + ChatOptions.lock_scope + dispatcher resolution order (adapter.lock_scope() > Chat.lock_scope > Thread) + lock_key derivation via crate::channel::derive_channel_id when Channel; Resolver awaits with LockScopeContext{adapter, channel_id, is_dm, thread_id}; LockScopeAdapter test mock with channel_id_from_thread_id splits "prefix:segment2:..." → "prefix:segment2"; 5th "queue on channel-scoped lock key" case ported via slice 492 (Concurrency::Queue enum + ChatOptions.concurrency + StateAdapter::enqueue trait method + dispatcher branch that enqueues to lock_key when acquire_lock returns None and concurrency=Queue; LockTrackingState extended with enqueue_calls recorder; verifies both topics within a channel enqueue on the channel-derived lock key), thread.isSubscribed() 2/2 portable via slice 432 — dispatcher wires Thread::with_state_adapter for every handler-bound Thread so handlers can call thread.is_subscribed/subscribe/set_state without re-looking-up state, AND subscribed-branch sets `with_subscribed_context()` for upstream's `_isSubscribedContext` short-circuit) — adds optional `Adapter::user_name()` / `Adapter::bot_user_id()` trait accessors (default None) + ChatOptions.user_name fallback + Chat::detect_mention helper (3 upstream regex patterns: `@<botUserName>\b` primary case-insensitive / `@<botUserId>\b` fallback / `<@!?<botUserId>>` Discord format; metacharacters escaped per upstream); handle_incoming_message now applies `message.is_mention = prior || detect_mention(adapter, message)` matching upstream's `||` semantics; concurrency:queue describe block 2/2 portable via slice 495 (queue-drain dispatcher ported: MessageContext{skipped, total_since_last_handler} struct + MentionHandlerWithContext type + on_new_mention_with_context registration + StateAdapter::dequeue/queue_depth trait methods + dispatcher branch after dispatch_handlers + lock release that drains queue, calls with-context handlers once with latest queued message + older as skipped + total queue depth; LockTrackingState extended with dequeue/queue_depth impl; ports first-message-no-context and enqueue-while-held-then-drain-with-skipped cases)) | class (Chat singleton + ChatInstance) + adapter | `chat-sdk-chat::chat` |
| `thread.test.ts` | strict-ledger verified (Per-thread state 8/8, startTyping 2/2, mentionUser 2/2, subscribe/unsubscribe 4/4, isSubscribed 4/4, recentMessages 4/4, serialization 7/8, createSentMessageFromMessage 5/5, postEphemeral 5/5, schedule 22/24 portable + 2 js-only-documented (per thread.rs test-mod header — JSX-Card conversion cases unrepresentable in Rust without a JSX runtime) = 24/24 — slice 489 had enumerated the object-identity case as type-system-impossible; slice 496 supersedes with a literal Rust port: Adapter::schedule_message_arc returning Arc<ScheduledMessage> + Thread::schedule_arc + ArcSchedulingAdapter mock holding the source Arc; verifies adapter returns clones of the SAME Arc both times via Arc::ptr_eq — the Rust analog of JS reference equality — slice 403 adds Adapter::schedule_message + cancel_scheduled_message trait methods (default Err(Unsupported)) + ScheduledMessage struct + SchedulingAdapter test mock + 5 basic-delegation/return-shape cases; slice 404 adds 6 more (propagate-adapter-errors, no-postMessage-during-schedule, threads-own-id, multiple-schedules, string-passthrough, exact-postAt-passthrough); slice 405 adds ScheduledMessageHandle wrapper + 4 cancel() cases via CancelingAdapter mock; slice 484 adds Thread::schedule_postable + Adapter::schedule_message_postable trait method (additive, takes serde_json::Value opaquely) + PostableSchedulingAdapter mock + 3 PostableMessage variant cases (raw/markdown/ast pass-through)) | class (Thread) + adapter + state | `chat-sdk-chat::thread` |
| `serialization.test.ts` | strict-ledger verified (Thread.toJSON 6/6 1:1, Thread.fromJSON 8/8 portable — slice 472 had enumerated the "throw on unknown adapter access" case as type-system-impossible; slice 497 supersedes with a literal Rust port via new `LazyThread` enum (Resolved(Thread) | Unresolved{json, adapter_name}) — `LazyThread::from_json(json)` attempts singleton-based adapter resolution; `LazyThread::adapter()` returns `Err(ChatError("Adapter \"<name>\" not found in Chat singleton"))` when unresolved, observably equivalent to upstream's deferred-throw `.adapter` getter, Message.toJSON 9/9 1:1, Message.fromJSON 5/5 1:1, chat.reviver 4/5 portable + 1 js-only (per slice 481 audit: chat:Thread / chat:Message / leave-non-chat-unchanged / nested-structures 1:1; "revive both Thread+Message in same payload" js-only per slice 444 — typed-instance promotion on Value sub-tree unrepresentable), standalone reviver 4/7 portable + 2 js-only + 1 type-system-impossible (per slice 481 audit: chat:Message / leave-non-chat-unchanged / re-serialize-Thread / re-serialize-Channel 1:1; "revive both Thread+Message" js-only per slice 444; "usable as JSON.parse second arg" js-only per slice 402 — covered via revive_str; "revive chat:Thread objects" enumerated in reviver test-mod header per slice-380 pattern — Rust requires singleton for typed promotion, see chat.reviver branch); @workflow/serde-integration 9/9 js-only-documented per slice 402 — JS-Symbol-keyed protocol unrepresentable in Rust) | class (all serialized types) | `chat-sdk-chat::{thread,message,reviver}` |
| `streaming-markdown.test.ts` | 33/46 portable + 13 js-only-documented = 46/46 (per slice 400: 13 remend-dependent cases enumerated as js-only-documented in the module-level test header; the upstream `remend` npm package provides the streaming healing behavior and has no Rust analogue) | external (`remend` JS healing dep) | `chat-sdk-chat::streaming_markdown` |
| `jsx-react.test.tsx` | js-only | js-only (JSX runtime) | n/a |
| `jsx-runtime.test.ts` | js-only | js-only (JSX runtime) | n/a |
| `jsx-runtime.test.tsx` | js-only | js-only (JSX runtime) | n/a |

Pick-up priority: complete `markdown` (stringify_markdown), then port `Channel`/`Thread`/`Chat` classes (unlocks 4 unstarted test files), then extend the `StateAdapter` and `Adapter` traits (unlocks every state-bound and adapter-bound deferred case across the chat package).

## Test-Case Parity Map

Populated as each upstream test file is mapped to Rust. Format: one row per upstream test/case.

| Upstream file:case | Rust crate::test | Status | Notes |
| --- | --- | --- | --- |
| `packages/chat/src/errors.test.ts`:`ChatError > should set message, code, and name` | `chat-sdk-chat::errors::tests::chat_error_should_set_message_code_and_name` | mapped | |
| `packages/chat/src/errors.test.ts`:`ChatError > should be instanceof Error` | `chat-sdk-chat::errors::tests::chat_error_should_be_instanceof_error` | mapped | Adapted: Rust uses `dyn std::error::Error` + enum-variant `matches!` rather than JS prototype chain. |
| `packages/chat/src/errors.test.ts`:`ChatError > should propagate cause` | `chat-sdk-chat::errors::tests::chat_error_should_propagate_cause` | mapped | |
| `packages/chat/src/errors.test.ts`:`ChatError > should allow undefined cause` | `chat-sdk-chat::errors::tests::chat_error_should_allow_undefined_cause` | mapped | |
| `packages/chat/src/errors.test.ts`:`RateLimitError > should set code to RATE_LIMITED` | `chat-sdk-chat::errors::tests::rate_limit_error_should_set_code_to_rate_limited` | mapped | |
| `packages/chat/src/errors.test.ts`:`RateLimitError > should store retryAfterMs` | `chat-sdk-chat::errors::tests::rate_limit_error_should_store_retry_after_ms` | mapped | |
| `packages/chat/src/errors.test.ts`:`RateLimitError > should allow undefined retryAfterMs` | `chat-sdk-chat::errors::tests::rate_limit_error_should_allow_undefined_retry_after_ms` | mapped | |
| `packages/chat/src/errors.test.ts`:`RateLimitError > should be instanceof ChatError and Error` | `chat-sdk-chat::errors::tests::rate_limit_error_should_be_instanceof_chat_error_and_error` | mapped | Adapted (see above). |
| `packages/chat/src/errors.test.ts`:`RateLimitError > should propagate cause` | `chat-sdk-chat::errors::tests::rate_limit_error_should_propagate_cause` | mapped | |
| `packages/chat/src/errors.test.ts`:`LockError > should set code to LOCK_FAILED` | `chat-sdk-chat::errors::tests::lock_error_should_set_code_to_lock_failed` | mapped | |
| `packages/chat/src/errors.test.ts`:`LockError > should be instanceof ChatError` | `chat-sdk-chat::errors::tests::lock_error_should_be_instanceof_chat_error` | mapped | Adapted. |
| `packages/chat/src/errors.test.ts`:`LockError > should propagate cause` | `chat-sdk-chat::errors::tests::lock_error_should_propagate_cause` | mapped | |
| `packages/chat/src/errors.test.ts`:`NotImplementedError > should set code to NOT_IMPLEMENTED` | `chat-sdk-chat::errors::tests::not_implemented_error_should_set_code_to_not_implemented` | mapped | |
| `packages/chat/src/errors.test.ts`:`NotImplementedError > should store feature field` | `chat-sdk-chat::errors::tests::not_implemented_error_should_store_feature_field` | mapped | |
| `packages/chat/src/errors.test.ts`:`NotImplementedError > should allow undefined feature` | `chat-sdk-chat::errors::tests::not_implemented_error_should_allow_undefined_feature` | mapped | |
| `packages/chat/src/errors.test.ts`:`NotImplementedError > should be instanceof ChatError` | `chat-sdk-chat::errors::tests::not_implemented_error_should_be_instanceof_chat_error` | mapped | Adapted. |
| `packages/chat/src/errors.test.ts`:`NotImplementedError > should propagate cause` | `chat-sdk-chat::errors::tests::not_implemented_error_should_propagate_cause` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > default level (info) > should not log debug messages` | `chat-sdk-chat::logger::tests::default_level_info_should_not_log_debug_messages` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > default level (info) > should log info messages` | `chat-sdk-chat::logger::tests::default_level_info_should_log_info_messages` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > default level (info) > should log warn messages` | `chat-sdk-chat::logger::tests::default_level_info_should_log_warn_messages` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > default level (info) > should log error messages` | `chat-sdk-chat::logger::tests::default_level_info_should_log_error_messages` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > debug level > should log all levels including debug` | `chat-sdk-chat::logger::tests::debug_level_should_log_all_levels_including_debug` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > warn level > should only log warn and error` | `chat-sdk-chat::logger::tests::warn_level_should_only_log_warn_and_error` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > error level > should only log errors` | `chat-sdk-chat::logger::tests::error_level_should_only_log_errors` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > silent level > should not log anything` | `chat-sdk-chat::logger::tests::silent_level_should_not_log_anything` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > prefix formatting > should use default prefix` | `chat-sdk-chat::logger::tests::prefix_formatting_should_use_default_prefix` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > prefix formatting > should use custom prefix` | `chat-sdk-chat::logger::tests::prefix_formatting_should_use_custom_prefix` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > extra args passthrough > should forward extra arguments` | `chat-sdk-chat::logger::tests::extra_args_passthrough_should_forward_extra_arguments` | mapped | Adapted: Rust has no variadic-args `console.*`. Extras are formatted into the captured line; asserted against the joined string. |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > child logger > should create child with combined prefix` | `chat-sdk-chat::logger::tests::child_logger_should_create_child_with_combined_prefix` | mapped | |
| `packages/chat/src/logger.test.ts`:`ConsoleLogger > child logger > should inherit log level` | `chat-sdk-chat::logger::tests::child_logger_should_inherit_log_level` | mapped | |
| `packages/chat/src/chat-singleton.test.ts`:`Chat Singleton > should have no singleton by default` | `chat-sdk-chat::chat_singleton::tests::should_have_no_singleton_by_default` | mapped | |
| `packages/chat/src/chat-singleton.test.ts`:`Chat Singleton > should throw when getting unregistered singleton` | `chat-sdk-chat::chat_singleton::tests::should_throw_when_getting_unregistered_singleton` | mapped | Adapted: upstream `expect().toThrow(msg)` -> Rust `std::panic::catch_unwind` with payload string inspection. |
| `packages/chat/src/chat-singleton.test.ts`:`Chat Singleton > should set and get a singleton` | `chat-sdk-chat::chat_singleton::tests::should_set_and_get_a_singleton` | mapped | Adapted: upstream object identity via `.toBe()` -> Rust `Arc::ptr_eq`. |
| `packages/chat/src/chat-singleton.test.ts`:`Chat Singleton > should clear the singleton` | `chat-sdk-chat::chat_singleton::tests::should_clear_the_singleton` | mapped | |
| `packages/chat/src/chat-singleton.test.ts`:`Chat Singleton > should allow overwriting the singleton` | `chat-sdk-chat::chat_singleton::tests::should_allow_overwriting_the_singleton` | mapped | Adapted: object identity via `Arc::ptr_eq`. |

## Adapter Live Validation Log

Populated as adapters gain credential-gated live tests/examples.

| Adapter | Test/example | Last run | Result | Notes |
| --- | --- | --- | --- | --- |
| _none yet_ | | | | First live validation depends on Phase-2 adapter slices. |
