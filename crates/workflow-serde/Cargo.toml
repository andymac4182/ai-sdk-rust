[package]
name = "workflow-serde"
version = "0.1.0"
edition = "2024"
rust-version = "1.85.0"
description = "Rust crate for the standalone Vercel Workflow serialization marker package."
license = "Apache-2.0"
repository = "https://github.com/andymac4182/ai-sdk-rust"
readme = "../../README.md"

[lib]
name = "workflow_serde"
path = "src/lib.rs"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
