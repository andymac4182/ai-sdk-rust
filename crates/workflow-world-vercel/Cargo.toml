[package]
name = "workflow-world-vercel"
version = "0.1.0"
edition = "2024"
rust-version = "1.85.0"
description = "Rust port of the standalone Vercel Workflow world-vercel package."
license = "Apache-2.0"
repository = "https://github.com/andymac4182/ai-sdk-rust"
readme = "../../README.md"

[lib]
name = "workflow_world_vercel"
path = "src/lib.rs"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"

[dependencies]
base64 = "0.22"
hmac = "0.12"
serde_json = "1.0.149"
sha2 = "0.10"
workflow-world = { path = "../workflow-world" }

